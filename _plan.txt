<timestamp>Saturday, Jul 18, 2026, 8:11 AM (UTC+10)</timestamp>
<user_query>
Part 4 — Cursor prompts (paste‑ready)

Iron rules: locate‑don't‑guess; one commit per task; don't git add -A; extend goldens before changing render code; no behaviour change except the Part 2 decisions.

P‑1 — descriptor SoT types + goldens

On refactor/expertgrid-consolidation. In lib/mediaplan/expertGridChannelConfig.ts, extend the field descriptor type with: surfaces?: "grid"|"card"|"both" (default "both"), placeholder?: string, cardSpan?: 1|2, and a channel-level optionFlags: {key:string;label:string}[] (migrate from billingFlagKeys/billingFlagLabels; keep back-compat exports). Mark current grid-only fields (unitRate, and the trailing Net Media/Σ qty) surfaces:"grid". Do NOT change any label text yet. Then EXTEND the grid goldens (test:expert-goldens) to also pin map*ExpertRows + computeLoadedDeliverables for Radio, Cinema, DigitalVideo (currently uncovered). Run goldens — must stay green. Commit: "feat(expertgrid): descriptor gains surfaces/optionFlags; goldens extended to divergent channels".
</user_query>