import { pageMetadata } from "@/lib/nav/routeManifest"

export const metadata = pageMetadata("/admin/media-container-best-practice")

export default function AdminBestPracticeLayout({ children }: { children: React.ReactNode }) {
  return children
}
