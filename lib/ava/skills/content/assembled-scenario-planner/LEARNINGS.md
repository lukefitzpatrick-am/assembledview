# Learnings — assembled-scenario-planner

- Always call `run_scenario`. Never compute a what-if in prose.
- `8k` means 8000 dollars. Cap figures are dollars per day.
- If `rate.basis` is plan, say the plan rate was used. Never invent a delivered rate.
