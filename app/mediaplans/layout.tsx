import { pageMetadata } from "@/lib/nav/routeManifest"

export const metadata = pageMetadata("/mediaplans")

export default function MediaplansLayout({ children }: { children: React.ReactNode }) {
  return children
}
