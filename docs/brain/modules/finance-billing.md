# Module: Finance & Billing

Admin-only **Finance sections IA** (FN7 + FIN-1): sidebar **Finance** = Clients billing (`/finance/invoicing`, tabs Periods + Xero) · Publishers (`/finance/costs/*`) · Forecasting · Investment. Bare `/finance` and retired Overview (`/finance/home`, `?tab=overview`) redirect to invoicing. Classic tabbed `FinanceHubPageClient` deleted. Legacy `?tab=` + path aliases permanently redirect (FN1 map). Forecasting: `?fmode=booked|target|variance`, FY-only scope bar. Booked forecast plan crawl still Xano-shaped (`fetchFinanceForecastRawFromXano`) until T6. Snowflake is not involved in finance. Default parse is still `billingSchedule`/`deliverySchedule` blobs; `DATA_BACKEND_FINANCE_SCHEDULE=blob|shadow|rows` (default `blob`) rebuilds the same `BillingMonth[]` from Postgres `schedule_months` via `lib/finance/scheduleMonthsSource.ts` (inverse of `explodeScheduleToMonthRows`). `shadow` serves blob and records domain `finance-schedule` diffs; `rows` serves rows-derived with blob fallback on ZERO rows (DISPOSITIONS §E). Admin `?probe=finance-schedule` emits the published-version CSV. Only status (billed/notes/invoice) is overlaid from persisted `finance_billing_records`. Hub API gates: most routes `requireFinanceAdmin` (admin); accrual + forecast reads `requireRole(admin|manager)` (SEC-C); periods/xero-match use admin|manager with admin-only write-offs.

## The fee engine — the seam that matters most

- **`lib/finance/computeCampaignFinancials.ts` is the shared planning+finance engine** — it merely lives under `lib/finance/`. Both mega-pages import it; `recomputeBillingScheduleOnSave` gates every schedule PATCH with it ($0.01 equality → 409 `BILLING_SCHEDULE_DIVERGENCE`).
- It composes the canonical primitives from the planning domain: `computeBurstAmounts` (fee math — see INVARIANTS), `computeBillingAndDeliveryMonths`, `prorateAcrossMonths`, `deliverableBudget`.
- **Plan-C S1-P1b:** server-generated `billingSchedule` / `deliverySchedule` attach `month.lineItems` via `attachScheduleLineDetail` (id = stable line id, `monthlyAmounts` + `feeMonthlyAmounts` from resolved lines / `prorateBurstFeesToMonths`). Header ↔ lineItems sum invariant (±$0.01); assert throws when `PLANC_LINE_DETAIL_ASSERT=1`. `PLANC_SERVER_AUTHORITY=enforce` remains OFF — separate flip.
- Fee % resolution: `resolveFeePctFromFeeLoading` → `FEE_FIELD_BY_MEDIA` (client `fee*` columns per channel). production→0; influencers→`feecontentcreator` fallback; integration→no fallback. `normaliseScheduleMediaType` returns **null** for unknown types (C-9 FIXED) — fee resolution warns `[builder-issue]` and uses 0%; schedule placement may still fall back to search as a last resort.
- Ad serving + production are first-class in the PC2 full-scope gate (`approved_slice` + `SAVE_GATE_FULL_SCOPE`). Adserving is a `schedule_component` enum value; production remains media on production lines.
- One fee engine: `generateBillingLineItems` and `computeDerivedCampaignFeeAmount` consume `computeBurstAmounts` (C-7/C-8 FIXED). Agency fee month drift tolerance = $0.01.
- Publish freezes `media_plan_versions.approved_slice` once. **`mba_fee_snapshots` persist only on `mode=publish`** (not `draft` / not bare `new_version`) — **independent of `campaign_status`** (live: krusty012 v3 = status `draft` + snapshot). Request bodies may still carry `feeSnapshot`. Upsert by default; `FEE_SNAPSHOT_WRITE_ONCE=on` → insert-once + admin `POST /api/admin/fee-snapshots/resnapshot` overwrite.

## Key files

- `components/finance/sections/*` + `lib/finance/sections/*` — section pages/shell/scope. `lib/finance/useFinanceStore.ts` remains for `EditableFinanceGrid` / shared fetch helpers (not hub chrome).
- **Invoicing Excel** (`/finance/invoicing`) = bookkeeper workbook via `exportReceivablesWorkbook` (`lib/finance/exportFinanceHub.ts` → `writeMediaFinanceWorksheet` / `writeSowFinanceWorksheet` / per-retainer `writeRetainerFinanceWorksheet`). Sheets: Media, Scopes, one sheet per retainer client. Legal business name + ABN per client block from `GET /api/clients` (`loadFinanceExcelClientMeta`); empty `legalbusinessname` falls back to display name and the missing set is toasted on export. CSV stays flat (`exportBillingRecordsCsv`). Flat XLSX (`exportFlatBillingWorkbook`) is not the primary Excel button. **FIN-6 / F-28:** Simmone Logue + Olive Grove legal name/ABN (and their internal buckets) stay blank by Luke decision — update later in Clients admin; do not treat as export bugs.
- **Invoicing toolbar (FIN-2):** one card (`InvoicingToolbar`) — scope (FY/months/clients + Apply) and local filters (type/status/publishers/search/drafts + CSV/Excel). Scope still Apply-gated; filters still on-change.
- **Forecasting auto-load (FIN-4):** booked forecast fetches on cold entry for current AU FY + all clients (`buildDefaultFinanceScope`); scope bar still Apply-gated for FY changes. `shouldAutoReloadForecast` always allows the effect.
- **Forecast row model (FIN-5):** UI = Fees (service fee digital + retainer) → Commissions (three existing buckets; caption discloses client fixed fees remain blended) → optional other revenue → Total revenue → Media by container type → publisher → MBA. Builder still emits AA/AM entity billing lines; additive `media_billing` lines carry `media_type_key` / `media_type_label` / `publisher_name` (`schema_version` `finance_forecast_v1.1`). Identity: `sum(media_billing) ≡ AA+AM`; Fees+Commissions (+ other when present) ≡ revenue body ex `total_revenue`. Media breakouts are excluded from snapshot persistence (comparison key lacks media/publisher grain). Portfolio/billing subtotals use AA+AM only. Gate helper: `lib/finance/forecast/forecastPresentationIdentity.ts`; live check `scripts/verify/fin5-forecast-identity.ts`.
- `lib/finance/composeFinanceHubRecords.ts` — shared derive → dedupe (`receivableMergeKey`) → month filter → status overlay pipeline; multi-month is byte-identical to concatenated single months.
- `lib/finance/derive{Receivable,Payable,ScopeSow,Retainer}Records.ts` — version/SOW/retainer → synthetic records. Record `total` = schedule month header ex-GST (`monthExGstFromScheduleEntry`), NOT re-derived from raw line items. SOW months: billing schedule is authoritative — missing or $0 months contribute nothing; never fall back to full scope `cost` (`summarizeScopeScheduleCoverage` surfaces gaps for the list UI).
- `lib/finance/computeCampaignFinancialsFromVersion.ts` — hydrate path from persisted JSON (or PC1 attached `schedule_months` rebuild); returns empty `deliveryVsBillingDelta`, no `reconciliation` — silent falsy defaults, not "unknown".
- `lib/finance/scheduleMonthsSource.ts` — PC1 `DATA_BACKEND_FINANCE_SCHEDULE` gate; cents→dollars only via `centsToDollars`; `hydrateVersionsFinanceScheduleSource` wired into billing/payables/accrual/forecast loaders.
- `lib/finance/relevantPlanVersions.ts` — which version is authoritative per month (from `master.version_number` only; staged versions never relevant); 30s cache cleared only by the schedule PATCH route.
- `lib/finance/overlayFinanceStatus.ts` (`invoice_key`), `billedDrift.ts` (FNV-1a hash of billed line set → `billed_drift` flags on later edits), `computeAccrual.ts`.
- `lib/finance/forecast/` — `buildFinanceForecastDataset.ts` (1.1k lines; derived booked projection for an AU FY; full version-history crawl BY DESIGN — do not switch to the latest view), `mapping/definitions.ts` (field mappings, commission buckets, the `≤1 = decimal fraction` scale heuristic), snapshots (immutable, hashed, process-local duplicate guard), `varianceEngine` (snapshot A vs B) + `targetVsActual` (targets in `revenue_forecast_lines` vs `billed_amount`, hardcoded RAG bands).
- `lib/billing/` — `computeSchedule`, `buildBillingSchedule` (drops $0 lines — the reason `injectMissingFeeLinesFromMedia` exists for client-pays fee lines), `parsePersistedBillingScheduleToMonths` (legacy-shape inference), `seedLineFees`, `applyBillingLineMode` (billingMode auto/manual — see INVARIANTS), `exportBillingScheduleExcel`, **PC4** `balancer.ts` / `collisionWorksheet.ts` / `rebalanceLineOnSchedule.ts` (flag `NEXT_PUBLIC_BILLING_BALANCER`, default **off**), **O4** `postgresAutoBillingCorrection.ts` (AUTO-only drift never blocks `WRITE_BACKEND=postgres`; post-save correction toast; Xano `recomputeAndValidate` reject path unchanged).
- `lib/generateBillingSchedulePDF.ts` — PDF helper; former sole consumer `/api/campaigns/.../billing-schedule` retired (X3 410).

## Write path (planning → finance)

Editor → `buildEditorLineItemInputs` → `PATCH /api/mediaplans/versions/[id]/billing-schedule` → fetch `billing_overrides` → recompute + validate (409s: `BILLING_OVERRIDE_SUM_VIOLATION`, `BILLING_SCHEDULE_DIVERGENCE`) → `writeBillingSchedule` updates PG `legacy_schedules` + billing `schedule_months` (delivery only when C1 regenerates) → clear relevantPlanVersions cache → audit diff to `finance_edits`.

## Consumed by

61 files import `lib/finance/*`: both mega-pages (edit page has ~25 finance/billing imports), `components/billing/*` (9), client hub billing + Excel export, dashboard chart reshapes, AVA client-brain tools, creative upload digest (via `lib/billing/scheduleHeaders`). All 19 media containers import `lib/billing/*`.

## Gotchas (verified)

- Two engines produce `CampaignFinancials` (line items vs persisted JSON) with silently different completeness. `mbaScopeFromSchedules` prefers the **delivery** schedule to avoid double client-pays subtraction — fragile, untested boundary.
- `receivableMergeKey`/`composeInvoiceKey` changes orphan persisted status rows (billed reappears as unbilled).
- Payment terms "Net 30" hardcoded ×4; GST 10% centralised in `lib/finance/gst.ts` (good).
- Synthetic client IDs from djb2 name-hash fallback (C-16); synthetic record ids are per-request counters — store updates can't address derived rows.
- Three caches (relevantPlanVersions 30s / forecast raw 30s / forecast dataset 20s) with no shared invalidation; snapshot duplicate guard is process-local.
- Saved views: Xano `finance_saved_views` (legacy hub `hubSavedViews` localStorage retired with hub).
- Fee overrides are un-gated by construction; media overrides are gated — asymmetric.
- `FINANCE-HUB-STAGES-DISCOVERY.md` is historical — the tabs directory it describes no longer exists.
- Finance table reads (`finance_billing_records`, `finance_billing_line_items`, `finance_edits`, `finance_saved_views`, `billing_overrides`, `revenue_forecast_lines`, `revenue_line_catalog`, `scope_of_work`) go through `lib/data/readFinance.ts` + `DATA_BACKEND_FINANCE` / `DATA_BACKEND`. Scope-of-work **writes** (create/update/getById for scopes UI + PDF/scope-id) use `lib/data/writeScopeOfWork.ts` (Postgres). Month-relevant plan crawl (`lib/finance/relevantPlanVersions.ts`) uses `readPlanMasters`/`readPlanVersions` when `DATA_BACKEND_PLANS=postgres` (X3). Legacy hub `/api/finance/data` uses that crawl + `readClients`/`readPublishers`. Writes for billing upserts and finance_edits POST stay on Xano; overrides replace/reset → `writeBillingOverrides` (X2); Alter Billing PATCH → `writeBillingSchedule` (X3). **Forecast targets** read/write Postgres (`pgTargetLines`). **Forecast snapshots** read/write Postgres (`pgSnapshots`, migration `0016`; `XANO_FINANCE_FORECAST_SNAPSHOTS_*` author-only for data-move). **Booked forecast** still loads plan versions via `fetchFinanceForecastRawFromXano` (full `media_plan_versions` page + clients/publishers), then optional `hydrateVersionsFinanceScheduleSource` — flag for T6 rewire (do not half-cut in FN forecasting chrome). Shadow compare tags PG-deduped / Xano-duplicated extras as `duplicate-class` (EXPECTED) vs unexpected; `/api/admin/migration-diffs` surfaces the split (`financeDiffSplit`, `?probe=finance`). Billing-record overlay and billing_overrides reads throw on failure (M7 — never soft-fail to “no billed / no overrides”); see `docs/brain/READ-FAILURE-REGISTER.md`.
- **`mba_line_approvals` (PC0 / X1, FIXED `48654e11`):** `/api/mba-line-approvals` reads via `lib/data/readApprovals.ts` (`DATA_BACKEND_APPROVALS` / `DATA_BACKEND`); writes via `lib/data/writeApprovals.ts` follow `WRITE_BACKEND` with **no Xano mirror**. Absence of rows ⇒ all approved (fail-soft contract in `mbaLineApprovalsClient.ts`). Browser GET goes through `coalescedGetJson` (URL-keyed in-flight + 30s TTL); successful PATCH invalidates that key so edit-page hydrate + post-save refetch share one request without stale approvals. Postgres table + `ava_readonly` allowlist in `db/migrations/0007_mba_line_approvals.sql` (author-only apply). **Postgres-authoritative:** excluded from `db:etl` truncate-reload; recon reports counts but never fails on mismatch. Export still snapshots the Xano table for shadow. Shadow compare strips PG-only `created_at` (Xano X5.1 table has none).
- Postgres `finance_billing_records` stores `billed_amount_cents` + `billed_lines_hash` as real columns; API shape still exposes `billed_amount` (dollars). `finance_saved_views.user` → `user_id` rename is honourred on read/ETL. Dual `invoice_key` schemes (`media:`/`sow:`/`retainer:`/`xero:`) port verbatim.
- **Xero sync (T5, parity mode):** `app/api/cron/xero-sync` writes Postgres only via `lib/xero/*` (ingest → billing import → PDF Blob → contacts → **PC6 match_run_items**). Xano `daily_xero_sync` stays active until T6. Matcher = `lib/xero/matchMba.ts` (MBA token) + `lib/xero/matcher/threeTier.ts` (run-item 3-tier). Client aliases live in `xero_client_aliases` (migration 0006); learned links in `xero_contact_links` (0011). Never touch `media:`/`sow:`/`retainer:` invoice_key rows. Parity: `npm run db:xero-parity` after a cron run, before `db:etl`. Local morning check: `npm run db:xero-sync` then `npm run db:xero-parity` (or Windows task `AV Xero daily parity` → `scripts/xero-daily.ps1` → `xero-daily.log`).
- **PC6 Xero↔run-item matcher:** migration `0011_xero_invoice_matches.sql` (AUTHOR ONLY). Tier 1 reference+≤$0.01 silent match; reference+amount diverge → card; tier 2 contact+amount+month suggestion; tier 3 manual teaches `xero_contact_links`. Duplicate/orphan passes → cards. Actions via `POST /api/finance/xero-match` (write-off **admin only**, reason mandatory). **O7:** day-10 escalation is one card per `periodMonth` (never daily re-notify); Dispute pre-creates `xero_match_expected_credit_note` with contact+amount; negative AR / `ACCRECCREDIT` ingest auto-reconciles disputed matches within $0.01. Hit-rate → `xero_match_month_metrics` → PC5 pre-run nudge. Report-only replay: `npm run replay:pc6-matcher`.
- **Xero exceptions assignment UX (FIN-7):** `/finance/xero` pending table leads with `reference` → first line `Description` → `invoice_number` (never bare `invoice_key`; key is hover/expando). `GET /api/finance/xero-queue` enriches pending from `xero_ar_invoices` + `xero_contacts` and returns `mbaOptions` from `media_plan_masters`. Actions cascade: assign client → MBA select filtered by `client_id` (label `mba · campaign`) → Set; free-text MBA fallback when unmatched/empty. Matching / `has_pending_edits` rules unchanged. Fill-rate probe: `scripts/verify/fin7-pending-field-fill.ts`.
- Editor still saves via Xano until `WRITE_BACKEND=postgres`; when flipped, create/edit call `POST /api/plans/save` (T4c) which writes `schedule_months` from T4a.0-enriched `computeCampaignFinancials` schedules. Finance derive (PC1 `rows`/`shadow`) and PC5 run items read that tip — save-path correctness is a finance contract, not only a planning one.
- **MB-1 save-path override attach:** `savePlanVersion` loads `billing_overrides` for the resolved `versionId` **before** `computeCampaignFinancials`, so `legacy_schedules.billingSchedule` stamps `billingMode:'manual'` and month amounts that match override cents. `DATA_BACKEND_FINANCE_SCHEDULE` default `blob` (and `shadow`) serve that blob — computing without overrides left schedule_months `source='override'` while the blob stayed auto (supabase001 v1162). Delivery schedule amounts remain override-free; `reconcileOverrideSources` still stamps `source='override'`.
- **MB-12 delivery metadata is auto-only:** `attachScheduleLineDetail` stamps `billingMode` / `feeBillingMode` / `preBill` only when `basis === "billing"`. Delivery lineItems stay `auto` with no `preBill` even when overrides are attached for the billing blob (money was already basis-correct; metadata was lying after MB-1).
- **MB-13 fee override vs freeze/snapshot:** first publish with a fee `billing_overrides` row freezes `approved_slice.feeCents` from the override-attached financials (`mbaScopeTotals.fee` = override sum). `mba_fee_snapshots.fees` remains fee-rate JSON (`feeSnapshot` / `feeLoading`), not dollar overrides. Media+fee Prebill = two override rows; `reset_line` without component clears both (draft/planned only after MB-15c).
- **MB-15c published billing immutable:** `replaceBillingOverrideLine` / `resetBillingOverrideLine` refuse when `isApprovedOrBeyond(campaign_status)` (same helper as MBA generate — `lib/docs/isApprovedOrBeyond.ts`) with `VERSION_PUBLISHED_IMMUTABLE` (API 409). Timing UI is read-only with `publishedBillingTimingLockedMessage`. Draft in-place writes unchanged. MB-2 `carryBillingOverridesToNewVersion` stays (TRANSITIONAL until VC-3). Product gap: published-plan retiming only via new version until Revisions (KNOWN MB-16).
- **MB-2 publish override carry:** `billing_overrides.version_id` is per version row. On `publish`/`new_version`, after the new version + line_items land, copy tip−1 overrides onto the new `versionId` **before** the MB-1 override read (same `line_item_id`/`component`/`mode`/`reason`/`months`/`date_basis`). Overrides for deleted lines are dropped and returned as `droppedBillingOverrides`; one `app_notifications` row (`billing_overrides_publish_carry`) when anything was carried or dropped. C2 still blocks when copied months no longer sum to the new line media — PC4 collision worksheet stays client-side (edit ~7271 persist to the pre-increment version is still needed so the base tip holds the collision-resolved shape that MB-2 copies).
- **MB-11 living-id membership is canonical:** `livingLineItemIds` for MB-2 carry is built via `buildCanonicalBillingLineIdSet`; membership uses `canonicalBillingLineIdSetHas` so a decorated schedule id matches a bare `billing_overrides.line_item_id` (and vice versa). Same rule for `collectScheduleLinesById` and other billing-line Map/Set keys — CI `check:billing-line-id-equality` flags raw `.has/.get/.set/.delete(lineItemId)`.
- **MB-3 reset undoes schedule_months mirror:** `resetBillingOverrideLine` deletes override rows then restores billing-basis `schedule_months` for that (version, line, component) to override-free `computeCampaignFinancials` amounts with `source='computed'` (never copy delivery — client-pays/fee differ). `replace_line` expands payload months to the full auto span (missing → $0) before mirror and server-gates media sum ≡ line media ±$0.01 (409 `SUM_VIOLATION` + delta). Both paths reaffirm `legacy_schedules` from remaining overrides. Edit per-line reset must not clear local state when `resolveMediaPlanVersionRowId()` is null.
- **Postgres save modes → finance tip (O3 pins):** `resolvePostgresSaveMode` only — Draft+tip+`!forceIncrement` → in-place `draft` overwrite (same `version_number`); leave-draft / non-draft / `forceIncrement` → `publish` increment. Tip present + lazy-empty version history still increments (never resets to v1). Status vocab persisted lowercase: `draft|planned|approved|booked|completed|cancelled`. Publish with unmapped status → `MISSING_CAMPAIGN_STATUS` (never silent `"Approved"`). 23505: `line_items_version_id_line_item_id_key` → `DUPLICATE_LINE_ITEM_ID`; versions master/version UNIQUE → `VERSION_ALREADY_EXISTS`.
- **O4 AUTO billing on postgres save:** client preview AUTO drift vs burst `autoReference` does not block `WRITE_BACKEND=postgres`; server recomputes schedules; response may carry `billingCorrection` toast. Manual C2 sum gates and Xano `recomputeAndValidate` reject path unchanged.
- **O4.5 feeLoading on publish:** T4c bodies use `assemblePlansSaveRequestBody` so draft overwrite and publish/status-change both carry `feeLoading` (+ `adservaudio`). Snapshot `feePct` from `stampClientFeePctOnLineItems` is preserved in `buildSavePlanLineItemsFromSnapshots`. Empty fee inputs → media-only `schedule_months` (krusty015 v3 wipe); `[savePlan-fee-zero]` tripwire logs when resolved feePct > 0 but fee total is $0.
- **PC4 billing balancer:** when `NEXT_PUBLIC_BILLING_BALANCER=on`, AlterBillingDialog / LineTimingInlineEditor / ManualBillingSpreadsheet* lock one ⚖ month per line; collision worksheet on edit postgres save pauses for affected manual lines only. `schedule_source` stays `computed|override` (no `balancing` value). Create page gets balancer cell lock + rebalance on commit; collision pause is edit-path only (create has no prior override→booked delta). No save idempotency key yet — collision decisions ride the same save attempt. Default **off**.
- **Adjust timing line ids (C-34 / BUX-1 / MB-4):** persisted `billingSchedule.lineItems[].id` is often the bare `line_items.line_item_id`; MBA scope + seed use `billing-{mediaType}::{raw}`. All timing getters/injects/appends, Prebill on/off (`applyLinePrebillToMonths` / `restoreLinePrebillSnapshot`), cell edit / rebalance media-key find, **and** `compareBillingDivergence` row matching must use `billingOverrideLineIdsMatch` / canonical `toBillingOverrideLineItemId` (never strict `.id === lineItemId`). CI fails on that pattern via `npm run check:billing-line-id-equality`. When working months lack line detail, Edit Billing / Adjust timing borrows the computed auto schedule so fresh drafts are not stuck at $0. Untouched drafts must not surface the MBA billing divergence banner.
- **MBA Save billing changes (BUX-2):** `handleManualBillingSave` must always toast (or open the fee-drift dialog). Only `validateBillingBeforeSave` **blockingErrors** gate the override PATCH; `preservedManualOverrides` (manual≠burst after an intentional month edit) are notices — same parity as campaign save — not a silent early return. Blocking errors also open Advanced editor where the detail panel lives.
- **MB-6 Save must not strand empty draft:** on override persist success, close Advanced only — rebuild `manualBillingMonths` from fetched `billing_overrides` overlaid on auto reference (`rebuildTimingDraftAfterBillingSave`) while MBA modal stays open with `draftReady`. Never wipe months while leaving `manualBillingDraftReady` true. Draft teardown (Done / modal close / full reset) clears months + ready + `manualBillingOverrideMetaRef` together. `validateManualMediaMonthsSum` / `validateAgencyFeeMonthTotalDrift` skip when months are empty (absent draft ≠ off-by-line-total).
- **MB-7 single modal billing view:** `resolveMbaBillingModalState` is the only derivation MbaBillingModal reads — core line inputs + override rows, with open `manualBillingMonths` layered on top. Healthy draft session: `financials.billingSchedule` and `resolvedMonths` are the same array (left inputs ≡ right Media column). `draftReady` + empty months → `viewReady=false` and both halves empty. Persist / `savePlan` / override API unchanged; write layer remains `setManualBillingMonths`. Invariant: `assertMbaBillingModalMonthsAgree`.
- **MB-20 pending billing carrier:** edit page holds `pendingBillingOverrideRows` (+ `pendingBillingOverrideMetaRef`) as the client statement of Applied-but-not-campaign-saved manual timing. Built only via `buildPendingBillingOverrideRows` → `layerDraftMonthsOntoOverrideRows` (no second derivation). `resolveBillingOverrideRowsForModal` precedence: **pending (unsaved) > table (saved `billingOverrideRowsForPanels`) > computed auto**. Apply (= former Save billing changes) promotes draft → pending + working **and** still calls `persistManualBillingOverrides` (DB write removed in MB-23). Done closes the timing draft but keeps pending; Cancel / Dialog X / Escape / footer Cancel discard draft + pending. Per-line Reset to auto clears that line from pending (DB `reset_line` unchanged).
- **MB-10 fee dialog one source:** `monthsForMbaBillingGates(state, draftFallback)` is the only months selection for fee-drift gate **and** displayed month-fee sums (Advanced "Current sum", fee row Total on edit+create). Never a parallel raw-draft `reduce` of `manualBillingMonths.feeTotal`. Display figure = `validateAgencyFeeMonthTotalDrift(monthsForGates, derived).sumOfMonthFeeTotals`.
- **MB-14 post-persist refetch loud recovery:** after successful Save billing changes, empty override refetch (or throw) still retains prior `manualBillingOverrideMetaRef` / just-saved months, but must `console.warn` versionId+mba+persist counts and insert `app_notifications` (`kind=billing_overrides_refetch_anomaly`, audience `admin`) via `POST /api/billing-overrides/refetch-anomaly`. Never soft-swallow the contradiction.
- **Save billing / Prebill persist (BUX-6):** client Save hits `POST /api/billing-overrides/replace_line` (not X3 billing-schedule alone). Payload = `{ media_plan_version_id, mba_number, line_item_id (bare), component, mode:'manual', reason, months:[{month:'YYYY-MM', amount}], date_basis }`. `applyBillingLineMode` must use `billingOverrideLineIdsMatch` (C-34). Empty replace counts must not toast success. Server upserts `billing_overrides` and mirrors billing-basis `schedule_months` with `source='override'` (enum has no `manual`; delivery-basis rows stay `computed`). UI hydrate: `billing_overrides` → `attachOverridesToLineInputs` / modal `applyBillingOverrideRowsToMonths`; finance tip prefers override billing months when present.
- **MB-17 months[0] overlay contract:** Manual billing grid reads line amounts from `manualBillingMonths[0]` only (`resolveManualBillingLineItemAmount`). `applyBillingOverrideRowsToMonths` must write each override month onto **every** month row's matching line via `syncLineItemMonthlyAmountAcrossAllMonthRows` / `syncLineItemFeeMonthlyAmountAcrossAllMonthRows` (mediaKey resolved with `collectScheduleLinesById`) — never mutate only `next.find(monthYear)`. Same contract as Prebill (`applyLinePrebillToMonths`). Do not "fix" by reading the per-month row instead of months[0] (MB-4 / C-34 $0-duplicate heuristic).
- **MB-18 overrides load notice:** `billingOverridesLoadNotice` is set only on a real `fetchBillingOverridesClient` throw (`nextBillingOverridesLoadNotice({ kind: "fetch_failed" })`). Unresolved version id (`version_unresolved`) and fetch ok clear it — including `refreshBillingOverrideRowsForPanels` early-return. Never treat missing id as load failure. Null mediaKey on overlay: `warnBillingOverrideMediaKeyMiss` (console.warn once per line id); control flow stays no-op.
- **MB-19 orphan override skip on save:** `persistManualBillingOverrides` never aborts the whole save on one empty `monthsIso` (deleted-line orphan via BUX-6 meta fallback). Per-line `continue` + `skippedEmptyMonths` in the result; toast via `manualBillingPersistSkipNotice`. Does not `reset_line` orphans (cannot tell deletion from hydrate lag); publish carry still drops deleted-line overrides (MB-2). BUX-6 meta fallback unchanged.
- **MbaBillingModal chrome (BUX-3 / MB-9):** header status row is MBA version · approval · billing mode (`Auto billing` / `Manual billing — N lines`, spaced via `manualBillingHeaderLabel`) only — no glyph jargon chips. One vocabulary (`lib/billing/manualBillingVocabulary.ts`): Manual / Media prepaid / Prepaid / Fee adjusted / Client pays — same word on container pill, line badge, timing editor, and schedule title pills. Amber month dots for deliberate timing are suppressed (false alarms).
- **MB-9 divergence banner gate:** `BillingDivergenceBanner` mounts only when `isUnintendedBillingDivergence` — line kinds `line_total` | `adserving_total` | `missing_in_saved` | `missing_in_computed`. Month-header redistribution alone (reconciling Prebill / Adjust timing) is not an Acknowledge alarm; the header pill already states manual mode calmly. C2 Off-by / blocking sum gates unchanged.
- **MB-8 Prebill scope:** Prebill opens `PrebillScopeDialog` (Media only default vs Media + fee). Media only = `applyLinePrebillToMonths` + media `reason=prepayment`; fees stay on delivery timing (manual-billing-is-timing-only). Media + fee also writes `component=fee` via `applyLineFeePrebillToMonths` + optimistic fee row. Session memory (`prebillScope.ts`) skips re-prompt for the same line inside one draft. Badge words from `prebillBadgeLabelFromFlags`: **Prepaid** only when media AND fee are prepayment; media-only → **Media prepaid** (container pill, line badge, timing editor share the same string). `reset_line` without component deletes both override rows. Fee-drift must not fire on media-only when month fee headers still sum to derived campaign fee.
- **Divergence banner hydrate gate (BUX-5):** edit-page `compareBillingDivergence` (one-shot + debounce) and MbaBillingModal banner/ack require `allChannelsHydrated` — not only `loadPhase === ready`. Otherwise empty channel line arrays false-diverge against saved months while the status row still shows "Loading channels…".
- **PC1 schedule source:** `DATA_BACKEND_FINANCE_SCHEDULE` default **`blob`** until explicitly set to `shadow`/`rows`; zero-row versions fall back to blob (logged). Line-level shadow compare ignores header-only blobs without `lineItems`. **S8 soak baseline** (`scripts/verify/soak-s8-perf-baseline.ts`, local `shadow`): finance-hub cost dominated by Xano `relevantPlanVersions` crawl (~4.5s, P-2), not shadow compare (~10ms est.); `schedule_months` query ~130ms. Campaigns list cold ~1.5s postgres / warm ~0ms TTL (P-8 client remount not measured). M8 `rows` flip measures against that table.
- **PC5 finance periods:** migration `0010_finance_periods.sql` (AUTHOR ONLY) adds `finance_periods`, `finance_run_items`, `app_notifications`, `clients.retainer_end_month`. Crons `finance-pre-run` / `finance-run` / `finance-lock` use UTC near-window schedules + Sydney wall-clock guards. Run reads published-tip `schedule_months` basis=billing (not blobs). Flag `FINANCE_PERIODS` default off; shadow = periods/runs real, lock advisory. UI: `/finance/periods` (sections). **FIN-8:** Clients billing shell hides the Periods tab when the flag is off (`FinancePeriodsFlagProvider` from `isFinancePeriodsEnabled()`); `/finance/periods` route still exists for direct URL.
- **Investment fee coverage (C-27 / FIN-8):** UI shows `FEE_COVERAGE_USER_NOTICE` only; technical `FEE_COVERAGE_CAVEAT` stays in the info tooltip + API `coverage.fee.caveat`. **Developer rule:** never treat cut `fee_cents` / `billable_cents` as complete agency fee — join `mba_fee_snapshots` and/or recompute stamped `feePct × media`; never invent fee as `billable − media`.
- **Campaign-level synthetics display (FN-FIX-1 / FIN-8):** wire key remains `campaign-level (no line detail)` (`CAMPAIGN_LEVEL_NO_LINE_DETAIL`) for totals identity. UI label is **Campaign totals (no line breakdown)** — rendered as a separated summary row below publishers (Investment cut + Publishers treemap/top table), not sorted among real publishers. Totals unchanged.
- **CP-3 finance status + payables composition:** Sections summary, costs, investment cut, and agency economics filter published tips to `approved|booked|completed` (`lib/finance/sections/financeCampaignStatus.ts`). Draft/planned/cancelled dollars surface in `coverage.excludedByStatusCents` (never silent-drop). Payables / Costs booked-cost headline = **delivery media only** (ex client-pays); fee and adserving are separate labelled figures. Version authority = published tip + `schedule_months` (D1) — not the legacy `relevantPlanVersions` pool. Orphan non-service schedule keys (no `line_items` row) stay in the media headline but are not fully attributed — `coverage.orphanLineCents`. Verify: `npm run recon:finance-sections-summary -- --fy=2026`.
- **CP-8 Costs client-pays:** `/finance/costs/client-pays` + `GET /api/finance/sections/costs/client-pays` show the inverted footprint (delivery media where `client_pays_for_media`, same status scope + dual-shape join), nested client → MBA → line with monthly columns. Complements the payables headline (`coverage.clientPaysExcludedCents` tile on Costs overview links here). Fee column omitted — schedule fee rows are $0 for these lines (C-27); billing-schedule fee is a future slice. Caption discloses C-29: `__service__*` rollups cannot carry the flag.
