# Learnings — media-plan auto-populate

- P1 channels only: radio + ooh. Other containers → say not supported yet.
- ARN text month / week-number flight headers: detector may return empty flight.columns; trust the mapper grid.
- Bonus / AV lines: keep `is_bonus` and exclude from paid budget totals in summaries.
- Always confirm before `apply_parsed_plan`.
