# Module: Dashboards, Charts & Document Generation

## Client dashboards

- Home `/dashboard` (`DashboardOverview`): Key metrics tiles derive from the same filtered live campaign/scope arrays as the panels (`lib/dashboard/homeDashboardFilters.ts`). Live scopes use `isLiveScopeStatus` (Approved / In-Progress, case/hyphen/space tolerant) — no end-date bound (scopes lack one). Live-client KPI union keys through `normalizeClientFilterValue` so campaign/scope name drift does not inflate the count. Fourth tile **Media Spend to Date** sums `GET /api/dashboard/planned-to-date?fy=` `byMba` over `filteredLiveCampaigns` (`sumPlannedToDateForCampaigns`); join keys are `mbaJoinKey` (trim + lowercase) on both sides; displayed `mp_mba_number` stays as stored; missing keys contribute 0; duplicate campaign rows that differ only by MBA case count once; no client-side clamp or pro-rata. Compact display is `formatMoneyCompact(n, { millionScale: "home-spend" })`; tooltip is exact AUD + “Planned media, published versions, to {d MMM yyyy}” (Melbourne). Fetch in flight → skeleton (never a money zero); fetch error → omit the tile (`console.error`); AVA `counts.metrics` matches the visible tiles. Not `mp_campaignbudget`, not delivered actuals. Campaign client names come from `GET /api/media_plans` → `mediaPlanVersionsCache` (postgres overlays master `mp_client_name` via `overlayMasterOwnedListFields` — DI-9b). Page identity stays in `MediaPlanEditorHero` (CTA only); search/client/saved-view controls live in `DashboardFilterBar` below the hero. `/mediaplans` reuses the same filter bar + pin storage (`lib/dashboard/savedDashboardViews.ts`, key `dashboard:savedViews:v2:${userId}`) with Home’s hero→filter→FY/Layout rhythm and **no** key-metrics strip (BUX-4). AU FY overlap filter (`lib/dates/auFinancialYear.ts`, `?fy=` start-year / `all`; default current) is client-side on Home campaign cards and `/mediaplans` — reuses `australianFyStartYearForDate` from `lib/finance/months.ts` (not AVA ending-year `fyToRange`). Shared hero row uses content-floor wrap (`basis min(280|460|780px,100%)` + `shrink-0`; `actionsFloor` `default` vs `toolbar`) so CTAs cannot squeeze/clip under `PageHeroShell` `overflow-hidden` when the sidebar collapses. Filters update tiles and panel badges together; "Clear filters" clears the active filter only and does not wipe saved client pins. "Save selected clients" upserts a `Pinned clients` saved view (restore source on load); the legacy `clientFilters` key is write-through only. Row dates use `lib/dashboard/safeFormatDate.ts` so one blank/malformed date cannot blank the page. Heading outline: `PanelRow` section titles are `h2`; nested `PanelTitle` panels under Campaigns & scope use `as="h3"` (default `PanelTitle` is `h2`, `CardTitle` is `h3`).
- Routes: `app/dashboard/[slug]/**` (tenant-scoped client views) + `components/dashboard/**` (57 files: hero KPI bar, spending insights, campaign viz, delivery sections, slide-overs).
- Campaign MBA page: exactly **one** planned media-by-type chart — a **donut** in `SpendChartsRow`, fed only from the **delivery schedule** via `lib/dashboard/mediaMixFromDeliverySchedule.ts` (same parser family as Expected Spend / Finance). Basis caption: `MEDIA_MIX_DONUT_BASIS_CAPTION`. Under the donut (inside `BaseChartCard` children / PNG capture), `ShareBreakdownLegend` lists colour · label · `fmt.currencyCompact` · 1-dp share (`maxRows` collapses to Other). Not on the shell `legend` slot. **Display layer** filters `isNonMediaMixSlice` (`Fees` + `Production`/`production`) from the donut, legend, CSV, largest-channel tile, and monthly stack — `channelTotalsFromDeliverySchedule` / Planned-to-date still include them. Subtitles say `excludes fees`. Publisher bar does not yet use ShareBreakdownLegend. Media plan Summary must not show a second media-mix chart. Progress summary pacing sentence uses `computePacing` → `pacingStatus()` (`lib/dashboard/campaignPacingVerdict.ts`). **B1-1:** admin-only `CampaignKpiPacingStrip` under progress summary (`lib/kpi/kpiPacing.ts`) — display-only; hidden for clients and when no campaign KPI targets exist. **Hours to date:** admin-only `CampaignHoursWidget` (`GET /api/codex/time/summary`) — total hours + by-member + 4-week sparkline from mirrored `time_entries`; never shown to client roles. Client details modal never links clients to `/mediaplans/.../edit`. Live-now freshness is derived from delivered `asOf`, never a hardcoded "ago". Client-facing gantt sideline labels (`buildGanttSidelineLabel`) are `{publisher} • {creative_targeting → site ≠ left → placement → daypart → Line item id}` — bid strategy is not used on the sideline. `GET|POST /api/mediaplans/mba/[mba_number]/material-deadlines` is tenant-scoped (POST also admin); `MaterialDeadlinesStrip` is unmounted.

- Client hub (`/dashboard/[slug]` and `/client/[slug]`) date window is URL `startDate`/`endDate` (`lib/dashboard/clientDateRange.ts`). No params = current Australian FY (Jul–Jun), matching the spend donuts. Legacy `?fy=` (start-year or `all`) still translates server-side. Lists are campaigns whose flight **overlaps** the range; live/planning/completed stay judged vs today within that subset. Spend series (`spendByMediaType` / `spendByCampaign` / monthly stacks / `totalSpend`) use the same window. `spendPast30Days` stays a fixed 30-day lookback. Planned-to-date clamps monthly amounts to the range. Delivered tile sends `from`/`to` to `GET /api/dashboard/[slug]/delivered` (absent = unbounded DATE_DAY). Insights strip is not day-scoped (YYYY-MM `period` only). Spending insights donuts/stack are planned media from delivery-schedule **months** via `normalizeDeliveryEntryMediaBreakdown` (types-shape line amounts or costs-shape `mediaCosts`; `feeTotal` is not added). Captions are `spendInsightsCaption` — not campaign `MEDIA_MIX_DONUT_BASIS_CAPTION`.
- Client hub campaign grid (`ClientDashboardPageContent`) uses `ViewState` / `ViewStateBoundary` for status-pill filtering (zero in bucket → Clear filters; zero campaigns overall → create CTA). Campaigns are SSR — no list-fetch error branch. Client Brain is a 4th hub/admin hero rail icon (`HeroBanner` → `ClientBrainSlideOver` / `ClientBrainPanel`); same `adminHub` gate as Details/Finance/KPIs — not a full-width card on the overview. Admin slide-over icons sit in a 2×2 grid on the hero (not a vertical stack); `HeroKPIBar` is a full-width 5-up from `lg`. `CampaignCardCompact` identity has a min-width and wraps View/status below the title when tight; dates use `formatDateRangeCompact` (`1 Apr – 31 Dec 26`). Admin hub also mounts `RecentInsightsPanel` (last five live `campaign_insights` for the client → `/insights?clientId=`). Campaign pages mount the same panel scoped to MBA plus `QuickAddInsightForm` (MBA-scoped POST). `/insights` is admin-only library (FTS + filters + create/edit/supersede); `GET|POST /api/insights` and `PATCH /api/insights/[id]` return 403 for client roles — no DELETE.

- Campaign delivery section renders **nine** containers (was six). Direct Booked Digital replaces Ad Serving: Digital Display, Digital Video, Digital Audio, BVOD (`digital-display` | `digital-video` | `digital-audio` | `bvod`). All four read PACING_FACT channel `Ad Serving - CM360` (`channel === "ad-serving"`) and are partitioned by plan container, not by warehouse channel.
- Delivery accordion line labels use `lib/delivery/lineItemDisplayName.ts` (`platform ?? publisher …` • `creative_targeting`/`creative`, clamped to 90 with `title=full`). `line_item_name` is unset on essentially all plan rows — never show raw `line_item_id` when platform/publisher+targeting exist. Social `formatLineItemHeader` delegates to the same helper (unclamped, platform+creative_targeting only). Direct Booked Digital uses four thin adapters (`digitalDisplayAdapter`, `digitalVideoAdapter`, `digitalAudioAdapter`, `bvodAdapter`) over `directDigitalAdapterShared` (ZERO-$ LAW: `derive_spend_from_plan` is OFF for all Direct Booked Digital). Snowflake rows stay `channel === "ad-serving"` (CM360) for all four. The campaign MBA page fans four plan-container arrays via `getAllLineItemsFromKeys` (`digitalDisplay`/`digiDisplay`, `digitalVideo`/`digiVideo`, `digitalAudio`/`digiAudio`, `bvod`/`digiBvod`/`digi_bvod`) into `CampaignPageAssembly`, which applies one shared burst-window predicate then passes four filtered arrays to `CampaignDeliverySection` (render order Display → Video → Audio → BVOD after programmatic). `DeliveryDataProvider` takes one unioned `directDigitalLineItemIds` set — one `/api/pacing/bulk` call. `loadDeliverySnapshot` groups are `digital_display` | `digital_video` | `digital_audio` | `bvod` (same order after programmatic); `assembleCampaignReportData` `CHANNEL_LABELS` match those groups. AVA `get_pacing_snapshot` stamps cached ad-serving rows by `channelFamily` onto those four group strings. Search keeps **one accordion block per plan line**; `EntityBreakdownTable` (collapsed by default) lists delivered entity actuals with `stripPlanCodeSuffix` so client UI never shows plan-code suffixes (incl. rollover codes like `-BOSS002SE1`). Search uses `columns: "spend"` (Ad group · Spend · Clicks · Impressions · CPC · CTR) via a thin `AdGroupBreakdownTable` wrapper. Social Meta/TikTok attach the same table from `combinedRows.entityId`/`entityName` inside `socialAdapterShared` (not `socialChannelCompute`): Meta `{ad set, ad sets}`, TikTok `{ad group, ad groups}`, `columns: "spend"` (real platform spend — ZERO-$ LAW does not apply). Blank `entityId` rows are dropped; if none remain, the table is omitted. Direct Booked Digital uses `columns: "delivery"` (Placement · Impressions · Clicks · CTR · Video completions · Completion rate) grouped from `combinedRows.entityId`/`entityName` — no Spend/CPC (ZERO-$ LAW; `derive_spend_from_plan` is OFF for Direct Booked Digital; CM360 `amount_spent` is 0). A single placement still renders. No planned/variance on that table — planned stays on the parent only. `search-pacing-service` adds optional totals-only `adGroups` without changing campaign/line daily maths.
- Direct Booked Digital daily charts (`directDigitalAdapterShared`): dual-axis ComboChart — impressions bars (`format: "number"`) plus a right-axis line chosen from container rollup data (`videoCompletes > 0` → completion rate `percent`; else clicks `number`). Not keyed off `ChannelKey`. Daily completion rate is `sum(completes) / sum(impressions)` after `aggregateDailyRows(..., ["impressions", "clicks", "videoCompletes"])` — never the mean of per-line rates. Container and line-item charts share that right-axis choice. Containers with 2+ line items now render the aggregate roll-up (summary chips, two progress cards, KPI band, daily chart); when `videoCompletes > 0` the roll-up also carries a Completion rate chip/tile (≤5 chips). No `amount_spent` series (ZERO-$ LAW; the flag is OFF for Direct Booked Digital, so modelled spend does not apply here).
- Deliverable ProgressCard **titles** use `lib/delivery/deliverableLabel.ts` (buyType→noun; aggregate = single noun or "Deliverable"). Per-line social/prog still use metric-key helpers (`getDeliverableLabel` / `getProgrammaticDeliverableLabel`) which now delegate to `deliverableLabelForMetricKey`. Mixed buy-type containers correctly say "Deliverable delivery"; do not treat that as proof the roll-up number is commensurate (C-37).
- Channel roll-up render gate (`shouldShowChannelAggregate`): show aggregate when the container is empty (0 lines) or is a roll-up-safe channel (`search` | `social-meta` | `social-tiktok` | `digital-display` | `digital-video` | `digital-audio` | `bvod`) with 2+ line items. Programmatic (`programmatic-display` | `programmatic-video`) never shows the aggregate block (C-37: one mixed deliverable card). Programmatic line membership is `delivery_source_map` (0063 AUTHOR ONLY; runtime TS seed in `lib/delivery/deliverySourceMap.ts`): active row for `(publisher || platform)` after trim+lowercase, else excluded. `dsp` Snowflake channel is the container; `cm360` is `ad-serving`. Connection pills name the source (`CM360 (Quantcast)` for Quantcast, never `DV360 connected`). `mapCombinedRowToDv360` takes an accepted-channel set and throws on anything else. A `derive_spend_from_plan` programmatic line's `actualsDaily.spend` is modelled from plan rate (`deriveSpendFromPlanRate`) and labelled "Delivered spend (modelled from plan rate)"; DSP lines keep Snowflake spend and "Spend delivery". Direct Booked Digital stays ZERO-$ (`derive_spend_from_plan` OFF) and is roll-up-safe because impressions and clicks stay on two progress cards (`bookedDeliverables` routes planned totals by buy type). Single-line containers still render `LineItemBlock` directly (no inner accordion). Adapters still compute `aggregate`; presentation only.
- Money/date presentation: client-facing dashboard surfaces use `lib/format/money.ts` (`formatMoney` / `formatMoneyCompact` / `formatPercent`) and `lib/format/date.ts`. KPI tiles + chart axes → compact; tables/line items/invoice-reconcile figures → `formatMoney` with 2 decimals. Do not mix compact and full within one card/KPI row. Home **Media Spend to Date** is the exception on the compact tile: `formatMoneyCompact(n, { millionScale: "home-spend" })` is two million decimals below $10M (`$1.06M`) and one at/above (`$12.4M`); tooltip is exact `formatMoney` plus “Planned media, published versions, to {d MMM yyyy}” (Melbourne as-at). Other three Home tiles stay unformatted counts. `lib/format/currency.ts` is a thin compatibility shim over the same helpers (legacy locale/currency option objects still accepted for editor/export callers).
- API: `GET /api/dashboard/[slug]` (and `/delivered`) — client users must match slug via `getUserClientSlugs`; admin is unscoped (SEC-4 FIXED).
- Aggregation layer: `lib/api/dashboard/{client,publisher,global,finance,shared}.ts` — `shared.ts` owns **tip** (`resolveDashboardLiveVersionRow`: master tip → else highest `published_at`; `pickHighestVersionRow` aliases it) and **commercial** (`isBookedApprovedCompleted` = booked\|approved\|completed strings), plus `normalizeSchedule` and the shared 10s axios timeout. Do not use BAC to pick the live version. Home `DashboardOverview` per-MBA picker uses `isPlannedBasisCampaignStatus` (client-safe BAC mirror) for **inclusion**; `CampaignStatusBadge` / `resolveCampaignPhase` is **display only**. Do not re-point inclusion at phase. Publisher Hub spend (`getPublisherDashboardData` → `lineItemMatchesPublisher`) treats empty/whitespace `header1` as absent (`header1 || publisher || publisherid`) and joins ingest short stamps via `publisher_profiles.publisher_id` (0036), never catalogue display name. Hub Meetings is a read-only `client_notes` list (`GET /api/publishers/[publisherId]/meetings`, `requireAdmin`) after charts and before ingest.
- Finance-hub schedule-spend treemaps (`GET /api/dashboard/global-monthly-{publisher,client}-spend` → `getGlobalMonthlyPublisherSpend` / `getGlobalMonthlyClientSpend`): list rows are `{ month, publisher|client, amount }` (month ISO `YYYY-MM-DD` string, identifiers strings, amount AUD dollars number). When `getDataBackendFor("plans") === "postgres"`, rows come from `lib/data/dashboardMonthlySpend.ts` (delivery-basis `schedule_months` on `masters.published_version_id`, publisher from `line_items`, client from `mp_client_name`; FY filter stays app-side). xano/shadow still call Xano `dashboard_monthly_*`. Upstream/DB failures throw (M7 — never soft-fail to empty months).
- Home planned-to-date (`GET /api/dashboard/planned-to-date?fy=<startYear|all>` → `buildPlannedToDateByMba`): `{ byMba, fy, generatedAt }` — one expected-spend-to-date number per MBA, keyed by `mbaJoinKey` (trim + lowercase). Home lookup lowercases `mp_mba_number` the same way; displayed MBA text stays as stored. Same delivery-schedule parser as the client hub (`expectedSpendToDateFromDeliveryScheduleMonthly` then `clampMonthlyAmountsToRange` against `auFyBoundsDateOnly`). `fy=all` is unclamped. Commercial set is BAC (`isBookedApprovedCompleted`); no client/search/live-today filter — the caller filters. Does **not** reuse `global-monthly-*` (those are month×client/publisher full-month totals for the current FY, not per-MBA to-date). Auth matches `global-monthly-*`: `requireRole(["admin"])`. Cache is `globalSpendCache` (300s, tag `dashboard-global-spend`, `fy` in the key). Reads `readPlanVersions` (keeps `deliverySchedule`; never `mediaPlanVersionsCache` which strips schedules). Planned only — no Snowflake.
- `lib/dashboard/plannedSpendConsistency.ts` deliberately couples the "Planned to date" and "Plan committed" (`budgetUtilizedPct`) tiles to one computation; its status filter must stay in sync with `deliveryScheduleByMBA` in `lib/api/dashboard/client.ts`. The UI label is "Plan committed" (planned ÷ plan budget) — not delivered utilisation.
- Campaign summary Budget & Spend tiles (`CampaignSummaryRow`) are label + number only; grey explanatory captions and the Melbourne delivery freshness stamp are not shown on this page (HeroKPIBar still stamps client hub). Honest empty states stay: Delivered/Remaining without delivery show "No delivery reported yet" / "Campaign not started"; Expected Spend without monthly plan shows "Monthly plan data unavailable". "Remaining" is `budget − delivered` (`lib/dashboard/budgetSpendTiles.ts`) so Delivered + Remaining = Budget. Client campaign cards (`CampaignCardCompact` in `ClientDashboardPageContent` Live/Planned/Completed pills) lead with identity: name (`line-clamp-2`), start–end range (`formatDateRange`), MBA secondary; channel chips stay; expected-spend bar/% is smaller below. Progress binds to **expected spend to date** (same basis as the campaign-page Expected Spend tile), not Snowflake delivered.
- "Delivered" money comes from `lib/delivery/deliveredTotals` (same figure as /pacing/direct — but fetched cache-bypassed, P-7).
- Brand theming: `lib/client-dashboard/` (small, 2 importers — the real dashboard logic is in `components/dashboard` + `lib/dashboard` + `lib/api/dashboard`). `useClientBrand()` falls back silently to AV defaults (plausible wrong colours, not an error). Client brand hex columns still missing in Xano (known TODO). Delivery channel containers (`ChannelSectionData.mediaTypeColour` via `channelMediaTypeColour`) always use `getMediaColor` by channel key — never client brand. `ChannelSection` paints a quiet `getMediaBadgeStyle` icon chip from that colour; aggregate `DeliveryDailyChart` bars use `mediaTypeColour` and the dual-axis metric line uses theme ink (`DELIVERY_DAILY_METRIC_LINE_COLOR` / `--av-ink`) — never a `STATUS` pacing token. Brand stays on hero / campaign timeline / optional chart `brandColour` fallback. Programmatic display → `prog_display`, video → `prog_video`. Direct Booked Digital maps 1:1: `digital-display` → `digital_display`, `digital-video` → `digital_video`, `digital-audio` → `digital_audio`, `bvod` → `bvod`.


## Chart system

- `lib/chart-theme.ts` — palettes (`CHART_PALETTE`, colour-blind, sequential/diverging/status) — "no chart hard-codes a hex". `channelColorFor` resolves known media types via `MEDIA_TYPE_REGISTRY` first (gantt chips / spend series), then coarse `CHANNEL_COLORS` aggregates, then palette index.
- `lib/charts/registry.ts` — `MEDIA_TYPE_REGISTRY`, THE media-type chart colour/label source, mirroring `mediaTypeTheme` in `lib/utils` (two overlapping colour sources — keep in sync). Pills use `getMediaBadgeStyle` (`MediaChannelTag` on dashboard cards, mediaplans list, publishers) — do not add a local tone map.
- `lib/charts/theme.ts` — Recharts plumbing: Tailwind arbitrary selectors keyed on Recharts' literal default strokes (`#ccc`/`#fff`) — **a Recharts upgrade that changes those defaults silently un-styles every chart**.
- `components/charts/system/*` — the chart component library; PNG export via html2canvas (client-only, breaks on cross-origin images). Vertical `BarChart` / `ComboChart` category ticks use file-local `denseCategoryAxisProps` (≤8 plain; 9–20 angled `interval: 0`; >20 angled `interval: "preserveStartEnd"` + `minTickGap: 12`) — thins labels only, never data points. Width-aware target tick counts (ResizeObserver) are not wired yet. Media plan panel PNG (`MediaPlanVizSection.handleExportPng`) must pass a **computed** `backgroundColor` (not `hsl(var(--card))` — html2canvas throws `Unsupported angle type`), race fonts/rAF/html2canvas so Exporting… cannot stick, download via shared `triggerDownload` (blob + object URL), and size to `scrollWidth`/`scrollHeight`. Do not route the gantt through `captureNodePng`'s SVG fast path — gantt fills use CSS vars that a serialised SVG cannot resolve.
- **DeliveryDailyChart series contract:** `DeliveryDailyChartSeries` in `components/dashboard/delivery/common/DeliveryDailyChart.tsx` is the one series shape. `ChannelAggregate.chart.series` and `LineItemBlock` `DailyChart.series` import that type — do not inline a second `{ key, label, yAxis }` list. ComboChart axis format is per-series `format` (bars default `dollars`, lines default `number`); omitting it is byte-identical to the pre-BV1 hardcode. MultiLineChart still uses `valueFormat="compact"`.

- **B2-1 chart export:** `BaseChartCard` always exposes a shared overflow menu (Download PNG + Download CSV when `exportSeries` is set). Filenames `{page}-{title-slug}-{yyyymmdd}.{png|csv}` via `lib/charts/chartExport.ts`. CSV columns = x + one per series; values are raw (not display-formatted). Visible to any role that can see the chart (no admin gate). Charts not on `BaseChartCard` stay unwired (list in the B2-1 report — do not hand-wire one-offs).
- Normative UI rules: `docs/design-refresh/SYSTEM_RULES.md` (see INVARIANTS UI section).

## Document / export generation

Three stacks — know which one you're in:
1. **exceljs** workbooks: `lib/generateMediaPlan.ts` (2.3k lines — also the `LineItem`/`MediaItems` **type hub with 32 importers**; renaming a field cascades to all 20 containers + KPI grouping + Excel), finance exports (7 modules), naming/trafficking workbooks, MI spec workbook. Editor Excel stays client-side; `POST /api/mediaplans/generate-pdf` is locked (admin/manager, `{mba_number,version_number}` only) and returns stored file metadata — no client totals. 422 unless `isVersionPublished` (`published_at`); copy is `unpublishedDocumentError` (no `campaign_status`). The leftover `isDownloadableCampaignStatus` (everything except draft) stays exported and unused by this gate.
2. **jsPDF**: `generateMBA.ts` (PC3: footer `v{n} · {hash8}` from `snapshot_checksum`; `/api/mba/generate` renders from `schedule_months` + `approved_slice` + `mba_fee_snapshots`, deriving the slice from billing `schedule_months` at read time when none was frozen — never writes `approved_slice`; optional `selectedMonthYears` / `approvedLineItemIds` overlay the PDF only (`X-Slice-Source: live`); optional `liveCampaignDates` prints form campaign dates without setting `liveOverlay`; checksum stays frozen; extra keys 400 `CLIENT_TOTALS_REJECTED`; `X-Slice-Source` header for support; `requireRole(admin)`, body `{mba_number,version_number}` plus those optional keys, 422 unless `isVersionPublished` — not approved-or-beyond; partial MBA adds one `Scope:` line + `_partial` filename, full MBA PDF bytes unchanged unless live dates change the Campaign Dates line), `generateScopeOfWork.ts`, `generateBillingSchedulePDF.ts`.
3. **PPTX ×2 incompatible approaches**: pptx-automizer (`lib/planning/export/buildPlannerDeck.ts` — never starts from blank; hardcoded placeholder geometry) vs raw JSZip token substitution (`lib/reports/buildPerformanceReport.ts` — fixed-arity payload; adding a 5th channel/KPI means changing template + token map + tuple types + AVA tool together; `escapeXmlText` must escape `&` first).
4. **B3-1 campaign report:** `POST /api/campaigns/export-report` (admin + rate limit) → `assembleCampaignReportData` + `buildCampaignReportDeck` on the same Assembled root/template as export-deck. Period picker on campaign **Review & Report**. Native pptxgenjs `addChart` via `slide.generate` (pptx-automizer wraps pptxgenjs — not PNG). Commentary slide is PLACEHOLDER only (AVA insight wiring follow-up). Filename `campaign-report-{mba}-{period}-{yyyymmdd}.pptx`. Ambiguous KPI percent units omitted (N7 / AV-25).

Checksum: `lib/docs/snapshotChecksum.ts` — sha256 over canonical `(schedule_months + approved_slice + fee snapshot)`; written on publish to `media_plan_versions.snapshot_checksum`; weekly tripwire `GET /api/cron/snapshot-checksum` (CRON_SECRET, report-only).

While an unpublished draft exists, document downloads keep serving the **published tip** (edit page `isPublished` gate + toast “Publish this plan to download and send to client”). `DOWNLOAD_BLOCKED_MESSAGE` uses that same publishing wording. Localhost download/generate use `isVersionPublished`; leftover `isDownloadableCampaignStatus` stays unused. Edit/create POST `/api/mba/generate` with `{mba_number,version_number}` (live `campaign_status` may be posted; generate ignores it). Edit Generate MBA and Save-and-download-all also send live overlay keys (`selectedMonthYears`, `approvedLineItemIds`) from `deriveLiveMbaScopeSelection` when Partial MBA is on, hydrated, and at least one line is not excluded, plus `liveCampaignDates` when the form `mp_campaigndates_start` / `_end` differ from the loaded version after Melbourne ISO normalisation. Full MBA (`isPartialMBA` false) posts identifiers only for overlay — a non-null overlay re-sums totals and drops the billing fallback — but may still send live dates. Save-time document upload and create stay identifiers-only (frozen slice, no live dates). Empty `selectedMonthYears` is omitted (frozen months stay); empty `approvedLineItemIds` is never posted from the client (null selection → no overlay).

Template/skill files resolve via `process.cwd()` — serverless bundle tracing must include them.

## Email

- `lib/email/sendHtmlEmail.ts` (`server-only`, SendGrid; **throws** when `SENDGRID_API_KEY` missing — cron digests fail loudly; `EMAIL_FROM` required).
- `lib/email/inviteSender.ts` (Auth0 invites, SMTP fallback). Consumers: admin users route + the 3 digest crons.

## Excel/export gotchas (verified, recurring)

- Excel Column N "Gross Media" contains **net** (C-1); no per-line fee column; export reads `calculateAssembledFee()` totals verbatim, never recomputes per line.
- `groupLineItems` substitutes `deliverablesAmount` when `grossMedia === 0` — inflates section totals for client-pays lines.
- Partial-MBA: export must read `partialMBAValues` (see INVARIANTS fee section).
- exceljs is imported client-side in the mega-pages and finance dialogs (bundle cost); `file-saver` static in several finance components.
