# Blast Radius — "if I touch X, what else breaks?"

**Use this page before every change.** Find the file(s) you plan to touch; the right-hand column is your review-and-test checklist. Import counts verified by grep at commit `ecc948b`.

## Tier 0 — the choke points (ranked by importing files)

| # | File | Importers | What breaks if you change it |
|---|---|---|---|
| 1 | `lib/utils.ts` | **216** (277 incl. subtree) | `cn()` — every component's class merging. `mediaTypeTheme` colour keys — every chart/gantt/legend for that channel silently loses colour |
| 2 | `lib/api/xano.ts` | **106** | Every outbound Xano call. `parseXanoListPayload` returns `[]` silently on unknown shapes; `getRequiredEnv` returns `""` in browser (no error) |
| 3 | `lib/format/money.ts` | **80** | All currency display + `roundMoney2/4` in derived totals → billing/forecast/export reconciliation drift |
| 4 | `lib/mediaplan/expertModeWeeklySchedule` / `weeklyGanttColumns` | 65–68 | Expert-mode week math + gantt columns everywhere |
| 5 | `lib/mediaplan/expertChannelMappings.ts` (8.4k lines) | **63** | Standard↔expert row conversion, all 20 channels |
| 6 | `lib/billing/types.ts` (`BillingMonth`) | **61** | 33+ files across finance and billing |
| 7 | `lib/rbac.ts` | **53** | Sole authority for roles/tenant. Regression = mass lockout or mass elevation. Imported by edge middleware — must stay edge-safe |
| 8 | `lib/auth0.ts` | **52** | **Throws at import** on missing env → takes the whole app down, not one route. `beforeSessionSaved` drops claims silently if changed. After claims persist it fail-soft upserts admin into `team_members` (`syncAdminRosterOnLogin`) — login must never throw. Login gate stays claims-based; Layer 2 roster sync is a separate eligibility path (`rosterEligibility.ts`) |
| 9 | `lib/requireRole.ts` | **35** | Gate on all admin + all finance routes; the `"response" in result` discriminant shape is relied on at every call site |
| 10 | `lib/generateMediaPlan.ts` (2.3k lines) | **32+** | A **type hub disguised as a generator** — all 20 containers import `LineItem` from it; `MediaItems` feeds KPI grouping + Excel. Buy Type cells use `lib/mediaplan/buyTypeLabels.ts`. Server regenerate (`buildMediaItemsFromPersisted`) also calls it — do not rename LineItem fields. Channel sheets omit lines outside `mba_scope` (`filterMediaItemsForMbaScope`). Totals `excluded_from_mba_scope_note` is a label-only row under Total Gross Media (copy: `Not in this MBA: $X across N lines (see plan for full schedule)`; no col-14 number) |
| 11 | `lib/clients/slug.ts` | **24** | Slugs ARE tenant identity (URLs, Auth0 claims, cache enrichment). Algorithm change → client users locked out. `legalsuper → legal_super` override is load-bearing. Auth0 **write** path stores `clients.slug` as `client_slug`, not the mbaidentifier slug |
| 12 | `lib/api/xanoPagination.ts` | **22** | All multi-page reads. Silent truncation at 10,000 rows; dedupe fallback can drop distinct rows on tables without `id` |

## The five highest-consequence contracts

### 1. `bursts_json` shape — `lib/mediaplan/serializeBurstsJson.ts` + `formatBurstsForPersist.ts`
Changing a key or type breaks, simultaneously: pacing (`lib/pacing/burst/parseBursts.ts` → all 5 live-line-item resolvers), delivery compute (social/programmatic/search), billing (`generateBillingLineItems`, `seedLineFees`, `assertPersistedLineFeesMatchBilling`), finance (`computeCampaignFinancials`), dashboard gantt/charts (`normalizeLineItem`), the Snowflake snapshot sync (`fetchAllLineItems`), Excel/AA exports.
**Trap:** `mediaAmount` in the JSON is sourced from `deliveryMediaAmount`, so it is non-zero when `clientPaysForMedia` is true. `fee` history: see INVARIANTS (bursts contract).

### 2. `line_item_id` — `lib/mediaplan/lineItemIds.ts` (`MEDIA_TYPE_ID_CODES`, `buildLineItemId`)
The join key for: KPI fan-out (28 refs in `lib/kpi/fanOut.ts`), billing burst resolution, creative/trafficking pickers, Snowflake dedupe key, search-suffix pacing match, orphan assignment. Legacy `ML` codes still live in the warehouse after the May 2026 ML→DD/IT/PV/PB/PA/PO/OH split — matchers must handle both.

### 3. Fee math — `lib/mediaplan/burstAmounts.ts::computeBurstAmounts`
**The single widest blast radius in the repo.** 4 branches (see INVARIANTS). Consumed by ~19 container `get*Bursts` + every expert grid via `expertRowFeeSplit` + `computeCampaignFinancials` → every receivable, payable, accrual row, forecast line, snapshot, and export.
Known **former** divergent re-implementations (C-7/C-8 FIXED in PC2): `generateBillingLineItems` and `computeDerivedCampaignFeeAmount` now route through `computeBurstAmounts`; agency fee drift tolerance is $0.01.

### 4. Published-version rule — `lib/mediaplan/publishedVersionGuard.ts`
7 external call sites (channel GETs, list caches, dashboard client/finance/shared, MBA route, staged-version reaper). Getting it wrong makes **staged, unpublished versions visible as live plans** across dashboards, finance, and every channel read. **Published cut** = `published_version_id` AND `published_at` set (`publishedVersionIdFromMaster`; SQL `PUBLISHED_VERSION_JOIN_SQL`). Stamp predicate is canonical `isVersionPublished` — do not re-derive. Raw pointer (may be unstamped) is `publishedVersionPointerIdFromMaster`. Staging watermark `publishedVersionFromMaster` is `master.version_number` (reap/list caps only). Never `max(versions)`. Finance sections SQL (summary, cut, costs, client-pays, cut AR, dashboard monthly spend, `probeFinanceScheduleDiffs`) interpolates the fragment. Do not ship that reader change before 0070 — today it would drop the 12 C-113 plans (~$609k). After 0070 the two predicates agree and those totals must not move. `relevantPlanVersions` is still the watermark family.

### 5. Pacing maths — `lib/pacing/maths/index.ts`
Status ladder order deliberately mirrors Snowflake `V_LINE_ITEM_PACING` (`// Order from V_LINE_ITEM_PACING — DO NOT REORDER`). Changing a band changes: all spend-channel composers, the ops digest email bands, Overview tiles, AVA's narrative — and silently diverges from the warehouse view. Campaign-delivery Ahead / Behind / On track is a separate helper (`lib/pacing/deliveryStatusFromPct.ts`, behind below 90 / ahead above 110) — do not fold it into this ladder.

## Per-area impact tables

### Media plans
| Touch | Also check |
|---|---|
| Adding a channel | ~12 hardcoded 20-entry maps must all be updated: `MEDIA_TYPE_ENDPOINTS/FLAGS/ALIASES` (MBA route), `CHANNEL_LINE_ITEM_ENDPOINTS`, `MEDIA_PLANS_ALLOWLIST`, `MEDIA_PLAN_TABLES`, `LINE_ITEM_BROWSER_API_PATH`, `MEDIA_CONTAINER_ENDPOINTS`, `LINE_ITEM_SOURCE_TABLES`, `PUBLISH_INTEGRITY_CHANNEL_FLAGS`, `clearVersionChildren.SLUGS`, `reapUnpublishedStagedVersions.STAGED_CHILD_SLUGS`, `MEDIA_TYPE_LABELS/COLORS`, `MEDIA_TYPE_ID_CODES`, `CREATE_MEDIA_TYPE_CATALOG`, `MediaItems`, `CANONICAL_MEDIA_KEYS` — plus container/grid/config/schema quad and ~4 blocks in each mega-page. Also `normaliseScheduleMediaType` returns null for unknown types (fee → 0 + builder-issue warn); do not reintroduce silent search-fee inheritance. |
| `ExpertGrid.tsx` (5.2k lines) or `expertGridChannelConfig` | All 21 channels at once, in create AND edit. Perf contract tests: `expertGridRowPerf.memoContract.test.ts`. Dialog wrappers must stay `overflow-hidden` (not `overflow-auto`) or the grid scroller traps the wheel. Sticky totals-label tests: `expertGridDescriptorStickySpanWidth.test.ts`. Compact descriptor pane: `expertGridDescriptorColWidthsForMode.test.ts`, `expertGridDescriptorMode.test.ts` (every channel `rowLabelKey`; auto-mode uses logical scroll + `canCompact`, not raw `scrollLeft`; compensation is gated by a user-input intent flag, not a 34ms timer; shrinking floors `scrollLeft` at 40px when maxScroll allows; sticky descriptor cells do not transition width; `DESCRIPTOR_AUTO_COMPACT_ENABLED` currently true — unpinned follows auto-mode). Trailing sticky widths come from `getExpertTrailingColumns` (not `trailingColWidthsPx` alone). Body-row height is `EXPERT_GRID_BODY_ROW_HEIGHT_PX` (41) enforced on cells via `--eg-body-row-height`; `bodyRowHeightPx` overrides per channel only with `bodyRowHeightPxReason`. Virtualiser sizing reads `offsetHeight` (not `getBoundingClientRect`). Schedule table is `border-separate` / `border-spacing-0` (not collapse). Tests: `expertGridSticky.sm11.test.ts`, `expertGridDescriptorColWidthsForMode.test.ts`, `expertGridDescriptorStickySpanWidth.test.ts`. Multi-cell Delete/Cut wired (C-88); week-cell context menu is additive (same copy/cut/paste helpers); it closes on scroll (capture) and resize, is keyboard-reachable, and clamps to the measured menu box. Tests: `expertGridWeekContextMenu.ce4.test.ts` + `ExpertGridWeekContextMenu.test.tsx` (`test:expert-grid-selection`); range-move still backlog (`expertGridSelectionOps.sf9.test.ts`). Unit Rate idle display is `formatRate` (`expertGridUnitRateDisplay.ts`); fill/paste still copy raw numbers (`expertGridFill.test.ts`, `expertGridPaste.test.ts`). Week-start is `useWeekStartsOn` (`av:week-starts-on`), not per-container state (`test:week-starts-on`). |
| `containerChannelConfig.fieldMap` | Changes both hydration and API payloads → what Xano stores → what Snowflake/pacing see |
| `emptyChannelLineItemsDefault` / channel `useForm` defaultValues | All 20 channels (6 via `useMediaChannelContainer`, 14 bespoke). Empty array is the empty-container contract (`ContainerEmptyLinesPlaceholder`). Add/append still uses `buildDefaultLineItem`. Do not seed a $0 starter. Edit hydrate still replaces via `useStableHydration`. Tests: `npm run test:empty-channel-defaults`. |
| PUT/PATCH publish contract (`deferMasterVersionPublish`, `forceIncrement`) | Both mega-pages hardcode stage-then-publish; phase-two PATCH is gated by `shouldRunDeferredMasterPublish` (`deferredPublish` ∧ `saveIntent==="publish"`). `publishVersionIntegrity` / empty-publish guard share that intent gate on purpose |
| `db/migrations/0069_published_pointer_must_be_stamped.sql` / `0070_backfill_vp1_unstamped_pointers.sql` / `savePlan.ts` publish stamp | VP-1: pointer must be NULL or `published_at` set. Required order: R1 live in production, then 0070, then 0069. 0069 deferred constraint triggers do not scan existing rows, so 0070 must stamp the 12 C-113 rows first (`published_at = created_at`, `published_by = backfill:vp-1`). AUTHOR ONLY, not applied. Do not apply 0069 until the stamping writer is live — production main `8f31a43d` still advances the pointer unstamped and those publishes would fail at COMMIT. Consumers of `published_at` / `isVersionPublished` / Stage 2a immutability. **C-114:** R1 omits `createdAt` on the stamp `.set(`; trunk resets it. `published_by` is email-only — never Auth0 `sub`. |
| `plansSaveBodySchema.ts` / `savePlan.ts` / `mbaScopeForSave.ts` / `mbaScopeClient.ts` | Optional `mbaScope` on the save body (legacy `selectedMonthYears` still mapped). Same txn as slice freeze + billing explode writes `mba_scope`. Null ids = all-in. Tests: `test:save-plan`, `lib/mediaplan/__tests__/mbaScopeForSave.test.ts`, `mbaScopeClient.test.ts`. Do not resume twin PATCH of `mba_line_approvals`. |
| `components/billing/MbaBillingModal.tsx` | Switches/chips unchanged; header copy is version-scope. Hydration from `version.mba_scope` (SQL-null → legacy GET). |
| `lib/mediaplan/drafts/pill.ts` / `PlanWizardSaveMessages.tsx` | Partial publish rail: `Client MBA covers {in} of {total} lines.` (`describePartialMbaPublishRail`). Tests: pill / `planWizardSaveBar`. |
| `lib/docs/filterMediaItemsForMbaScope.ts` | Persist regenerate, draft render, and both twins must use this helper. Empty `lineItemIds` drops all countable lines. Tests: `lib/docs/__tests__/filterMediaItemsForMbaScope.test.ts`. |
| Anything in `create/page.tsx` | Probably needs the same fix in `edit/page.tsx` (near-parallel 8k/12k-line implementations: own xlsx/pdf generation, publisher fetch, MBA numbers, ~20-branch save block) — and vice versa. Field-name split: create reads `fv.mp_client_name`, edit reads `fv.mp_clientname`. Write 401 copy + `WriteSessionExpiredError` throw on create postgres `!ok` so `handleSaveAll` does not clear dirty or navigate. Edit save no longer navigates on success (`exitAfter` / `saveDraftThenExit` only). Create primary Publish downloads then lands on `/mediaplans/mba/{mba}/edit`; `exitAfter` / `saveDraftThenExit` return to Campaigns. Both bars are `PlanWizardBottomBar` (Publish, Save draft, then `wizardDownloadControls` draft vs published groups; zip is Publish & download all on the Publish caret). `CampaignStatusControl` `persisted` is create=`mediaPlanId != null` / edit=`true` — do not gate on `mba_number` or derived phase. Wizard chrome is `PlanWizardHeader` passed as `PlanWizardShell.header` on both pages — primary row is title + subtitle only (no actions slot); `secondary` holds edit version trail + picker and remounts AVA skill actions (`AvaMediaplanCreateActions` / `AvaMediaplanEditActions`); do not restack a third hero row, put status or Creative/Trafficking back in the header, or put version chrome back on the Campaign Details card. Step 01 `#campaign-setup` frame + `#builder-section-campaign` card + `md:grid-cols-2 xl:grid-cols-4` field grid are lockstep class strings (edit has no `data-create-step`, no `PlannerCreateTargetsStrip`, keeps `mp_plannumber` in the grid). Edit bootstrapping paints that same `#campaign-setup` frame inside `PlanWizardShell` so xl swap does not shift the content column. Pin: `lib/mediaplan/__tests__/postgresSavePayload.integration.test.ts`. Save-bar messages render in `PlanWizardSaveMessages` below Draft Summary on both pages (`issues`, `extraProblemTexts`, `savePrimary`, `saveTip`, `isSaving`); draft banners sit under `PlanWizardHeader`; presence and the Draft badge live next to `PlanWizardVersionChrome`; discard confirms before `planDraft.discard()`; leave with a saved draft is Stay/Leave only; the floating bar is action buttons plus autosave status. Edit Draft Summary uses `summaryLoading`; MBA labels key on `mbaBusy`. Do not pass `saveMode` or `alerts` — those props no longer exist and the panel renders nothing if they are the only content. `isSaving` alone is enough to keep the card mounted mid-save. Both bars use `showPlanDraftSaveButton`; create passes `isPublished: false` (no published tip yet) and `gateDownloadsOnPublish={false}`. Draft MBA / Draft Media Plan POST `/api/mediaplans/draft-documents`; published MBA (vN) stays generate / stored GET. AA draft is edit-only. Both default exports wrap `useSearchParams` / `use(params)` in a page-module `<Suspense>` the client also renders (create `CreateMediaPlanPage`, edit `EditMediaPlanPage`). Keep `edit/loading.tsx`. Do not wrap `ClientLayout` `{children}` in Suspense. |
| `lib/auth/writeSessionExpiry.ts` / `SessionExpiredBanner` | Banner persists until a successful **write** (`postPlansSave` 2xx / draft PUT 2xx). Do not clear on GET `/api/me`. Create + edit + `usePlanDraftSession.persistServer` + SavingModal chrome must share the same copy |
| `proxyAllowlist.MEDIA_PLANS_ALLOWLIST` | Catch-all is staff-only (`requireRole`); `clearVersionChildren` needs `{table}/{id}` DELETE allowed; new staff POSTs 403 unless added |
| `LINE_ITEM_BROWSER_API_PATH` / `app/api/media_plans/<channel>` | Every map value must have a dedicated GET (`createChannelLineItemsGetHandler` + `checkClientMbaAccess`). Table-name segments hit the admin catch-all (C-89). `isChannelLineItemEndpoint` is `media_plan_*` only; kebab maps via `resolveChannelLineItemEndpoint`. Tests: `test:channel-line-item-routes` |
| `lib/mediaplan/campaignDatePresets.ts` / `CampaignDatePresetBar` | Create + edit pass no `presets` (default `CAMPAIGN_DATE_PRESETS`, four pills). Planner Stage A (`StageBrief`) passes `PLANNER_DATE_PRESETS` (adds Annual plan). Changing the default array changes both mega-pages. Tests: `lib/mediaplan/__tests__/campaignDatePresets.test.ts` |
| `lib/mediaplan/channelHydrationGate.ts` / `MediaPlanLoadStatusPill` | Edit-only load toast. `buildHydrationToastItems` must stay on the same maps as `computeAllChannelsHydrated`. Do not dismiss on `loadPhase === ready` / fetch arrival; do not count Campaign details in `m`. Hang copy after `HYDRATION_TOAST_HANG_MS`. `"error"` settles hydration but does **not** unlock Save (C-119 / BZ-3): `channelLoadSucceededFromMediaStatus` / builder load-status map; do not fold into `saveHeldForHydration`. Create has no twin pill, no duplicate save gate, and treats enabled channels as loaded-by-construction (`channelLoadSucceededWithoutFetch`). Tests: `lib/mediaplan/__tests__/channelHydrationGate.test.ts` + postgres-save BZ-3 |

### Pacing
| Touch | Also check |
|---|---|
| `lib/pacing/deliveryStatusFromPct.ts` | Campaign-delivery Ahead / Behind / On track (behind below 90, ahead above 110). Consumers: `programmaticAdapterShared`, `socialAdapterShared`, `searchAdapter`, `directDigitalAdapterShared`, portfolio assemble + `/pacing/portfolio` legend. Do not reuse for admin `PacingStatus` or search `compareCpcStatus` / `compareHigherIsBetter`. Tests: `npm run test:programmatic-delivery`. |
| `fetchSearchPacingCampaignRows.ts` | Search composer + shared master/version crawl (`fetchAllMasters` / `fetchCurrentVersionRowsForMasters`, `DATA_BACKEND_PACING`). Search **line items** follow `getDataBackendFor("plans")` — Postgres `fetchLineItemsFromPostgresByEndpoint("media_plan_search")` at the published watermark, else the Xano walk. Programmatic / social / ad-serving live lines go through `lib/pacing/plans/resolveLivePlanLineItems.ts`. Touching the live-master filter still changes every pacing surface. |
| `lib/pacing/plans/resolveLivePlanLineItems.ts` | Shared live-line resolver for programmatic / social / ad-serving. Postgres: `readPlanMasters` + `fetchLineItemsFromPostgresByEndpoint` at the published watermark. Else: existing Xano per-table walk. Warns when live masters yield zero rows. Tests: `lib/pacing/plans/__tests__/resolveLivePlanLineItems.test.ts`. |
| `pacingRowsCache.ts` | 5 channel API routes + Overview + AVA `getPacingSnapshot` + ops digest. Channel key shape must stay matched so Overview lands on the tabs' warm entries — changing it cold-starts Overview. `pacingScopeKey` is also the portfolio snapshot `scope_key` (`all` or sorted slugs). The portfolio GET no longer serves from the nested `["pacing-rows","portfolio",…]` cache. |
| `lib/pacing/portfolio/buildCampaignPacingRows.ts` / `assembleCampaignPacingRows.ts` / `portfolioRowFlags.ts` / `buildAndStorePortfolioSnapshot.ts` | Campaign-level portfolio for the cards / table view. Serving path is `pacing_portfolio_snapshots` via `GET /api/pacing/portfolio` (hit / admin inline+store / client 202) and `/api/cron/pacing-portfolio`. Composer loads the five channel caches in parallel (`loadPortfolioChannelSources`). Client graphs import flags/counts from `portfolioRowFlags` only — never `assembleCampaignPacingRows` (that module reaches `@/db` via `resolveClientSlugs`). Touches expected-spend resolution (`lib/spend/resolveCampaignExpectedSpend.ts`) and `channelCoverage` source statuses. Direct `REPORTED_SPEND` overlays onto the owning channel by lowercase `LINE_ITEM_ID` (same as `loadDeliverySnapshot`); do not dump fixed-cost programmatic / BVOD onto a Direct pseudo-row. Do not add Snowflake queries here. Tests: `npm run test:pacing-portfolio`. |
| `lib/pacing/detail/buildCampaignDetail.ts` / `assembleCampaignDetailPayload.ts` / `GET /api/pacing/campaign/[mba]` | Single-MBA overlay. Snapshot supplies `CampaignPacingRow` when present; otherwise assemble from this MBA's channel sources only. Payload includes `scenarioLines` (verification-only rows dropped) and `clientId` for Codex. Cards (`CampaignPacingCard`, `LinePacingCard`) and table View (`CampaignPacingTable`, Search) open `CampaignDetailModal`. Do not call portfolio composers from this route. Tests: `npm run test:pacing-detail`. |
| `lib/pacing/scenario/*` / `ScenarioPlannerPanel` / `GET\|POST /api/pacing/scenarios` | Pure engine plus planner UI. Consumers: card / detail "Plan a scenario", AVA Scenario tab. Recalc is local. Persist `pacing_scenarios` (0084 AUTHOR ONLY; C-76 fail-soft). Draft change note opens AVA with scenario JSON; Create Codex task POSTs `/api/codex/tasks`. Do not call `resolveCampaignExpectedSpendToDate` from the engine. Tests: `npm run test:pacing-scenario`. |
| `components/pacing/PacingShell.tsx` | Portfolio is first tab; `/pacing` redirects there. Channel tabs stay as drill-downs. Row filters stay disabled on Overview only. Portfolio toolbar hosts the Cards / Table toggle (`pacing.portfolioLayout`). |
| `queryPacingFact` (`lib/snowflake/pacing-fact.ts`) | social + programmatic + ad-serving composers AND 4 standalone POST routes. `CHANNEL LIKE '%…%'` matching — a warehouse channel rename silently yields zero rows, not an error. Social types are `meta` \| `tiktok` \| `reddit` (reddit LIKE before a generic fall-through). Programmatic OOH is `LOWER(CHANNEL) LIKE '%programmatic%' AND LIKE '%ooh%'` and must sit **before** display/video in the CASE so `Programmatic - OOH` does not match display. `pacingFactChannelWhere` is the shared matcher. Tests: `lib/snowflake/__tests__/pacing-fact.channelWhere.test.ts`. |
| `lib/pacing/social/classifySocialPacingPlatform.ts` | Social pacing attach (`resolveLiveSocialLineItems`), campaign delivery buckets (`CampaignDeliverySection`), snapshot groups (`loadDeliverySnapshot`), and `socialChannelCompute.classifyPlatform`. Meta / TikTok / Reddit only; anything else is unclassified and lands in campaign Awaiting delivery. Do not add a 21st `line_channel` for a platform. Tests: `lib/pacing/social/__tests__/classifySocialPacingPlatform.test.ts` |
| `buildSocialRedditSection` (`socialRedditAdapter.ts` / `socialAdapterShared`) | Campaign delivery Social Reddit container. Same shared adapter as TikTok (`platform: "reddit"`, warehouse channel `Social - Reddit`, connections “Reddit connected”, `socialmedia` colour). `DeliveryDataProvider` unions `redditLineItemIds` into `/api/pacing/bulk` (no new route). Bulk `pacing-service` CASE maps `%reddit%` before `%meta%` and `ALLOWED_CHANNELS` includes `reddit` or SOCIAL_PACING_FACT Reddit rows are dropped. `partitionRedditDeliveryLines` keeps lines with reddit fact rows in this container and sends the rest to Awaiting delivery so a Reddit line never renders twice. Snapshot group `social_reddit`. `summarizeActuals` returns `null` (not 0) for cpm/ctr/cvr with no impressions, cpc with no clicks, cost_per_result with no results — `KpiTile` dashed empty, not `$0.00`. Tests: `npm run test:social-delivery`. |
| `fetchDirectPacingRows` / `direct/types` / `queryDailyFacts` | `/pacing/direct`, Overview, ops digest, client dashboard delivered money (`lib/delivery/deliveredTotals`), AND `fixedCostMedia` overlay for programmatic **and** Direct Booked Digital (`loadDeliverySnapshot`, `/api/pacing/bulk`). Do not write a second REPORTED_SPEND query. |
| `fetchAllPlanLineItemsForDelivery` (`lib/api/media-containers.ts`) | Plan-line source for `loadDeliverySnapshot` → campaign strip + `GET /api/dashboard/[slug]/delivered` (`getDeliveredTotalsForClient`). Postgres when `getDataBackendFor("plans")` is `postgres`; otherwise delegates to `fetchAllMediaContainerLineItems` (Xano). Do not point AVA / creative at this helper — they stay on the Xano fan-out. Tests: `lib/delivery/__tests__/loadDeliverySnapshot.test.ts`. |
| `lib/pacing/ad-serving/*` / `lib/pacing/overview/mapOverviewItems.ts` | ZERO-$ LAW: `derive_spend_from_plan` is OFF for all Direct Booked Digital. Ad-serving DTOs have no budget/spend columns; `mapAdServingRowToOverviewItem` keeps `budget: null` / `spendToDate: null`. Do not wire modelled spend here. Tests: `lib/pacing/overview/__tests__/mapOverviewItems.test.ts`, `components/dashboard/delivery/channels/__tests__/directDigitalChart.test.ts` |
| `slugifyPlanClientName` | Auth scoping in every pacing route; note it is a DIFFERENT slugifier from `lib/clients/slug` — both sides of the auth→plan join |
| Orphan assign | UPDATEs `MART.SEARCH_PACING_FACT` directly (no mapping table) — reverted by any warehouse full refresh |
| `lib/pacing/admin/unmappedPlacements.ts` / `GET /api/admin/unmapped-placements` / `/pacing/admin/unmapped-placements` | Read-only CM360 list. LABEL_MAP first, then snapshot published ids. Do not add assign/write here. Tests: `npm run test:unmapped-placements`. Toolbar treats all `/pacing/admin/*` as unfiltered. |
| `lib/partner-ingest/*` / `GET\|POST /api/cron/partner-ingest` / `ASSEMBLEDVIEW.RAW.PARTNER_*` | Multi-supplier mailbox ingest. Two-factor sender match (`SENDER_DOMAIN` + LIKE `SUBJECT_PATTERN`). SHA-256 skip on `(INTERNET_MESSAGE_ID, ATTACHMENT_NAME, ATTACHMENT_SHA256)` with `STATUS='loaded'`. Raw lines INSERT before parse. DELETE+INSERT on one `withSnowflakeSession` — never `querySnowflake`/`execWithRetry` (serverless one-statement). No UPDATE on RAW. Uncoded rows load NULL. Mail is moved, never deleted. T1+T4+T5+T6 gate (T4/T5 are `n/a` for a source with no video columns; T5 scoped to `VIDEO_VIEWS > 0`; C-123). Tests: `npm run test:partner-ingest` against `lib/partner-ingest/__tests__/fixtures/channel-factory-2026-09-14.xlsx` and `vistar-exchange-sample.csv`. Graph token uses `PARTNER_INGEST_*` only, never `M365_*` / `NEXT_PUBLIC_M365_PROVISIONING` (C-116). |
| `lib/partner-ingest/parsers/index.ts` (`parserForSource`) | The only slug→parser map. Adding a supplier means a `PARTNER_SOURCE_MAP` row AND an entry here AND an `EXPECTED_HEADER` for T1 — a slug with no parser throws `PartnerIngestError` and `runPartnerIngest` files the mail as `unrecognised`, so the supplier goes silently unloaded. Shared cell/date/matrix helpers live in `parsers/shared.ts`; `parseReportDate` serves every parser (Date, Excel serial, `YYYY-MM-DD`, `d-MMM-yy`). Tests: `__tests__/parsers.test.ts`, and `__tests__/parseChannelFactory.test.ts` pins the Channel Factory `ParsedPartnerFile` by SHA-256 so a shared-helper change cannot drift it. |
| `lib/partner-ingest/parsers/parseVistar.ts` | Vistar exchange CSV (prog OOH, Vistar + Broadsign buys). Header found by `Day`+`Impressions` in the first 20 rows; `detectedHeader` is comma-joined (Channel Factory is pipe-joined) so `EXPECTED_HEADER` must match the separator. Blank-`Day` totals row stays in `PARTNER_FILE_LINES` only and feeds T6. Impressions stay fractional through the parser and are rounded once, in `deliveryInsertBinds`. `AV_LINE_ITEM_ID` comes from `Contract Number` (usually blank), not the creative name. A missing required column throws `PartnerIngestError` → `parse_failed` naming the column. |
| `lib/partner-ingest/sql.ts` `DELIVERY_COLUMNS` | 25-column INSERT; `AMOUNT_SPENT`, `PLAYS`, `VENUE_TYPE`, `METRO_AREA`, `STATE`, `PARTNER_CAMPAIGN_ID`, `PARTNER_CREATIVE_ID` sit after `SOURCE_FILE` and must stay in the order `sql/snowflake/raw/partner_ingest_tables.sql` declares. Channel Factory binds NULL for all seven (ZERO-$ stays a view concern). `DELIVERY_PARAMS_PER_ROW` drives the writer's batching — never hard-code the count. `resolveLoadMode` is the single `LOAD_MODE` gate for the writer and for `runPartnerIngest`. |
| Splitting/renaming a delivery `ChannelKey` | `types.ts`, `getChannelIcon`, `channelMediaTypeColour`, `shouldShowChannelAggregate`, the adapter (`planOnlyAdapter` for `plan-only`, `socialRedditAdapter` for `social-reddit`, `programmaticOohAdapter` for `programmatic-ooh`), `CampaignDeliverySection`, `CampaignPageAssembly`, `page.tsx`, `DeliveryDataProvider`, `loadDeliverySnapshot` group strings, and the two group-string consumers (`assembleCampaignReportData` `CHANNEL_LABELS`, `getPacingSnapshot`). Adding `plan-only`, `social-reddit`, or `programmatic-ooh` is this row, not a 21st `line_channel`. |
| `DeliveryDailyChart` / `DeliveryDailyChartSeries.format` | Optional per-series ComboChart format. Bars default `dollars`, lines default `number` — every existing caller relies on those defaults. Direct Booked Digital passes `number` / `percent`. Changing a default silently reformats every dual-axis delivery chart that omits `format`. |

### Dashboards
| Touch | Also check |
|---|---|
| `lib/api/dashboard/client.ts` / `lib/dashboard/clientDateRange.ts` / `components/dashboard/shared/DateRangeSelector.tsx` | Client hub URL `startDate`/`endDate`; no params = current AU FY. Lists overlap the range; spend series share the window; delivered tile `from`/`to`; `spendPast30Days` stays 30-day. `getClientDashboardData` defaults `campaignScope: "row"` (tenant: requested `clients` row only). Admin hub `/client/[slug]` passes `"group"` (mbaidentifier name-slug union). Group lookup reads `readClientsList` then `resolveClientGroup`. Overlay `client_id` via `overlayMasterOwnedListFields` (DI-9b twin). Smoke: `/dashboard/[slug]`, `/client/[slug]`, `GET /api/dashboard/[slug]`, `/delivered`, `/name`, `/dashboard/[slug]/creative`. Tests: `test:client-dashboard-range` |
| `lib/delivery/programmatic/programmaticCompute.ts` / `components/dashboard/delivery/channels/programmaticAdapterShared.ts` / `lib/delivery/deliverySourceMap.ts` / `lib/delivery/deriveSpendFromPlanRate.ts` / `lib/delivery/programmatic/applyReportedSpend.ts` / `CampaignDeliverySection` / `ChannelSection` / `shouldShowChannelSectionSummary` / `loadDeliverySnapshot` / `0063_delivery_source_map` / `0074_delivery_source_map_channel_factory` / `0075_delivery_source_map_twitch` / `0077_delivery_source_map_prog_ooh` | Campaign Programmatic Display + Video + OOH. Programmatic 2+ lines render the existing `aggregate` chips + daily chart (`shouldShowChannelSectionSummary`); progress cards / KPI band stay gated (C-37 / C-41). Do not recompute section totals in `ChannelSection`. Keep a line when the map has an active row for `(publisher \|\| platform)` after trim+lowercase; unmapped stays excluded from the programmatic container (campaign delivery remainder). `snowflakeChannelsForDeliverySource(source, dspChannel, lineChannel?)`: `dsp` and `partner_file` consume the family channel (`prog_video` keeps the DSP video channel; `prog_ooh` → `programmatic-ooh`); `cm360` is `ad-serving`. Callers that omit `lineChannel` keep the prior DSP-channel behaviour. Connection pills name the source (`CM360 (Quantcast)` never `DV360 connected`; Channel Factory is `Channel Factory (partner file)`, Vistar/Broadsign are `Vistar (partner file)` / `Broadsign (partner file)`). Twitch is `cm360` with `derive_spend_from_plan` true (Quantcast pattern). Vistar and Broadsign are `partner_file` / `derive_spend_from_plan` false (`0077` AUTHOR ONLY mirrors the TS seed). `derive_spend_from_plan === true` replaces that line's `actualsDaily.spend` with modelled plan-rate spend (capped at planned media, labelled modelled; deliverables uncapped). DSP spend stays Snowflake. `fixedCostMedia` programmatic lines overlay `REPORTED_SPEND` from `queryDailyFacts` (same reader as Direct) after line+date grouping and are labelled "Reported spend (fixed cost)". The modelled-spend label stays on `derive_spend_from_plan` only. Programmatic OOH CPM deliverable is IMPRESSIONS (plan `calculatedValue`); PACING_FACT `RESULTS` is plays as a KPI (CPM, Plays, Cost per play — no CTR/CPC/CVR/CPA). Daily chart is Spend + Impressions. Channel Factory is `partner_file` / `derive_spend_from_plan` false (Datorama report 1248052; no platform cost). Overview skips those rows (`includeProgrammaticRowInOverview`); `combineDeliveredTotals` excludes snapshot programmatic IDs (`programmatic_display` \| `programmatic_video` \| `programmatic_ooh`) from the Direct sum. Programmatic pacing tab: delivery stays, CPM/CPV and spend-pacing Behind are suppressed, pointer to `/pacing/direct`. The flag is OFF for all Direct Booked Digital, so `lib/pacing/ad-serving/*` and `mapOverviewItems.ts` keep no-spend row shapes — do not touch `directDigitalAdapterShared` for modelled-spend. CM360 programmatic lines attach `EntityBreakdownTable` `columns: delivery` from original `combinedRows.entityId`/`clicks` via `groupPacingRowsByPlacement` (Dv360DailyRow drops `entityId`). DSP / partner_file lines do not. Do not model spend onto that table. Do not SELECT `delivery_source_map` until 0063 is applied (C-76); 0074/0075/0077 seed the table only — runtime is still the TS seed. `summarizeDv360Actuals` returns `null` (not 0) for cpm/ctr/cvr with no impressions, cpc with no clicks, cpa with no conversions — tile builders pass null through; do not format null as `$0.00`. Tests: `test:programmatic-delivery` |
| `lib/dashboard/spendInsightsCaptions.ts` / `components/dashboard/SpendingInsightsSection.tsx` | Captions are `spendInsightsCaption` (planned delivery-schedule-month media). Do not reuse `MEDIA_MIX_DONUT_BASIS_CAPTION`. Tests: `lib/dashboard/__tests__/spendInsightsCaptions.test.ts` + `SpendingInsightsSection.render.test.tsx` |
| `components/dashboard/campaign/CampaignStatusStrip.tsx` / `lib/dashboard/statusSentence.ts` / `lib/dashboard/plannedImpressions.ts` / `lib/delivery/channelCoverage.ts` / `components/dashboard/campaign/ChannelsAtAGlance.tsx` | Campaign MBA "Where we are" strip replaces `CampaignSummaryRow` + the verdict `<p>` (keep that file and `campaignSummaryRowProps` / `campaignPacingVerdict` tests). Pill is `pacingStatus` via `campaignPacingVerdict` only when `actualSpend` is a finite number (unset spend is not $0 behind). Sentence is `statusSentence` rules a–d (first match); unset spend after rule a is "No delivery reported yet." `channelCoverage` is one entry per channel-platform group (`reporting` / `connecting` / `not_started` / `no_source`); glance cards omit `no_source` (those stay in Awaiting delivery). Connecting = `delivery_source_map` row or social classifier and no fact rows; a direct-digital group with neither is `no_source` (Awaiting delivery), not Connecting. caption "Awaiting first report" (no expected-date field — C-131). Strip `channelsReporting` / `channelsTotal` / ahead-channel come from that helper; planned impressions switch to reporting groups and the caption says "channels reporting" (all-line-items fallback when coverage is empty). Glance sits under the strip; hide when coverage cards are empty. Planned glance/strip totals come from section deliverable captions (Planned Y), not raw line `impressions`. Direct-digital groups read `progressCards[0]` (impressions); other families read `[1]`. When matching line ids exist, those line captions are summed. Glance noun is Views only when the whole group is CPV. ZERO-$ LAW on glance spend. One `/api/pacing/bulk` via `CampaignDeliverySection` feeds glance + accordion (`onCoverage`). KPI tiles pass null ratios through (`KpiTile` dashed empty). Tests: `lib/delivery/__tests__/channelCoverage.test.ts`, `components/dashboard/campaign/__tests__/ChannelsAtAGlance.test.tsx`, `lib/dashboard/__tests__/statusSentence.test.ts`, `components/dashboard/campaign/__tests__/CampaignStatusStrip.test.tsx`, `lib/dates/__tests__/melbourneDayMetrics.test.ts` (`test:campaign-dashboard-range`) |
| `app/dashboard/[slug]/[mba_number]/components/CampaignPageAssembly.tsx` | **Consumers.** Sole production mount: `app/dashboard/[slug]/[mba_number]/page.tsx` (SSR spend/delivery, `isAdmin`, line-item buckets, `showDeliverySection`). Changing section order or `isAdmin` gates hits admin and client views together. Composed: `CampaignReadSection` (between hero and Where we are; clients see published only), `CampaignStatusStrip`, `ChannelsAtAGlance` (hidden when coverage cards empty), admin Hours / **KPI review** (`KpiReview`, clients + admins; `kpiPacing` unmounted C-135) / insights, `CampaignDeliverySection` (accordion plus hidden coverage-only mount for `onCoverage` + `onKpiReviewGroups` + `onDeliveredByLineId`), `MediaPlanVizSection` (`isAdmin` Weekly/Monthly + PNG), `SpendChartsRow` ("The plan": donut + monthly stack, no summary tiles). Downstream of any `ChannelKey` split. Gantt wrap 44×2; future-start bars `Starts {d MMM}`; delivered-in-bar uses accordion deliverable actuals (`delivered / planned`, fill capped at 100%). Tests: `CampaignPageAssembly.order.test.ts`, `handleCoverage.identity.test.ts`, `SpendChartsRow.test.tsx`, `mediaGanttReshape.test.ts`, `lib/charts/__tests__/wrapGanttLabel.test.ts`, `lib/kpi/__tests__/kpiReview.test.ts`, `KpiReview.test.tsx`, `CampaignReadSection.test.tsx` (`test:campaign-dashboard-range` / `test:kpi-review` / `test:campaign-read`) |
| `lib/charts/registry.ts` / `lib/dashboard/campaignMediaTypeTags.ts` / `MediaChannelTag` / Home `DashboardOverview` / `/mediaplans` | Media-type pill colour is `getMediaBadgeStyle`. List tags emit `campaignMediaTypeTagLabels` (registry labels), never a local Tailwind switch. Aliases must keep all 20 `line_channel` values on a registry colour. Tests: `npm run test:charts-registry`. |

### Planning
| Touch | Also check |
|---|---|
| `lib/planning/upload/*` (`parseRoyMorganWorkbook`, `mapRoyMorganToChannels`, `buildUploadedAudienceResponse`, `uploadedAudienceRepo`) / `POST /api/planning/uploads` / `POST /api/planning/uploads/[id]/audiences` / `POST /api/planning/audience/uploaded` / `GET /api/planning/uploaded-audiences` / `0058_planning_uploaded_audiences` / `AudienceDraft.source` / `plannerAudienceRequest` / `planner-client` `fetchOne` / `parseSavedDefinition` / Stage B upload UI (`StageAudiences`, `UploadAudienceDialog`, `UploadedAudiencePicker`, `UploadCoveragePanel`) / `TaxonomyStatusBadge` / `OutcomeCharts` addressable card / `StageCompare` save + footer / `ExportDeckButton` / `buildPlannerDeck` / `plannerDeckProvenance` | Must keep emitting locked `AudienceResponse` so `adaptAudienceToEngine` is unchanged. Optional `mapping_provenance` is upload-only. No Snowflake universe fallback when TOTAL is absent (`universe_wc` 0). Alias table grows with a test, not a silent code change. Save maps server-side from `parse_json` and retains the upload in the same transaction as the audience insert. Rebuild (`POST /api/planning/audience/uploaded`) uses row scalars, not parent `parse_json`. Uploaded drafts fetch `/api/planning/audience/uploaded`; composed path unchanged. `planning_audiences.definition_json` is additive (missing `source` loads as composed). Deck provenance is extra lines in existing placeholders — do not add slides. Create handoff stays `buildRecommendedSplitV1` → `mapEngineSplitToCreateTargets` (source-blind). Parser is `server-only` — the dialog POSTs the file. Tests: `lib/planning/upload/__tests__/royMorganUpload.test.ts`, `coverageHonesty.test.ts`, `plannerAudienceRequest.test.ts`, `parseSavedDefinition.test.ts`, `plannerDeckProvenance.test.ts`, `plannerSessionPersist.test.ts`, `components/planning/__tests__/audienceDraftSource.test.ts`, `taxonomyHonesty.test.ts`. |

### Finance / billing
| Touch | Also check |
|---|---|
| `computeCampaignFinancials.ts` | MBA create + edit pages (3 call sites), `recomputeBillingScheduleOnSave` (→ every schedule PATCH can 409), `computeCampaignFinancialsFromVersion` (→ all receivables) |
| `deriveReceivableRecords.ts` | Hub/invoicing compose (`composeFinanceHubRecords`). Record status is `resolveFinanceCampaignStatus` (once). Client-pays **media** is refused when the blob item **or** the joined plan line is client-pays; Assembled fee stays on the month header. `total` stays `monthExGstFromScheduleEntry`. Tests: `npm run test:finance-derive` |
| `resolveFeePctFromFeeLoading` / `FEE_FIELD_BY_MEDIA` | Wrong mapping = save-blocking 409s for planners, not just wrong numbers. Special cases: production→0, influencers→`feecontentcreator` fallback, integration→no fallback |
| `monthExGstFromScheduleEntry` | THE definition of a month's ex-GST value: receivable totals, `billableEqualsMba` validation, MBA panel indicators |
| `receivableMergeKey` / `composeInvoiceKey` | Key change orphans existing `finance_billing_records` status rows → billed invoices reappear as unbilled |
| `finance_billing_records` lifecycle stamps (`0053`) | State is derived via `lib/finance/billingLifecycle.ts` — do not add a `state` column. Do not drop `billed_*` until nothing reads them. PATCH-by-id is field-allowlisted and refuses `billed` / `billed_at` / `billed_by` / `total` / billed snapshot / lifecycle stamps (`FIELD_NOT_ALLOWED`). `POST /api/finance/billing/mark-billed` is 410. Line-item `amount` / `received_amount` freeze on parent `approved_at` (`APPROVED_FROZEN`); **delete** of a line uses the same SQL predicate (`r.approved_at IS NULL`). Unapprove / mark-exported `ok` is `errors.length === 0` (boolean), not a literal true. Do not add an account-lead on `clients`. `matched_xero_invoice_id` lives on the app record (text, no FK); do not alter `xero_ar_invoices` / `xero_ap_bills` for this link. Partial index `approved_at IS NULL` is the To bill working set. Approve POST never trusts a client grain — compose via `loadComposedBillingRecordsForMonth` then `grainFromBillingRecord`. Mark-exported is "Mark as sent to finance" (skips unapproved keys); Excel does not stamp. Unmark-exported clears `exported_at` / `exported_by` only. Drifted approved rows re-approve with `reapprove: true`; once `exported_at` is set, re-approve refuses (`ALREADY_EXPORTED`) — un-mark sent first. A re-approve audit `old_value` is JSON of the prior `approved_at` and `approved_amount_cents`. **`db:etl` must not truncate-reload `finance_billing_records` / `finance_billing_line_items` (C-85)** — both are in `POSTGRES_AUTHORITATIVE_TABLES`. |
| `scripts/db/drift.ts` / `scripts/db/mirrorColumnDrift.ts` | Live Postgres vs `db/schema/*.ts`. Mirror-ahead columns (named in the TS, absent from the database) are a deploy blocker — FATAL banner, exit 1. Do not treat `db:generate` empty-diff as proof the DB has the columns. Fixture tests: `npm run test:db-drift`. Live check: `npm run db:drift` against the applied database before cherry-pick to `main`. |
| `lib/xero/client.ts` (`xeroApiRequest`) | Accounting API is GET-only — no body, never POST. Token refresh at `identity.xero.com` is a different endpoint. Do not add a write method. Call sites: ingest invoices, contacts refresh, PDF fetch. |
| `lib/finance/billingLifecycle.ts` / `components/finance/BillingStateBadge.tsx` | Invoicing overlay + `/finance/invoicing` + `/finance/owed` badge. Overdue = critical, paid = success, all other states secondary. Approval drift (`approved_drift` from `detectBilledDrift` against the approval snapshot in `applyStatusOverlay`) — Owed still labels the approved badge "Approved · changed since"; invoicing rows pass `approvedDrift={false}` and put the copy in `RowActionLine` context with Re-approve in ⋯. Do not block writes. Do not invent a parallel status palette. The badge is the finance-row **pill** (never clickable). Inline-amount write gate is `needsInlineAmountConfirm` (evidence **or** `billed === true`). Tests: `lib/finance/__tests__/billingLifecycle.test.ts`, `lib/finance/__tests__/approvalDriftOverlay.test.ts`. |
| `components/finance/RowActionLine.tsx` / `RowActionMenu.tsx` / `InvoiceDocumentButton.tsx` | Locked three-way row chrome (CONVENTIONS.md). Pill → context → spacer → primary → document → ⋯. Document control returns null when no PDF — never disabled, never "coming soon". Disabled overflow items stay visible with `disabledReason`. Mounted on `/finance/invoicing` per `invoice_key` and `/finance/in-xero` per match row (Differs → Accept Xero figure; Missing → no primary; Extra → Assign). Do not add a fourth kind of control. Tests: `components/finance/__tests__/rowActionLine.test.tsx`, `components/finance/sections/invoicing/__tests__/invoicingPlanRow.test.tsx`, `components/finance/sections/inXero/__tests__/inXeroMatchRow.test.tsx`. |
| `lib/finance/periods/preRunSweep.ts` (`clientMissingBlockers`) | Period pre-run card **and** invoicing row blockers (ABN / legal name; no PO — C-84). Import the predicate, never `buildPreRunSweepCard` / `orchestrate` from the invoicing UI. Flag `FINANCE_PERIODS` stays off. |
| `components/ui/MoneyInput.tsx` | Shared primitive: invoicing inline cell, create+edit campaign budget, ProductionContainer burst cost, forecasting TargetGrid. `onChange` fires on blur only (parse-then-format). Do not make it call `onChange` while typing without checking those four other consumers. Invoicing cell commits from that parsed `onChange` argument — never a stale `draft` from a parent `onBlur` |
| `components/finance/receivables/InlineScheduleAmountCell.tsx` | `/finance/invoicing` media lines with `schedule_line_item_id`. Write path is `commitInlineScheduleAmountEdit` (billing-schedule PATCH). Display chip uses `hasBillingEvidence(record.state)`. Write-gate confirm is `needsInlineAmountConfirm` (evidence **or** `billed === true`) so a legacy billed row that is still `ready` still prompts. Confirm is an AlertDialog (`layer="nested"`), not `window.confirm`. Tests: `npm run test:inline-schedule-amount` |
| `components/finance/receivables/ReceivablesSummaryStrip.tsx` / `lib/finance/sections/invoicingFunnel.ts` | `/finance/invoicing` KPI tiles are a funnel: Ready to approve · Approved · Sent to finance (labels match the row pills). Amounts are `record.total` regrouped by `invoicingFunnelBucket` — ready; approved; everything past approved — so the three cents add to the scope total. Tiles + All filter the row list by lifecycle (`recordMatchesLifecycleFilter`); default Ready; independent of campaign STATUS. Must use StatTile four-state model — loading/error never render `$0.00`. Invoicing passes `accent="none"` (ex-GST is once in the page header: "All amounts ex-GST"). Bulk approve is per-month on the month bar (`BulkApproveReadyButton`), not the filter row. Un-approve lives on the approved row (`UnapproveBillingButton` / `unapproveCopy.ts`). Tests: `lib/finance/sections/__tests__/receivablesSummaryStrip.test.ts`, `invoicingFunnel.test.ts`, `unapproveCopy.test.ts`, `invoicingBulkApproveCopy.test.ts`, `components/finance/sections/invoicing/__tests__/bulkApproveReady.test.tsx`, `invoicingPlanRow.test.tsx` |
| `relevantPlanVersions` | Which version is authoritative per month, across receivables/payables/finance data. Staged-unpublished versions must never become relevant. Still watermark (`master.version_number` via `buildMbaToLatestVersionMap`) — not `PUBLISHED_VERSION_JOIN_SQL`. Feeds `/api/finance/billing` and `/api/finance/billing/approve`. Same C-113 dollars, different bug; leave until scoped. |
| Forecast `line_key`/`group_key` taxonomy | Breaks comparability of **existing persisted snapshots** (variance joins on those keys) and target-row validation |
| `billedDrift` hash functions | Every already-billed record's stored hash mismatches → whole book flags as drifted |
| `lib/billing/balancer.ts` / `collisionWorksheet.ts` | Timing editors + edit postgres save collision pause when `NEXT_PUBLIC_BILLING_BALANCER=on`; C2 gates unchanged |
| `lib/finance/periods/*` / `0010_finance_periods.sql` / crons `finance-pre-run|run|lock` / sections `components/finance/sections/periods/*` + `/finance/periods` | Flag `FINANCE_PERIODS`; publish→stale via `savePlan`; lock sheets immutable Blob; AUTHOR-ONLY migration; sections board exposes run/lock/approve/adjust/hold only (classic hub `FinancePeriodRail` removed FN7); FIN-8 hides Periods tab in Clients billing when flag off; admin POST `/api/admin/finance-periods/{run,lock}` 409 `FINANCE_PERIODS_OFF` when off (never `force`); cron routes gate on the flag first and pass `force` only after that gate; PeriodDetail Run/Lock stay visible but disabled with tooltip while mode is off; C-68 collectors |
| `lib/finance/sections/scopeUrl.ts` / `financeHref.ts` / `SectionScopeBar` / `useFinanceScope` / `defaultScope.ts` | In-section finance links must go through `financeHref` (keys from `FINANCE_SCOPE_QUERY_KEYS` / `scopeToSearchParams`). Sidebar snapshot stays static; AppSidebar attaches the query at render. Apply always enabled (clean Apply refetches via `scopeVersion`). Periods hydrates from the URL on mount (same `hydrateFromUrl` as the bar) but has no bar; Xero neither hydrates nor shows a FY notice — UX-4. Do not fold Investment cut `basis`/dimension into this helper. `clampToCurrentMonth` is default/initial scope only (`buildDefaultFinanceScope`); Apply / draft months / URL parse use `normaliseToFy`. Tests: `scopeStore.test.ts` |
| `lib/finance/sections/owedLedger.ts` / `owedQuery.ts` / `GET /api/finance/sections/owed` / `/finance/owed` | Debtors ledger. Do not FY-clip the default view. Do not drop unresolved clients. PDF via `GET /api/finance/invoices/[xeroInvoiceId]/pdf` when `pdfAvailable` — never href a private `pdf_file` blob. VOIDED/DELETED out of every bucket/total. Report only — no dunning. Ex-GST cents. Tests: `owedLedger.test.ts`, `invoicePdf.test.ts`. |
| `lib/finance/invoices/invoicePdf.ts` / `GET /api/finance/invoices/[xeroInvoiceId]/pdf` / `GET /api/finance/bills/[xeroInvoiceId]/pdf` / `lib/auth/assertClientAccess.ts` | CB-6 proxied private-blob PDF read. AR: admin or CB-1-resolved client via `assertClientAccess` (`decideClientAccess` passes when `requestedClientId` is in the `resolveClientGroup` member-id set). AP is a sibling (admin-only) — different table and no client tenant. Missing `pdf_file.url` and `BlobNotFoundError` are typed 404 `PDF_NOT_AVAILABLE`, never 500. Do not make the blob public. Future helper move: dashboard slug checks in `app/dashboard/[slug]/[mba_number]/page.tsx` and `app/dashboard/[slug]/creative/page.tsx`. |
| `lib/finance/sections/draftMatch.ts` / `draftMatchQuery.ts` / `pullXero.ts` / `GET\|POST /api/finance/sections/draft-match` / `POST /api/finance/sections/pull-xero` / `/finance/in-xero` | Draft match before Xero authorise. GET report is a pure read. Pull = ingest then import then auto-stamps (stamps only are transactional; ingest and import are sequential and not rolled back together) (24h If-Modified-Since; JSON `notes.source=pull-xero` + `pulled_by` skipped by cron resume). Unchanged matches do not rewrite `matched_at`. Auto stamps skip a manual match (or a different existing invoice) without rolling back; genuine missing rows still fail the transaction. Never unguarded `notes::jsonb` — CASE in `lib/xero/syncLogNotes.ts`. Per-user 1/min is best-effort from the last completed pull-xero log row; it does not reserve and does not serialise concurrent pulls. Do not write to Xero. Do not mutate `approved_*` on accept. Do not auto-resolve Extra. Do not reuse PC6 `matcher/threeTier.ts` — reuse `matchMbaAgainstMasters`. VOIDED/DELETED ignored. Two drafts in one client-month = Differs listing both. Tests: `draftMatch.test.ts`, `draftMatchHonesty.test.ts`, `pullXeroRateLimit.test.ts`, `syncLogNotes.test.ts`. |

### KPI
| Touch | Also check |
|---|---|
| `lib/kpi/types.ts` (`ResolvedKPIRow`) & `deliveryTargets.ts` (`KPITargetValues`) | Treat as frozen public API — 41 importing files across pacing, delivery, dashboards, mediaplans, scripts |
| `lib/kpi/percentUnits.ts` (`normaliseRatioTarget` / `parsePercentHeuristic` / format / cell tint) | UI percentage points ↔ stored decimal for ctr/vtr/conversion_rate/viewability; NEVER cpv. No magnitude heuristic (AV-25 v2). Unset returns null, never 0. Data migration pending Luke — scan via `npm run scan:kpi-percent-units` |
| `lib/kpi/kpiWriteHandlers.ts` + `lib/data/writeKpi.ts` + `app/api/kpis/{campaign,campaign/sync,client,publisher}` writes | Admin write matrix after `requireRole(["admin"])`; validation → named 400; campaign/client PG-first + Xano mirror (X5); campaign_kpi upserts on `(lower(mba_number), version_number, lower(line_item_id))` — never a second row; publisher still Xano; percent bodies decimal ≤1 (`percentUnits` — no magnitude heuristic) |
| `deliveryTargetCurve.ts` | Contract behind every delivery chart's target line |
| `components/kpis/kpiHost.ts` (`createMediaPlanKpiHost`) | Media-plan Save KPIs persists `campaign_kpi` immediately when the plan has an identity (edit always; create after first save). Plan save still syncs the version it just wrote via `buildCampaignKpiSavePayload`. Tests: `npm run test:kpi-host` |
| `lib/kpi/kpiWorkbook.ts` (`buildKpiWorkbookBlob` / `toKpiSheetRows`) | Standalone Campaign KPIs xlsx. Columns: CTR / VTR / Conv Rate / Frequency (no CPV). Consumers: `KPISection` Download KPIs, create/edit zip-all, media-plan xlsx KPI append. Mapping is here only — do not re-copy `ResolvedKPIRow` → `KPISheetRow` in the mega-pages. Tests: `npm run test:kpi-writes` |
| `lib/kpi/kpiReview.ts` / `KpiReview.tsx` / `lib/kpi/kpiBandTargets.ts` | Campaign MBA per-channel review. Groups = `channelCoverage` buckets with `planByLineId`; targets never averaged across channels. CPV is a plan rate, not `campaign_kpi.cpv`. Row `targetSource` is `target` \| `benchmark` \| null; CPV captions name which plan-rate method applied. Channel KPI-band tiles (`KpiBand` / adapters) overlay the same resolvers via `applyKpiBandTargets` — do not add a second target path. All-empty cards use one pending/plan-edit line. Clients gated by `shouldShowKpiReview` (saved, benchmark, or CPV plan rate). Frequency blocked on reach (C-134). `kpiPacing.ts` unmounted (C-135). Tests: `npm run test:kpi-review`. |
| `scripts/kpi/backfill-benchmarks.mjs` / `lib/kpi/fanOut.ts` | Industry fill for published live/booked/approved lines: empty/all-zero UPDATE, missing INSERT, any non-zero CTR/CVR/VTR/frequency untouched. Never writes `cpv`. Requires 0080. Fan-out on plan save sets `target_source: "target"` so a saved KPI replaces a benchmark row. Tests: `npm run test:kpi-backfill`. |

### AVA / creative
| Touch | Also check |
|---|---|
| `lib/ava/types.ts` (`PageContext`, `FormPatch`) | 13+ files outside AVA: trafficking, creative, dashboard, finance, planning, both mega-pages |
| `lib/assistantBridge.ts` | 12 provider call sites; `__AV_ASSISTANT__` is a window global — breakage is silent, no compile error |
| `components/ChatWidget.tsx` / `components/ava/ChatAssistantMarkdown.tsx` | Presentation only: assistant markdown + GFM tables; user plaintext. Visible media-math calculator is `AvaMediaMathPanel` (toggle `ava:calc-open`, same localStorage namespace as `ava:panel-size`) — it calls `solveMediaMath` in-process and appends into the composer only, never the form. Clear empties budget / rate / deliverables / weeks / months and restores the cold-start hint (buy type kept; disabled when already empty). Ingest chip/error copy follows `docs/brain/AVA-VOICE.md` (no stage ids). Check ChatQuestionCard keyboard (Other + Skip + Confirm), ingest attach proposal (Review it / Not now), ingest confirmed block (Total line items / Total budget / Bonus line items from `IngestChatSummary`, never re-summed in the prompt), file-drop overlay (`isFileDragOver`, not widget-move `isDragging`), `ClientBrainPanel` / `InsightListRow` (share `react-markdown`). Do not add a second markdown library. Skip answers use `SKIP_ANSWER` / `isSkipAnswer` (`lib/ava/chatInterviewQuestion.ts`); ingest skip ≡ Leave unmapped (decline, no remap persist). Remove this mapping is the only chat delete. Load-into-form on create/edit (`lib/ava/applyIngestLineItemsLoad.ts` via `handleSetLineItems`) enables the channel flag and, on edit, dual-writes hydration + media so the container mounts; on a Partial MBA it unions loaded billing-stable ids into `partialMBASelectedLineItemIds` (all-in). Check both mega-pages, the returned `{n} lines loaded from {publisher}, all in scope` note, Excel `excluded_from_mba_scope_note`, and MbaBillingModal Gross Media. |
| Adding an AVA tool | `tools/registry.ts` **throws at module load** if order/names diverge from `AVA_TOOL_NAMES` in `summaries.ts` (same index) → 500s the whole chat route. Surface offer of `accept_ingest_proposal` is `pageToolOffer.ts` / `avaToolDefinitionsForPage` — create/edit and unknown route omit it (fail closed). |
| AVA `fy` / `fyToRange.ts` | Ending-year AU FY for all five Postgres tools; responses echo `range`. Finance hub `fyMonthRange` remains start-year — do not unify blindly |
| `lib/ava/anthropic.ts` (`AVA_MODEL`) | NOT AVA-only: ad-copy + search-copy routes, `researchClient`, and ingest `avaColumnMapping.server.ts` share it. **`import "server-only"`** — must not be imported from Client Components. |
| `lib/insights/queryCampaignInsights.ts` / `writeCampaignInsights.ts` / `/insights` / `GET|POST /api/insights` / `GET|PATCH /api/insights/[id]` | Admin-only library. Default live-only (`superseded_by IS NULL` → `idx_campaign_insights_live`); body search must stay `to_tsvector @@ plainto_tsquery` (GIN); never delete; EDIT=supersede / RETYPE in-place type only / RETIRE via `Retired:` row; writes return chain; never client-facing. |
| `lib/campaign-read/**` / `GET\|POST /api/campaign-reads/**` / `CampaignReadSection` / `assembled-campaign-read` / `0079_campaign_reads` / `0082_campaign_reads_generating` | Dashboard campaign read. `POST /generate` inserts `generating`, returns 202 (`maxDuration` 120), then `after()` runs the job on the same instance (`draft` or `failed` + `error_message`). Generate and GET fail any `generating` row older than 10 minutes (`Timed out`). Generate tools are campaign-scoped (`get_campaign_context`, `get_delivery_snapshot`, `get_campaign_insights` + this MBA's KPI review rows) — never `get_pacing_snapshot`. GET is tenant-scoped (`checkClientMbaAccess`); clients receive published only. Admin GET includes generating/failed; the section polls GET every 3s for up to 3 minutes with `credentials: "include"` + `cache: "no-store"` (middleware has no rate limit on this path). Publish unpublishes earlier published rows for the same mba+version. Published `body_markdown` opens `generate_performance_report` `execSummary` and the Review & Report commentary slide. Do not SELECT until 0079+0082 are applied (C-76). Tests: `npm run test:campaign-read`. |
| `lib/creative/getPrivateBlob.ts` | Shared by download routes: creative, MI specs, performance reports, AR/AP invoice PDFs, published plan-version files (`GET /api/mediaplans/[id]/download` via `servePlanFile`) |
| `POST /api/mediaplans/versions/[id]/documents` / `POST …/documents/regenerate` / `lib/docs/storePlanVersionDocuments.ts` / `regeneratePlanVersionDocuments.ts` / `renderPlanVersionDocuments.ts` / `buildMediaItemsFromPersisted.ts` / `explodeExcelLineItems.ts` / `filterMediaItemsForMbaScope.ts` / `mbaDataFromExplode.ts` / `planDocumentBlob.ts` / `lib/docs/planVersionFiles.ts` / `lib/docs/publishDocumentsAfterCommit.ts` | Publish-after file attach **or** admin regenerate from persisted rows. Live publish calls `runPublishDocumentsBestEffort` after the save txn (beside `markRunItemsStaleOnPublish`); failure is named on `/api/plans/save` `documents` and the Generate documents modal step — never rolls back. Multipart contract is `lib/api.ts` `uploadMediaPlanVersionDocuments` (do not rename fields). Writes private Blob `plans/{mba}/v{n}/{kind}/{filename}` + jsonb on `mba_pdf_file` / `media_plan_file` / `aa_media_plan_file` only — never `published_at` / `published_by` / `approved_slice` / line items, never `assertVersionMutable`. **`savePlan.ts` draft overwrite must not SET those columns to NULL** when `/api/plans/save` omits `mediaPlanFile` / `mbaPdfFile` / `aaMediaPlanFile` (DOC-1b; Drizzle omits `undefined`). Parser: Blob `url`/`uploadedAt` (`source` vercel-blob or regenerated), Xano `path`. Adapter hydrates via `readMbaPlanDetailFromPostgres` + edit-page explode (`shouldIncludeMediaPlanLineItem`). Empty `mba_fee_snapshots` overlays Excel fee/adserving from `legacy_schedules` billing blob (`legacyBillingTotals.ts`) — snapshot rates still win. MBA `NO_FEE_BASIS` on regenerate/`--out` uses explode adapter (`generatedFrom: "explode"`); live generate stays 422. jsonb `generatedFrom` is per kind. `--out` disk-only regenerate: `backfillPlanDocumentsArgs.ts`. Compare: `comparePlanWorkbooks.ts`. Tests: `npm run test:campaign-documents`. Backfill: `npm run docs:backfill` selects post-cutover published versions (`published_at >= 2026-08-01` AND `mba_pdf_file IS NULL`), not the master pointer. `regeneratePlanVersionDocuments` gates on `isVersionPublished` only. `--apply` writes Blob + jsonb for missing kinds (skip-if-present unless `--force`). |
| `components/mediaplans/SplitActionButton.tsx` / `PlanWizardBottomBar.tsx` | Wizard Publish/Save bar **and** `CampaignRowActions` (Home, `/mediaplans`, client hub). `PlanWizardBottomBar` is the create+edit floating bar (Publish, Save draft, then `wizardDownloadControls`: create/unpublished = Download draft MBA / Media Plan; published+dirty = Draft vs Published (vN); published+clean = unlabelled published files; zip is Publish & download all on the Publish caret). Draft buttons are outline (`bg-background`), never `bg-primary`. Create Save draft is always disabled with `CREATE_SAVE_DRAFT_DISABLED_REASON` (C-118 / C-121); `NEXT_PUBLIC_PLAN_DRAFTS` off still hides it via `showPlanDraftSaveButton({ enabled: planDraft.enabled })`. `hideCaret` / `menuOnly` are additive — do not break the wizard caret + primary. `menuOnly` is the outline Button itself (variant border + flat hover fill; no lift/shadow, no chrome wrapper). Default `menuSide` is `top`; `bottom` sets `avoidCollisions={false}` so the menu never flips up. `hasPublishedVersion: false` disables Download before any documents fetch. Open menu order is Edit media plan · vN then View campaign (`canEdit` false → View only). |
| `lib/specs/resolve.ts` / `buildMiWorkbook.ts` / `templateRegistry.ts` / `deriveMaterialDeadlines.ts` / `parseSupplyDeadline.ts` / `structuredDeadlineFromSpecColumns.ts` / `planDeadlineLines.ts` | MI SPECS mapping + Cover Overall Supply Deadline + SPECS Supply Deadline append. Civic Outdoor uses catalogue + `universal_specs` (not `formats[]`). Do not invent values; absent keys stay `NEEDS_SPEC`. Skip (`SKIP_ANSWER`) closes an open MI question without filling a spec. Required = fills-today. Never edit vendored `lib/specs/mi-library/`. Deadline derivations read structured min/max/business only (prose-only contributes nothing). Store is `0041_publisher_specs` (explicit `publishers.id` join; applied, 20-row seed verified). Readers prefer `publisher_specs` columns; fall back to vendored prose parse when the row is missing or columns are NULL. Tests: `lib/specs/__tests__/{resolve,buildMiWorkbook,templateRegistry,parseSupplyDeadline,sydneyBusinessDays,deriveMaterialDeadlines,publisherSpecsJoinSeed,publisherSpecsDeadlineSeed,structuredDeadlineFromSpecColumns,miHttpVersionScope,miResolution,parsePdfText}.test.ts` + `pdfParse.cjs.test.cjs` (live pdf-parse; pass Uint8Array, never Node Buffer) |
| `GET/POST /api/mediaplans/mba/[mba_number]/material-instructions` | Admin-only MI export. **Refuses silent MBA-wide** — requires `versionNumber` (published watermark) or explicit `mbaWide: true`. Callers: TraffickingBuilder, CopyChatPanel, SearchCopyChatPanel. Persist answers on `media_plan_versions.mi_resolution` (`0044`, applied) when version-scoped. |
| `db/migrations/0044_mi_resolution.sql` / `media_plan_versions.mi_resolution` | Persisted MI interview answers (`{ answers, updatedAt, updatedBy }`) — never a full `MiResolveResult`. Applied. CX2-9 is **0043** (`meeting_attribution_targets`). |
| `lib/specs/parsePdfText.ts` / `recordSpecPdfRun.ts` / `spec_runs.extracted` | pdf-parse reuse for spec-upload text; write path inserts `spec_runs`. Full AVA chat-upload UI is later. |
| `db/migrations/0041_publisher_specs.sql` / `db/schema/publisherSpecs.ts` / `spec_runs` | MI specs store + ingest-analogue run history. Join is explicit `publishers.id` (never fuzzy). SCA/SEN not seeded. id 61 not a join target. `spec_json` defaults `{}` — do not dump vendored JSON. RLS on; no `ava_readonly` grant. Applied (20 rows verified). Deadline columns seeded by `0046_publisher_specs_deadline_seed.sql` (AUTHOR ONLY, marker `0046_deadline_seed`, not drizzle-kit). |
| `db/migrations/0042_spec_deadline_overrides.sql` / `db/schema/specDeadlineOverrides.ts` | Manual material-deadline overrides (who / when / value). Never infer from drift. RLS on; no `ava_readonly` grant. Applied. CX2-9 is **0043** (applied). |
| `db/migrations/0043_meeting_attribution_targets.sql` / `db/schema/meetingAttribution.ts` | `client_notes.attributed_type` + `publisher_id`; `publisher_domains`; `meeting_title_rules`. Applied (51 client / 18 internal / 28 queue). Never seed publisher domains. RLS on; no `ava_readonly` grant. publishers.id 61 excluded from Assign. |
| `lib/codex/seedTasks.ts` | Campaign seed due dates. "Apply naming conventions" / "Traffic creative and QA" derive from published `line_items` via `masters.published_version_id` (never max(versions)) when structured deadlines exist; otherwise fixed offsets. Tests: `lib/codex/__tests__/seedTasks.test.ts` |
| `lib/codex/{estimateParse,clientMbas,queryHelpers,taskDetailWidth,quickAddParse,helpRoster}.ts` / `components/tasks/{TaskMbaSelect,TaskDetailSlideOver,TasksFilterBar,TaskEstimateChip,TaskBoard,TaskAskHelpDialog,TaskDetailClient}.tsx` / `app/tasks/TasksPageClient.tsx` | Codex list/board/detail UX. MBA labels are `<mba> — <campaign>`; combobox `preserveOrder` keeps MBA desc. Inbox GET is paginated by meeting; dismiss-all is one UPDATE of the note’s full proposed set (`POST /api/codex/proposals/dismiss-all`). `estimated_minutes` (0048 AUTHOR ONLY) is planner estimate only — not MyHours matching. Ask-for-help (`POST /api/codex/tasks/[id]/help`, 0078 AUTHOR ONLY) creates a child on the helper’s mine scope. Filter state is URL params (board is the omitted default); last layout is `codex.tasksLayout` in localStorage. Board is full-width, list stays contained. |
| `lib/codex/{auth0RosterSync,rosterEligibility,auth0LoginUpsert,rosterAliasGuard}.ts` / `POST /api/admin/auth0-roster-sync` / cron `GET|POST /api/cron/auth0-roster-sync` | Layer 2 Management API roster eligibility: `getUserRoles({app_metadata})` admin and not free-mail, **or** no client role and email domain exactly `assembledmedia.com.au`. Free-mail and client-role users are never roster-eligible. Does not write roles to Auth0. Does not deactivate. Login upsert (Layer 1) stays `getUserRoles` on the session. Changing `FREE_MAIL_DOMAINS` changes who Layer 2 skips. A new `email_aliases` value that another **active** roster member already holds is refused (API 409 names the other holder); Auth0 create drops the generated `first@` alias instead of colliding. `GET /api/codex/team` includes `alias_collisions` (report-only). Login and sync never overwrite `roster_source` on an existing row. |
| `MaterialDeadlinesStrip.tsx` / `GET\|POST /api/mediaplans/mba/[mba_number]/material-deadlines` / `deadlineOverrides.ts` | Campaign "Material deadlines" strip — unmounted (no page import). GET tenant via `checkClientMbaAccess`; POST admin + tenant. Renders nothing without structured dates; urgent within 5 Sydney business days or past (`text-status-critical-fg`). Missing `spec_deadline_overrides` table fail-softs to []. |
| `lib/naming/templates.ts` | THE law for composed names + trafficking Excel; changes break round-trip parse/validate + 13 tests |

### Shared core
| Touch | Also check |
|---|---|
| Env-key fallback chains in `lib/api/xano.ts` / `xanoClients.ts` | Order matters; `XANO_MEDIA_PLANS_BASE_URL` vs `XANO_MEDIAPLANS_BASE_URL` are live aliases; `XANO_BASE_URL` doubles as the assistant endpoint — see shared-core module page |
| `POST /api/plans/save` / `plansSaveBodySchema.ts` / `missingBuyTypeGate.ts` / `buildPostgresSavePayload.ts` / `savePlan.ts` | Publish (`mode === "publish"`) 422 `MISSING_BUY_TYPE` after parse, before `savePlanVersion`. Optional `mbaScope` (`lineItemIds` / `monthYears`, both nullable) rides the body; `selectedMonthYears` is deprecated and mapped when `mbaScope` is absent. Draft/`new_version` not gated. Production channel may be null (C-91). Empty string trim-to-null at schema + client payload. Twin pages toast + Issues panel. Existing published empties (C-120) untouched. Tests: `npm run test:postgres-save-mode`, `test:save-plan` |
| `DATA_BACKEND` / `WRITE_BACKEND` / `DATA_BACKEND_PLAN_DETAIL` / `lib/data/readReferenceMediaDetail.ts` / `readPublishers.ts` / `readClients.ts` / `writeClients.ts` / `writePublishers.ts` / `writeReferenceMediaDetail.ts` / `writeMediaContainerBestPractice.ts` / `writeBillingOverrides.ts` / `writeBillingSchedule.ts` / `writeFinance.ts` / `readKpi.ts` / `readFinance.ts` / `readPacing.ts` / `readMediaPlans.ts` / `readMbaPlanDetail.ts` / `readApprovals.ts` / `writeApprovals.ts` / `savePlan.ts` / `mirrorToXano.ts` / `WriteBackendContext.tsx` / create+edit layouts / `lib/finance/scheduleMonthsSource.ts` | Shadow must never change response body; `postgres` needs ETL-loaded Supabase; `/api/admin/migration-diffs` is process-local; per-domain env `DATA_BACKEND_PUBLISHERS` / `DATA_BACKEND_CLIENTS` / `DATA_BACKEND_KPI` / `DATA_BACKEND_FINANCE` / `DATA_BACKEND_PACING` / `DATA_BACKEND_PLANS` / `DATA_BACKEND_APPROVALS`; `DATA_BACKEND_PLAN_DETAIL` (default `postgres`, no global fallback) gates MBA GET only — `postgres` → `readMbaPlanDetail` (500 `PLAN_DETAIL_POSTGRES_FAILED`); `xano` → 410 `PLAN_DETAIL_XANO_GONE`; clients writes PG-first via `writeClients` (mirror `xano_client_mirror_failed`); reference writes (X4) PG-first via `writePublishers` / `writeReferenceMediaDetail` / `writeMediaContainerBestPractice` (mirrors until T7; bust publishers/BP/`xanoReferenceCache` + browser 24h media-details TTL on create); `mba_line_approvals` is postgres-authoritative (no Xano write mirror; ETL skips truncate-reload); finance schedule derive source `DATA_BACKEND_FINANCE_SCHEDULE` (`blob` / `shadow` / `rows`, default blob; domain `finance-schedule`; `?probe=finance-schedule`); finance admin summary splits `unexpected` vs `duplicate-class` (PG deduped / Xano duplicated); plans reassembles channel shapes from `line_items` (attrs spread + bursts); plans duplicate-class tags PENFOLD015/013/014 + BOSS001; `mba_line_approvals` absence = all-in (fail-soft); `XANO_LINE_ITEMS_SNAPSHOT` stays Xano until T6; MBA GET defaults to postgres (`DATA_BACKEND_PLAN_DETAIL`); `xano` → 410 (create/edit use `skipLineItems=true`); `WRITE_BACKEND` default `postgres` (explicit `xano` warns) — independent of `DATA_BACKEND`; when `postgres`, create/edit call `POST /api/plans/save`; T4b `mirrorPlanToXano` is gated by `XANO_MIRROR_ENABLED` (default off; `mirror: disabled`); `mirror: failed` never rolls back; admin retry `POST /api/admin/xano-mirror/retry` (409 `XANO_MIRROR_DISABLED` when off); **T0-1** `writeFinance.ts` owns app `finance_billing_records` writes (`invoice_key`, never `xero:`) |
| `app/api/media-details/[...path]/route.ts` | Allowlist + reference GET dual; POST creates PG-first + Xano mirror (X4) |
| `middleware.ts` / `components/AppSidebar.tsx` / `lib/nav/clientMenuItems.ts` / `contexts/AuthContext.tsx` | Auth-only for `/api/*`; `/api/cron/*` bypassed entirely (only `assertCronSecret`); client-role tenant confinement logic; Finance sections (FN7/FIN-1): `/finance` → invoicing + `?tab=` redirects; `/api/finance/sections/*` admin 403 fail-closed. Client sidebar is one dashboard item per `clientSlugs` (server `getUserClientSlugs`) then Creative then Knowledge Hub. Labels: `GET /api/dashboard/[slug]/name` — never `/api/clients`. Bottom nav caps clients at 3. Tests: `test:mba-number-alloc`, `test:tenant-isolation` |
| `app/api/cron/xero-sync` / `lib/xero/runSync.ts` / `lib/xero/stages/syncPdfs.ts` | Writes `xero_*` + `finance_billing_records` (`xero:` keys) + Blob PDFs + PC6 `match_run_items`; `xero_sync_log` INSERT is fail-open (run result still returned); resume watermark is the newest log row — a failed log write re-ingests from a stale If-Modified-Since. PDF pending is `pdf_file IS NULL OR NOT (pdf_file ? 'url')` (Xano empty stubs have no `url` key — never `IS NULL` alone); 429 backoff + per-row isolation (a PDF miss must not fail the stage). Batch default 50, `XERO_PDF_BATCH_SIZE` override. Xano task still active until T6 — dual writers; parity via `db:xero-parity`. **`db:etl` must not truncate-reload `xero_*` (C-70)** |
| `lib/xero/matchMba.ts` / `applyMatchMba.ts` / `stages/ingestInvoices.ts` / `syncLogNotes.ts` | Reference tokenizer separators include `()[]{}`; MBA then `scope_of_work.scope_id`; MBA wins; unique SOW match resolves existing open `xero_sync_exceptions` rows, does not write `mba_number`, and does not insert a new sync exception. Cron resume skips JSON `notes.source=pull-xero` via CASE-guarded extract — never unguarded `notes::jsonb` (C-71). Do not backfill historical rows from a matcher change. Do not change `threeTier.ts` |
| `lib/ops/health/checks.ts` | Ops-health cron (`/api/cron/ops-health`) email + JSON; Xero sync freshness reads `xero_sync_log.run_started_at` (green ≤36h / amber ≤7d / red older or empty) |
| `lib/xero/matcher/threeTier.ts` / `0011_xero_invoice_matches.sql` / `POST /api/finance/xero-match` / `GET …/xero-match/list` / sections `components/finance/sections/xero/*` | Extends T5 only; write-off admin+reason; `xero_contact_links` learn-forever; hit-rate feeds PC5 pre-run; sections expose accept/dispute/write-off (not reassign) |
| `GET/POST /api/finance/xero-queue` / `XeroExceptionsPanel` / `XeroPageClient` / `lib/finance/sections/xero/pendingIdentity.ts` / `enrichPendingFromXero.ts` / `lib/xero/contactLinks.ts` / `lib/xero/normalizeContact.ts` / `lib/xero/stages/importBillingRecords.ts` / `0054_seed_xero_contact_links.sql` | FIN-7 assignment UX; `assign_client` upserts `xero_contact_links` in the same PG txn as the billing client stamp; AR→client prefers links over name (`resolveClientFromContact`); header coverage is FY26 AR resolved-to-client. Do not change matcher or pending-edit rules. Do not guess ambiguous name matches. |
| `lib/finance/sections/costsQuery.ts` / `summaryQuery.ts` / `financeCampaignStatus.ts` / `scheduleLineJoinSql.ts` / `publisherIdentitySql.ts` / `GET /api/finance/sections/costs/summary` / `GET /api/finance/sections/summary` / `components/finance/sections/costs/*` | CP-3: media-only booked cost + **master** status `approved\|booked\|completed` (`FINANCE_STATUS_*_SQL` alias `m`); fee/adserving labelled separately; `coverage.excludedByStatusCents` / `orphanLineCents`; FN0 publisher CASE; version join is `PUBLISHED_VERSION_JOIN_SQL` (pointer AND stamp; `sql.raw` in Drizzle); line join must use `SCHEDULE_LINE_JOIN_SQL` (C-26); AP attribution name-heuristic only — unattributed never dropped; do not wire Costs UI to `global-monthly-*`; AP billed / Delta use `xero_ap_bills.sub_total` (ex-GST); overview Invoiced to date is `invoice_key LIKE 'xero:%'` only; Costs invoice PDF links `GET /api/finance/bills/[xeroInvoiceId]/pdf` (admin) — `pdfUrl` is the proxy path, never the private blob URL; `_debugSql` only when `?debug=1` |
| `lib/finance/sections/clientPaysQuery.ts` / `clientPaysCompose.ts` / `GET /api/finance/sections/costs/client-pays` / `/finance/costs/client-pays` / Costs overview `clientPaysExcludedCents` tile | CP-8: inverted CP-3 footprint (client-pays media only); complements payables headline; fee column omitted (C-27 — billing-schedule fee is a future slice); disclose C-29 line-detail limitation; dual-shape join required; version join is `PUBLISHED_VERSION_JOIN_SQL` |
| `lib/docs/buildMbaFromPersisted.ts` / `mbaRenderFilters.ts` / `deriveApprovedSliceFromSchedule.ts` / `POST /api/mba/generate` / `POST /api/mediaplans/versions/[id]/documents/regenerate` | MBA PDF. Header `Date:` is Melbourne generation day (`mbaHeaderDateLabel`), not campaign start; campaign dates stay on their own line. Optional `liveCampaignDates` prints the form pair (`datesUnsaved` is not drawn) and does not set `liveOverlay` or retarget the checksum. Partial MBA prints one `Scope:` line (`mbaScope.ts`) and `_partial` filename; full MBA is unchanged. Missing `approved_slice` is derived from billing `schedule_months` at read time (skip `__service__*` ); never write the derived slice. No snapshot + no fee-component rows + stored slice null → `NO_FEE_BASIS` (live 422; never fee = 0). Regenerated MBA on that code uses explode adapter (`mbaDataFromExplode.ts`). Optional live keys overlay render only (`X-Slice-Source: live`); checksum/footer stay on the frozen slice (generation date, live dates, and scope line are not in the hash). Extra body keys → `CLIENT_TOTALS_REJECTED`. No schedule rows → `MISSING_SLICE` skip, not a red save. Admin regenerate passes `now: published_at` and writes Blob jsonb only |
| `lib/docs/renderDraftDocuments.ts` / `POST /api/mediaplans/draft-documents` | Publish dry-run from the posted save body. Writes nothing (no Blob, no version, no `approved_slice`). Excel uses `explodeExcelLineItems` then `filterMediaItemsForMbaScope` (posted `mbaScope` wins over legacy `partialMba` chips). Draft-only extras (`clientAddress`, `partialMba`, `kpiRows`) are not on `plansSaveBodySchema`. Admin + `checkClientMbaAccess`. Header `X-Document-State: draft`. Tests: `lib/docs/__tests__/renderDraftDocuments.test.ts`, `app/api/mediaplans/draft-documents/__tests__/draft-documents.route.test.ts` |
| `lib/generateMBA.ts` (`draft`) | `MBAData.draft` defaults false. When true, every page draws diagonal "DRAFT - NOT FOR CLIENT" and header "DRAFT"; checksum footer is not drawn. Existing `/api/mba/generate` callers stay unchanged |
| `lib/generateMediaPlan.ts` (`draft`) | `GenerateMediaPlanOptions.draft` defaults false. When true, Media Plan and Campaign KPIs sheets get print header/footer plus a rotated DRAFT cell. Cover G4 campaign status is unchanged. Type-hub row above still applies |
| `components/finance/sections/costs/CostsAccrualsClient.tsx` / `useCostsAccrualData.ts` / `computeAccrualByClient` | Accrual section surface (hub panel deleted FN7); same billing+payables fetch path (M8 blob rule); Investment deep-links must pass client+month |
| `lib/finance/sections/investment/*` / `POST /api/finance/sections/investment/cut` / `components/finance/sections/investment/*` | FN3a billable composition; one basis only; LATERAL LIMIT 1 on line/publisher joins (no sm fanout); version join is `PUBLISHED_VERSION_JOIN_SQL`; fee/billable measures must surface `coverage.fee` (C-27); Actuals measures MBA×month grain only (`cutGrain.ts` / `cutArQuery.ts`) — refuse line dims with 422, never prorate; `coverage.ar` when Actuals requested; agency economics (`agencyEconomics.ts` / `agencyEconomicsAttach.ts`) — current-FY-only while C-27 holds, retainer via forecast mapping, `coverage.agency` caption, seeded presets via `?preset=`; channelGroup map sign-off in `channelGroups.ts`; Unmatched ≠ Costs Unspecified; classic Report tab untouched; `_debugSql` only when `?debug=1` |
| `lib/mediaplan/drafts/*` / `0012_plan_working_drafts.sql` / `0071_clear_plan_working_drafts.sql` / `GET\|PUT\|DELETE /api/plans/drafts` / `GET\|POST /api/plans/presence` / `0064_plan_presence.sql` / create+edit draft chrome / `hooks/usePlanDraftSession.ts` / `hooks/useDraftFieldDiff.ts` / `hooks/useStableHydration.ts` / `components/media-containers/ExpertCard.tsx` | Stage 2b working-draft persistence + offer always on (GET/PUT/DELETE ungated); identity is email else `sub` (`draftIdentity`) — missing both is 401 and no write (never `"unknown"`). GET returns `identity: { source, id }`. `NEXT_PUBLIC_PLAN_DRAFTS` gates autosave chrome only (default off; local may be on — not Vercel from Cursor). **Flag off retains rows** — offer + save-on-published stay reachable. **0071 AUTHOR ONLY** deletes eight live rows at go-live (no `campaign_status` update) and keeps STRMEA001 (267) and glenda009 (10012553); missing draft is `{ draft: null }`, not an error. IndexedDB `av-plan-drafts` is not cleared by 0071. Autosave timers re-arm on already-dirty editRevision emits via subscribeDirty (boolean snapshot stays sticky so create/edit do not re-render per keystroke). Create chip headline is live, not stored on activeDraft. Create persistServer is IndexedDB-only until a master exists (C-118). Pill always from `describePlanSavePill`; edit wizard header trail from `describeVersionHeaderTrail(planDraft.modeResolved)` in `PlanWizardVersionChrome` (same resolver — unpublished tip never shows "Next: vN"); stale-base publish 409 when `tipVersionIdAtLoad` is set and the published pointer has moved (`baseVersionId` is the fork source, not in the guard); Discard deletes the row; edit View changes is a read-only `diffLive` dialog (`PlanDraftFieldDiffDialog`) that diffs `formValues` as `campaignChanges` then groups line edits by channel/`lineItemLabel()` with `fieldLabels.ts` (never raw paths); must not apply/discard/reset; create disables View changes (no published tip); docs/pacing keep serving published tip. **Matching-base drafts auto-apply** after `allChannelsHydrated` via `buildDraftChannelApply` (both edit bags) except a local-only leftover (`isOrphanLocalDraft` — server `{ draft: null }`) which offers Apply/Discard; stale-base keeps Load anyway — never silent overlay of a newer tip. **Create-page local drafts** (`masterId` null) need meaningful content or they are deleted on open (empty / ≥14 days, no banner). Live field highlights vs captured tip snapshot (`DraftDiffProvider` / ExpertCard). Empty channel arrays are deleted-line; late tip hydration must not overwrite a restored draft. **Presence (SM-7):** `plan_presence` heartbeat via GET/POST `/api/plans/presence` (same identity helper; no identity → 401). Not a lock — never blocks Save/Publish. GET others = `last_seen_at` within 90s. Create has no master, so no presence. Do not SELECT `plan_presence` until 0064 is applied (C-76). |
| Any cache module | Two independent clients caches (10min vs 30s) with separate invalidation; publishers + mediaContainerBestPractice caches invalidate on X4 writes; remaining coalesced caches may still lack invalidation; caches are per-lambda on Vercel |
| `lib/m365/graphClient.ts` / `NEXT_PUBLIC_M365_PROVISIONING` / `0021_m365_provisioning_log.sql` | Flag default off; all Graph HTTP behind injectable transport; every attempt → `m365_provisioning_log`. Entra permissions in `docs/brain/modules/m365.md`. Do not call Graph without the flag + Luke's app registration |
| `lib/m365/reconciliation.ts` / `app/admin/m365-reconciliation` | Read-only admin join of clients + unmatched plan MBAs. Changing prefix-match rules changes which masters appear as TI-1 §3a junk. No writes. |
| `lib/fireflies/{sync,runSync,timeEntryDrafts,attribution,assign,assignSubmit,assignTargets,learnableDomains,titleClients,rosterAliases,lookback,actionItemBlocks,autoCreate,proposalRepo}.ts` / `lib/myhours/proposalRepo.ts` / `ava_time_entry_proposals` / `POST /api/admin/fireflies-sync` / `FirefliesSyncNowButton` / `GET|POST /api/admin/fireflies-unattributed` / `GET /api/clients/[id]/meetings` | A successful client-attributed Fireflies note fans out to roster-attendee time drafts (one per person after alias dedupe) and unique-roster action-item tasks (`auto_created` + `ava_auto_key`). Re-sync of an existing eligible `client_notes` row repeats time-draft upsert and action-item processing (idempotent — no duplicate tasks). Eligibility is non-internal + client id + attendee email intersecting active `team_members` (canonical or `email_aliases`); meeting date is Sydney civil date. An alias that matches more than one active roster row is not attributed — auto-create becomes an Inbox proposal naming both holders, and no time draft is written (log names both). Upsert refreshes proposed/blocked rows only — confirmed/skipped decisions are terminal. Auto-create requires a unique block-name match; Unassigned/ambiguous stay Inbox proposals; non-roster names stay on the note. Attribution order: client title → client domain → publisher domain → title rules → roster-internal → queue. **CLIENT BEATS PUBLISHER.** Publisher / internal / new_business notes never create tasks or time drafts. Changing `client_name_aliases`, grouping, `publisher_domains`, or `meeting_title_rules` changes which notes land in the unattributed queue. Admin Sync now (`requireAdmin`) shares `runFirefliesSyncToPostgres` with cron (`CRON_SECRET`); first-sync lookback default 60 days. Auto-created Dismiss is a training signal (`assignment_rules`). **Assign is per-note** (never cascade-writes other rows; `would_reattribute` is report-only). Publisher Hub Meetings is read-only `GET /api/publishers/[publisherId]/meetings` (`requireAdmin`) over `client_notes` (`publisher_id` + `attributed_type=publisher`). Client Hub Meetings is `GET /api/clients/[id]/meetings` (`requireAdmin`) over `client_id`. publishers.id 61 is excluded from the Assign dropdown. |
| `lib/mediaplans/ingest/*` / `lib/mediaplans/ingest/ingestEval.ts` / `scripts/ingest-eval.ts` / `tests/fixtures/ingest-golden/` / `app/admin/schedule-ingest` / `app/mediaplans/mba/[mba_number]/ingest/[stageId]` / `POST /api/admin/ingest/{review,remap,accept,ava-mapping,cancel,link-publisher,confirm-profile,parse-review}` / `GET /api/admin/ingest/stage/[stageId]` / `GET /api/publishers/[publisherId]/ingest` / `GET /api/cron/ingest-eval` / `0023_line_item_panels` / `0027_line_item_panel_flights` / `0024_publisher_profiles` / `0033_publisher_profiles_sen` / `0034_publisher_profiles_money_map` / `0035_publisher_profiles_reference_ignore` / `0036_publisher_profiles_publisher_id` / `0037_ingest_runs` / `0038_publisher_profiles_sca_drop_days` / `0049_publisher_profiles_line_granularity` / `0050_ingest_stages` / `0055_line_item_panels_unique` / `0059_publisher_profile_audit` / `0060_publisher_value_synonyms` / `0061_publisher_profile_field_defaults` / `0062_publisher_profiles_jcd_bought_rate` / `0066_publisher_profiles_money_rules` / `0067_ingest_eval_runs` / `0068_ingest_stages_source_file` / `lib/mediaplans/ingest/seeds/targetTemplates.json` | Human gate for publisher schedules. Completeness is template-first (OOH 6 / radio 7 required); leftover file columns are not used (not AVA). Independent line audit runs in `stageIngestReviewFromBuffer` after the proposal (`review.line_audit` jsonb; `lineAudit.server.ts`; never profile rules; `INGEST_AUDIT=off` skips; `POST /api/admin/ingest/review` `maxDuration` 300). Chat mapping cards keep AVA proposals only for unmatched `card_field_ids` canonicals; unused leftovers stay named in ignored/not_used; unmatched non-card enrich (lat/long/village…) is one named “stay empty” line, never a card. Review Section A is `buildReviewCardSurface` over `card_field_ids` (editor card + money + dates); OOH `detail_table` is one expandable “Line detail — N of M matched” row (each line IS its source row); `money_detail_ids` (charges, lunar) nest under media money. Header states detected media type (OOH or Radio) as detected, ambiguous, or unknown. Accept → `executeIngestAccept` → `savePlanVersion` then panel+flight insert in one txn (never bypass). AVA chat attach uses the same review path (`stageIngestReviewFromBuffer`, `get_pending_ingest_review`); create/edit then `load_ingest_into_form` (form, not saved — money + discrepancy + required-field + unresolved-value + parse-review confirm/exclude gates run at load; money fail records `ingest_runs` blocked, required-field, unresolved-value, discrepancy, and unconfirmed-row fail do not); human Save on those pages completes the stage via `POST /api/plans/save` → `completeStagedIngestAfterSave` (panels keyed to this save's line ids, not `ingestform`; missing stage never fails the save; panel insert fails loud on the 200; `ingest_runs`/retain fail open; spent `retained_at` is a no-op). Hub still `accept_ingest_proposal` until W-3. Unknown/absent route omits the writer (fail closed). Confirm-then-ask uses existing `AvaToolResult.questions` cards (never a second question UI); `get_pending_ingest_review` echoes the parity report (not the old “Reviewed it” table); unparsed leftover rows are named labels not a bare count; MBA is a candidate-campaign card when page context has none — never guessed; unknown publisher is a no-profile reply + blocked `ingest_runs` row. `GET …/ingest/stage/[stageId]` + `/admin/schedule-ingest?stage=` deep-link the same staged package (`ingest_stages` / 0050 AUTHOR ONLY, overlay until applied; sessionStorage is a same-tab cache). Successful accept retains the package (`expires_at` NULL). Cancelled/blocked keep the 24h TTL; daily ops-health sweeps expired non-retained rows. Stamp writes `attrs.network` from publisher, OOH `attrs.format` from `template_coverage.resolved_controlled` then `resolveControlledFormat` (`expertOohFuzzyMatch` via `controlledVocabularies`; Fuse score ≤ 0.6 plus ingest token-overlap so RAIL is not retail; publisher-name prefix strip; publisher synonyms auto-apply at review-build, global synonyms are suggestions only), never publisher prose; unresolved format stays `null` (never `""`) with the raw on `attrs.format_unresolved_raw` and `attrs.publisher_format_name` / `line_item_panels.publisher_format_name`; buy type is per-line from `buy_type`/`buyType` via `resolved_controlled` then `resolveControlledBuyType` — `buyTypeForMedia` (`fixed_cost` OOH / `spots` radio) is the fallback when the file has nothing, not the rule; sourced-but-unresolvable buy type rides `attrs.buyType_unresolved_raw` and is not replaced with the media default; `evaluateRequiredFieldGate` also walks `unresolved_controlled` (a waived field still gates on its values); load/accept refuse naming the raw; bursts via `serializeBurstsJson` (string budget, `buyType`-sensitive). `ingest:value:<field>:<raw>` cards ask which AV option a sourced value is (not which column feeds the field); options come from the field's registered vocabulary; confirm writes `learnSynonym` (`publisher_value_synonyms` / `0060` AUTHOR ONLY). Global-tier hits pre-select as a suggestion and still require Confirm. OOH hydrate is `hydrateOohEditorLine` (`OOHContainer`); empty `bursts[]` falls through to `bursts_json` in `resolveLineItemBursts`. Remap is `remapIngestColumn` (Hub + review share it), keyed by `publisher_name` while 1:1 holds: `POST /api/admin/ingest/remap` requires `knownHeaders` (sheet headers from `review.column_mapping`; 400 when absent) except a field-default clear (`header` `__constant__:<fieldId>` / `persistFieldDefault`, no knownHeaders); invented headers return 200 `{ ok: false }` and write nothing. Chat **Leave unmapped** / **Not in this file** / Skip decline (record, write nothing); **Remove this mapping (currently …)** is the only chat delete. Accepted writes attribute `updated_by` + `publisher_profile_changes` (`0059` AUTHOR ONLY; same txn; identity from session email, never `system`). Unknown-publisher first upload proposes a profile (never guesses the catalogue); Hub catalogue pick stamps the draft; `POST confirm-profile` inserts then re-parses from `source_file` (no re-upload). Unconfirmed drafts never load. `ingest_runs` writes on accept / cancel / blocked (409 recon, required-field, or unresolved value); chat unknown-publisher also records blocked. Hub ingest section at `/publishers/[id]`; `/admin/publisher-profiles` redirects to `/publishers`. Proposal never mints `line_item_id`. **Row-per-line:** `publisher_profiles.line_granularity` default `per_row` — each classified buy row is one line (QMS Paid 41, JCD 95 buy rows with identity or legend status, SCA one line per station row with spots); `grouped` collapse-by-`grouping_keys` is config-only (no seeded profile uses it). When a flight grid exists, occupancy is the candidate set and `retainBuyDataRows` keeps a row only with panel/grouping identity or a legend-status grid cell so MEDIA VALUE / SUMMARY / INVESTMENT leftovers are unparsed, not lines. **Money:** typed `column_map` targets (`media_rate:*`, `media_amount:stated`, `charge:*`); line media follows `publisher_profiles.money_rules` (`0066` AUTHOR ONLY, not `sheet_rules`): `line_total` = bought/stated cell once split across PAID weeks (JCD MEDIA BOUGHT RATE; SCA Client Total); `weekly_rate` / `lunar_rate` multiply by PAID weeks only (QMS weekly). Bonus/STA never $ and never merge into a paid burst. `file_stated_total` is the labelled stated cell (JCD `TOTAL MEDIA INVESTMENT (ex. P&I)`); never a silent column sum — missing cell falls back to column sum with a visible warning (SCA). MEDIA VALUE is rate-card info (Σ + discount), never the gate. `media_rate:bought` still stamps `attrs.unitRate`. Derived-vs-line-media >0.5% → warning only; file-total or section-subtotal Δ>0.5% blocks Accept (409). Required template fields unmatched without a waiver also 409. Unresolved controlled values also 409 even when the field is waived. Unresolved line-audit discrepancies also 409 until Parser / Audit / typed is recorded on `line_audit.resolutions`. Unconfirmed proposed rows also 409 until every row is confirmed or excluded (`parse_review`). `reference:ignore` is acknowledged (not unmapped / not AVA / not imported). Leftover samples are deduped by header across line-item sheets before the AVA gate. Mapping rows key by sheet+header+index. Line descriptors collapsed by default in review UI (flight summary per line). AVA mapping proposals only when a REQUIRED template field is unmatched and leftover headers exist — client POSTs leftover samples + unmatched required to `/api/admin/ingest/ava-mapping` (`avaColumnMapping.server.ts`); `avaColumnMapping.ts` is client-safe — never auto-accept; never parses values/bursts/grid_semantics. Remap / synonym / stage / `listPublisherProfiles` writers are `*.server.ts` siblings (`import "server-only"`); `await import("@/db")` does not keep a module out of the client bundle. `AVA_MAPPING_TARGET_DESCRIPTORS` includes `buy_type`, typed money (`media_rate:*`, `media_amount:stated`, `charge:*`), and `reference:ignore` so `parseToolProposals` does not discard them; a rate column and a stated-total column are different targets; `reference:ignore` is acknowledged-not-imported, not unmapped. Radio duration copies `length` onto `attrs.duration`. Seeds: QMS, SCA, JCDecaux, SEN (JSON + migration must agree). **OOH panel-line count > `OOH_PANEL_LINE_EXPERT_THRESHOLD` (30)** → Accept sets `preferOohExpertView`; `OOHContainer` suppresses card list (summary + open expert). A 95-line JCD accept therefore renders in expert view, not 118 standard cards. Flights: no money; dark letters = no row. Hub spend matcher is `lineItemMatchesPublisher` (empty/whitespace `header1` is absent; short stamps join via `publisher_profiles.publisher_id` / 0036). |
| `lib/api.ts` module scope | Throws at import server-side if publisher/client base URLs unset; imported by client components so no Node-only deps (reference reads use dynamic `import(/* webpackIgnore: true */)` of server-only modules — without `webpackIgnore`, `next build` fails on create/edit). `check:client-server-only` allowlists those four `lib/api.ts` webpackIgnore specs; a new webpackIgnore of a server-only module fails the gate |

## Duplication map (fix must be applied N times)

- Create page ↔ edit page (near-parallel mega-pages)
- 14 bespoke containers ↔ 6 `MediaChannelContainer`-based (half-finished refactor); 9 local `computeLoadedDeliverables` copies with a rounding split (5 rounded / 4 raw)
- Pacing composers: `lineItemStatusFromPacing` ×3 thin wrappers over `lib/pacing/status.ts` `pacingStatus()`; KPI-target block ×3; `resolveLive*LineItems` ×3; per-channel `LineItemPacingTable.tsx` ~1.2k lines each, ~90% identical (Status cells + scroll height now shared via `status.ts` / `pacingTableScroll.ts`); page clients share `ChannelPacingBoard` / `LinePacingCard` but still own fetch + filter; sticky-column freeze still diverges (search densest)
- Channel-key naming: 4 conventions coexist (`progBVOD`/`progBvod`/`digiDisplay`/`mp_*`) — alias tables `FANOUT_LINE_ITEM_MAP_ALIASES` and `MEDIA_TYPE_ALIASES` paper over it
- Two Snowflake read stacks with different semantics: `pacing-fact`/`search-campaigns-pacing` (pacing pages) vs `pacing-service`/`search-pacing-service` (dashboard, `/api/pacing/bulk`) — same MBA can show different numbers on /pacing vs /dashboard. Bulk non-social WHERE is `bulkNonSocialChannelWhere` (programmatic display + video + OOH + ad serving).
- Two clients caches; duplicate 194-line toast hook (`hooks/use-toast` vs `components/ui/use-toast` — separate module state); three Melbourne-date helpers; two Auth0 client instances (`lib/utils/auth.ts` vs `lib/auth0.ts`); two saved-view stores (localStorage + Xano)
- **Two media-plan list caches** — `lib/api/mediaPlansListCache.ts` (`GET /api/mediaplans`, Campaigns) ↔ `lib/api/mediaPlanVersionsCache.ts` (`GET /api/media_plans`, dashboard). Master-owned list fields (`mp_client_name`, plus `published_version_id` when present on the master) overlay via shared `lib/api/overlayMasterOwnedListFields.ts`. Fixing only one path is DI-9 / DI-9b class drift. In-memory MBA maps join on `mbaJoinKey` (trim + lowercase) — as-stored equality silently drops mixed-case masters (C-77).
