# Module: Media Plans

The campaign builder. Creates an MBA-numbered plan (`media_plan_master`), cuts immutable version rows (`media_plan_versions`) per save, and writes per-channel line items into 20 Xano tables (`media_plan_television` … `media_plan_influencers`), each row carrying a `bursts_json` array. Generates the MBA PDF + media plan Excel and uploads them to the version row.

## Key files

- `app/mediaplans/create/page.tsx` (~8,100 lines) — new-plan builder. **Near-parallel twin of the edit page**; fixes usually apply to both. Required campaign fields + media-channel toggles use `components/ui/field.tsx` (label association, real `required`/`aria-required`, error wiring). `components/ui/switch.tsx` requires `aria-label` or `aria-labelledby` at the type level.
- `app/mediaplans/mba/[mba_number]/edit/page.tsx` (~11,800 lines) — canonical editor; `handleSaveAll` is the save protocol.
- `app/api/mediaplans/mba/[mba_number]/route.ts` — THE media plan API: GET (master + version + channel line items), PUT (version cut + server billing recompute), PATCH (publish). GET honors `DATA_BACKEND_PLAN_DETAIL` (default `postgres` → `lib/data/readMbaPlanDetail.ts`; `xano` → 410 `PLAN_DETAIL_XANO_GONE`; failure → 500 `PLAN_DETAIL_POSTGRES_FAILED`). Shared combine: `lib/mediaplan/mbaGetAssemble.ts`. GET/PUT/PATCH all call `checkClientMbaAccess` (SEC-3 Verified). Peer `campaigns/[mba_number]` (+ billing-schedule) retired to 410 (X3); still `checkClientMbaAccess` on the stub (O6 / SEC-10).
- `app/api/mediaplans/route.ts` — list: admin/manager see all; client users get MBA-scoped rows via `resolveClientMbaScope` (SEC-2 FIXED). `mediaplans/[id]/mbanumber` POST is `requireRole(admin)` (O6). Upstream join is `lib/api/mediaPlansListCache.ts`: latest-per-MBA versions + masters; merge overlays published `version_number` **and** master-owned `mp_client_name` via shared `lib/api/overlayMasterOwnedListFields.ts` so `DATA_BACKEND_PLANS=postgres` matches Xano `_latest` row shape (Postgres versions do not store client name).
- `lib/api/mediaPlanVersionsCache.ts` — dashboard `GET /api/media_plans` latest-per-MBA list (shared `_latest` walk with the Campaigns list cache). Postgres path collapses versions then overlays the same master-owned fields (`applyMasterOwnedOverlayByMba`); Xano `_latest` already has them inline. **Twin of `mediaPlansListCache`** — any master-owned list-field change must land in both (DI-9 / DI-9b).
- `app/mediaplans/page.tsx` list UI maps load/search through `ViewState` / `ViewStateBoundary` (error ≠ empty; search-zero → Clear filters). Stored `campaign_status` is display SoT (`lib/mediaplans/campaignListStatus.ts`); past end date is a separate `scheduleEnded` hint, never rewritten to Completed. Search syncs `?q=` via fail-closed `matchesMediaPlanSearch` → `lib/search/matchText` (token-prefix AND; `String(x ?? "")` so a numeric `mba_number` cannot throw); shows result count + clear. AU FY overlap filter (`?fy=` start-year / `all`, default current) is client-side via `lib/dates/auFinancialYear.ts` (shared with Home). Search input and result-count slots use fixed widths so Create Campaign does not shift on type/focus/clear; count stays blank/skeleton while `loading` (never flash "0 of 0"). Hero uses `MediaPlanEditorHero` with `actionsFloor="toolbar"` (content floor 780) so Layout + search + Create stay one wrapping actions row (AVU4-1 pe brand-mark clearance; not mid-header orphan). Status buckets (Booked/Approved/…) are the list chrome — no separate "In market now" lead panel.
- `app/api/media_plans/<channel>/route.ts` ×13 GET-only + `[...path]` catch-all — channel GETs dual via `createChannelLineItemsGetHandler` / `DATA_BACKEND_PLANS`. Dedicated channel POSTs and `television/[id]` retired (X2). Catch-all channel POST/PUT/DELETE → 410 under `WRITE_BACKEND=postgres` (use `/api/plans/save`); legacy Xano proxy remains when `WRITE_BACKEND=xano`. Allowlist: `lib/security/proxyAllowlist.ts`; catch-all `requireRole(["admin"])` (SEC-1).
- `lib/mediaplan/` — the extracted logic: `containerChannelConfig` (20 configs + fieldMap mapping), `expertGridChannelConfig`, `expertChannelMappings` (8.4k lines, standard↔expert per channel), `expertModeSwitch`, `schemas.ts` (all Zod channel schemas), `lineItemIds.ts` (**`line_item_id` = `<MBA><CODE><n>` — universal join key**), `serializeBurstsJson` + `formatBurstsForPersist` (**bursts_json write contract**), `publishedVersionGuard`, `publishVersionIntegrity`, `nextMbaVersionNumber`, `reapUnpublishedStagedVersions`, `burstAmounts.ts` (**canonical fee math**), `deliverableBudget.ts` (**canonical deliverable math**).
- `components/media-containers/` — `ExpertGrid.tsx` (5.2k lines, fully shared spreadsheet grid; all 21 wrappers are ~40-line adapters) + `MediaChannelContainer.tsx` (shared shell used by only 6 channels: ProgDisplay/Video/BVOD/Audio/OOH, Search) + 14 bespoke 1.1–1.9k-line containers.
- `lib/api.ts` — client data layer (`save<Channel>LineItems` ×20, fetch caches). Channel GETs honor `DATA_BACKEND_PLANS` via `fetchChannelLineItemsForMbaGet` → `lib/data/readMediaPlans.ts` (Postgres reassembles from `line_items`).
- `lib/generateMediaPlan.ts` (2.3k lines) — Excel workbook AND the `LineItem`/`MediaItems` type hub (32 importers).
- `lib/data/readMediaPlans.ts` — plans-domain shadow/postgres readers: masters, versions (`legacy_schedules` → top-level billing/delivery blobs + `channel_flags` → `mp_*`), per-channel line-item reassembly (typed commons + attrs zod spread + `bursts`/`bursts_json`). Row shaping uses `coerceNumericStringsToNumbers` with default `IDENTIFIER_TEXT_FIELDS` — `mba_number` (and other text identifiers) stay byte-identical strings (`"001001"` never becomes `1001`).
- `lib/data/savePlan.ts` — T4a Postgres transactional save (`savePlanVersion`): one Drizzle txn over `DATABASE_URL` (version upsert → replace-set `line_items` → server-compute `schedule_months` via `computeCampaignFinancials` + `_scheduleTransform.explodeScheduleToMonthRows` → `billing_overrides` → `source='override'` → `legacy_schedules` mirror → publish BOSS006 + freeze `approved_slice` + optional `SAVE_GATE_FULL_SCOPE` C1 gate + `mba_fee_snapshots`). Gated by `WRITE_BACKEND` (default `xano`); `POST /api/plans/save` returns 501 until flipped. **T4c:** create/edit layouts inject `WRITE_BACKEND` via `WriteBackendProvider`; when `postgres`, `handleSaveAll` / create version save call `/api/plans/save` once (modal: Save plan → Mirror to Xano → KPI sync). Xano fan-out path stays default. **T4a soak:** report-only `scripts/verify/byte-diff-schedules.ts` diffs enriched recompute vs persisted `legacy_schedules` month.lineItems (id remap: decorated / legacy descriptive / `__service__*`); override fixture in `lib/data/__tests__/savePlan.test.ts` asserts `schedule_months.source` flips `override` ↔ `computed`. **O4.6:** for `publish`/`new_version`, `version_number` is resolved inside the txn as `max(version_number)+1` for the master (client number ignored); draft-overwrite keeps the loaded version; response returns the resolved `versionNumber`.
- `lib/mediaplan/buildPostgresSavePayload.ts` — T4c line + identity assembly for `POST /api/plans/save`: channel snapshots → `SavePlanLineItem[]`, `postPlansSave` client, `resolveMasterIdFromCombinedPlan` (prefer `media_plan_master_id` over combined-payload version `id`), `dollarsToCampaignBudgetCents`.
- `lib/mediaplan/resolvePostgresSaveMode.ts` — shared draft/publish mode + versionNumber + footer/`formatSaveModeLabel` mapping (edit footer + PC7 pill must not duplicate rules). Lazy-empty `versionRowCount` while tip exists still increments (draft→booked on v1 → publish v2), never "Will create v1". Create/edit snapshots use `assignStableLineItemNumbers` (never positional `reassignLineItemNumbers` on save).
- `lib/mediaplan/__tests__/postgresSavePayload.integration.test.ts` — O3 pin suite (`npm run test:postgres-save-mode`): masterId krusty014 283≠1108; unloaded `versions[]` footer from same helpers; full status vocab + `MISSING_CAMPAIGN_STATUS` source pin; stable ids / reorder / 23505 disambiguation; cents 0/null/decimals; create+edit twin wiring.
- `lib/mediaplan/drafts/*` + `hooks/usePlanDraftSession.ts` + `components/mediaplan/PlanDraftChrome.tsx` — PC7 three save tiers (IndexedDB soft save, server `plan_working_drafts`, publish via unchanged `savePlanVersion`). Pill text shares `resolvePostgresSaveMode` (do not duplicate mode rules). Flag `NEXT_PUBLIC_PLAN_DRAFTS` (default off). Stale-base publish → 409 `STALE_BASE_VERSION` + compare (no merge). Pill effect memos `modeResolved` on primitive inputs; disabled path `setPill` is a no-op when already null (avoids max-update-depth while the flag is off).
- `lib/data/mirrorToXano.ts` — T4b best-effort Xano write-back after Postgres commit (non-authoritative). Uses existing `save*LineItems` + `replaceChannelLineItems` (insert-before-delete while mirroring; shared `LINE_ITEM_WRITE_CONCURRENCY=4` semaphore). **O4.6:** publish mirrors also `PATCH media_plan_master/{id}` with `{ version_number, campaign_status }` (legacy watermark bump); draft/`new_version` mirrors do not touch the master. Version payload always carries `legacy_schedules` blobs from `savePlanVersion` / Postgres (`mirrorInputFromSave` → `billingSchedule` + `deliverySchedule`). Failure logs to the shadow-diff ring buffer **and** a durable `app_notifications` row (`audience=admin`, `kind=xano_mirror_failed`, payload `{ mba, version, error, timestamp, retried }`); never rolls back Postgres / never throws into the save path. Admin retry: `POST /api/admin/xano-mirror/retry` — success sets `read_at` + `payload.retried=true`.

## Save protocol (verified)

**Xano path (current default, `WRITE_BACKEND=xano`):**

1. Compute `approvalSelectionFingerprint`; approval-set change → `forceIncrement`.
2. `PUT` with `deferMasterVersionPublish: true` → server: reap staged orphans → `nextMbaVersionNumber` → `recomputeAndValidateBillingScheduleOnSave` (authoritative; 409 on $0.01 divergence) → POST new version row (or PATCH in place while `status === "draft"` && !forceIncrement).
3. ~20 conditional per-channel `save*LineItems` writes. Any failure → abort, master NOT advanced.
4. Docs generated + uploaded in parallel. **Below approved** (draft/planned): MBA PDF + Media Plan file steps are marked `skipped` with “Documents generate from approved versions” (`lib/docs/saveDocSteps.ts`); SavingModal does not enter “Saving with Errors” for skips alone. PC3’s approved-or-beyond gate on `/api/mba/generate` stays; only the dialog classification changed. Real doc failures on approved+ remain `error`.
5. `PATCH` publish (advance `master.version_number`). Failure → Retry-publish button. Xano has no multi-table transactions — stage-then-publish is the closest contract; abandoned failures leave staged rows until the next save reaps them.

**Postgres path (`WRITE_BACKEND=postgres`, T4c wired):** `POST /api/plans/save` → `savePlanVersion` then best-effort `mirrorPlanToXano` — draft updates `(master_id, version_number)` in place; `new_version`/`publish` insert at server-resolved tip+1 (O4.6; client `versionNumber` ignored for those modes); line ids are client-stable (never regenerated); publish aborts if `count(line_items)=0` (BOSS006); mid-txn failure rolls back version + lines + schedules (PENFOLD016 kill-shot). Editor sends `masterId` from `media_plan_master_id` (not combined-payload version `id`); mismatch vs mba → 422 `MASTER_ID_MISMATCH` (no silent remap). Publish carries mapped `campaignStatus` (lowercase: draft/planned/approved/booked/completed/cancelled) — no `"Approved"` invent. 23505 on versions UNIQUE → `VERSION_ALREADY_EXISTS` (not line-id dupe) — backstop only once tip is server-resolved. Mirror failure leaves Postgres intact and returns `{ mirror: "failed" }` (retry via admin); publish mirror also PATCHes Xano master watermark. Create/edit SavingModal steps collapse to transactional save → mirror → KPI sync. **O4:** AUTO-line / month-header drift vs the burst `autoReference` preview does **not** pre-block save (server recomputes `schedule_months` + blob anyway); response may include `billingCorrection` for a visible toast. Explicit `billingMode=manual` + C2 sum violations still 409. Xano MBA PUT still uses `recomputeAndValidate` reject-on-AUTO-divergence until T6. **O4.5:** create/edit postgres bodies go through `assemblePlansSaveRequestBody` — `feeLoading` / `feeSnapshot` / `adservaudio` always ride the shared assembler (no per-branch fee wiring). `buildSavePlanLineItemsFromSnapshots` prefers stamped snapshot `feePct` over billing-input meta. `savePlanVersion` logs `[savePlan-fee-zero]` (never blocks) when any line resolves a non-zero feePct but `mbaScopeTotals.fee` is $0. **PC7 working drafts** (`plan_working_drafts`, flag `NEXT_PUBLIC_PLAN_DRAFTS`) are orthogonal: autosave never writes a version; publish optionally sends `baseVersionId` and 409s if the tip moved.

### Full MBA GET `!skipLineItems` (C-22 / X2)

Create/edit load with `skipLineItems=true` and hydrate channels via `DATA_BACKEND_PLANS` / `readMediaPlans` per-channel readers. Full combined GET (`!skipLineItems`) serves from one `line_items` query set (`readMbaPlanDetail` + `mbaGetAssemble`); shape parity suite: `lib/data/__tests__/mbaPlanDetailParity.test.ts`. `nextVersionNumber` = tip+1 (O4.6). `DATA_BACKEND_PLAN_DETAIL=xano` → 410. Snowflake `XANO_LINE_ITEMS_SNAPSHOT` ingest: X7 PG path behind `LINE_ITEM_SNAPSHOT_SOURCE` (default xano until Luke STOP flip).

## Load

Header via `GET …?skipLineItems=true&billingScheduleFull=1&version=N`, then per-channel line items (60s soft cache + in-flight dedupe). `channelHydrationGate.computeAllChannelsHydrated` gates Save until every enabled container settles. Channel GETs are FK-first (`fetchChannelLineItemsByMba`) with a 5-attempt param-shape fallback for legacy rows. Edit-page `mba_line_approvals` hydrate uses `fetchMbaLineApprovalsClient` → `coalescedGetJson` (30s TTL); effect deps on `billingFeeSeedEnabledConfigs` can re-enter, but same-URL callers share one GET; save/approval PATCH invalidates. Browser media-details reference GETs (`getMagazines` / `getMagazinesAdSizes` / `getDisplaySites` / …) go through `fetchMediaDetailBrowser` (24h TTL); MagazinesContainer and DigitalDisplayContainer mount effects no longer double-hit the network under remount churn.

Edit-page channel-load budgets (Xano fan-out): **45s** initial + **90s** auto-retry; manual retry **180s**; hydration watchdog **150s** (must exceed initial+auto-retry). Late success after the watchdog still applies data and clears that channel's error/`did not finish loading` toast via `mediaLoadStatusAfterChannelSuccess` / `lineItemLoadToastAfterChannelSuccess`.

Empty API payloads settle immediately in the loader; non-empty channels settle when the container publishes via `onMediaLineItemsChange`. `LazyMountWhenVisible` force-mounts once that channel's `mediaLoadStatus` is `ready`/`error` (not only when global `loadPhase === "ready"`), so off-screen Search/etc. still settle. Loader effect cleanup clears `lastLineItemsLoadKeyRef` but does not bump the load generation — a 200 that arrives after cleanup still applies (P1-AMEND late success); only a newer load generation discards results.

## Depends on

Shared core (xano.ts, pagination, caches, auth/RBAC, proxyAllowlist), finance/billing engine (`recomputeBillingScheduleOnSave`, `computeCampaignFinancials` — imported back INTO the mega-pages), KPI (`addKPISheet`, fan-out), money/date/tz utils.

## Consumed by (verified importers)

Billing (8 files: deriveBursts, burstDate, seedLineFees…), finance (4), KPI (5 — `MEDIA_TYPE_ID_CODES` fan-out), pacing (`parseBursts` + 5 live-line-item resolvers), dashboards (7), trafficking/creative, Snowflake sync cron (`fetchAllLineItems` → `XANO_LINE_ITEMS_SNAPSHOT`), delivery compute, AVA autopopulate, planning create-prefill.

## Gotchas (verified)

- Create vs edit mega-pages duplicate xlsx/pdf generation, publisher fetch, MBA numbering, ~20-branch save blocks. Create reads `fv.mp_client_name`; edit reads `fv.mp_clientname`. Doc-step skip/error classification on save must stay twin-synced (create + edit).
- Media Plan Excel on save is client-side (`generateMediaPlan` / exceljs), not the PC3 server gate. Outer save-dialog catch handlers used to paint both MBA and Media Plan as errors when either failed — draft early-skip + per-step classification prevent that false dual-failure.
- DRAFT SUMMARY budget remaining is shared via `lib/mediaplan/campaignBudgetRemaining.ts` against `mbaScopeTotals.nettExGst` (total investment, ex GST) on both create and edit — never media-only `grossMedia`. Caption in `PlanWizardShell`.
- Dirty-on-load: bootstrap `mbaidentifier` / client-fee apply must not open the unsaved dialog — gate waits for `clientBootstrapDone`, bootstrap `setValue` uses `{ shouldDirty: false }` + temporary gate close, channel republishes use `markPassiveChannelChange` with a short quiet window. Save hold copy names outstanding channels via `formatSaveHydrationHoldReason` (gate still blocks). Single Save lives in the floating bar; draft summary is state + Exit only.
- Half-finished container refactor: 6 shared-shell channels vs 14 bespoke; drift is silent. Zod schemas and fieldMaps ARE centralised even for bespoke ones.
- `SearchContainer.getSearchBursts` is a documented escape hatch (hardcodes `deliverables: 0`, omits `lineItemId`) — billing depends on that exact shape.
- Route naming trap: `/api/media_plans` (underscore) = channel tables; `/api/mediaplans` = master/versions.
- List-row shape: Xano `media_plan_versions_latest` denormalises `mp_client_name` onto every version; Postgres versions omit it (master column only). **Two list-cache modules** must both overlay: `mediaPlansListCache` (`/api/mediaplans`) and `mediaPlanVersionsCache` (`/api/media_plans` / dashboard). Shared helper: `lib/api/overlayMasterOwnedListFields.ts`. Do not assume version rows alone carry client name. Xano-only list scalars with no Postgres column (`inputs_hash`, `rebill_needed`, `latest_version_id`, `temp_version_number`) are intentionally absent on the postgres path (DI-11: verified unused by list UI / list cache readers).
- `LINE_ITEM_BROWSER_API_PATH` mixes dedicated route segments and raw Xano table names via the catch-all.
- `publishMediaLineItemsIfChanged` fingerprints with `_reactKey` stripped to avoid an infinite render loop — new publish paths must do the same.
- `MEDIA_TYPE_COLORS` is ARGB (`FF` prefix), not CSS hex.
- Env alias: `XANO_MEDIA_PLANS_BASE_URL` / `XANO_MEDIAPLANS_BASE_URL` (both live, inline at ~15 call sites). API group id `RaUx9FOa` hardcoded in `lib/xano/mediaPlanTables.ts`.
- 79 test files cover `lib/mediaplan/` helpers; the mega-pages and 14 bespoke containers have essentially none.
- Adding a channel touches ~12 hardcoded 20-entry maps — full list in BLAST-RADIUS.md.
