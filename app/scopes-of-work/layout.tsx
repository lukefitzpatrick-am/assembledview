import { pageMetadata } from "@/lib/nav/routeManifest"

export const metadata = pageMetadata("/scopes-of-work")

export default function ScopesLayout({ children }: { children: React.ReactNode }) {
  return children
}
