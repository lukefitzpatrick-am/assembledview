# Module: M365 (Microsoft Graph provisioning)

SharePoint site + Teams provisioning for client groups. Builds on M1 client identity (`clients.m365_is_anchor`, `siteUrlForClient`). **Flag `NEXT_PUBLIC_M365_PROVISIONING` defaults OFF** — every Graph wrapper refuses with a clear error when unset/`off`.

## Entra app registration (Luke — portal)

No app registration exists yet. Create an application (daemon / client-credentials) and grant **application** permissions (admin consent):

| Permission | Why |
|---|---|
| `Sites.ReadWrite.All` | Create / resolve SharePoint sites; create folder paths; upload files |
| `Group.ReadWrite.All` | Create M365 groups that back Teams |
| `Team.Create` | Create Teams from groups / team templates |
| `ChannelMessage.Send` | Post channel messages (kickoff / ops notifications) |

**Credential:** certificate (preferred for prod). Client secret is acceptable only for local/dev mocks — do not ship long-lived secrets as the production auth mode. Record tenant id + client id in Vercel env when ready (`M365_TENANT_ID`, `M365_CLIENT_ID`, cert thumbprint / secret for local only).

## Code map

| Piece | Role |
|---|---|
| `lib/m365/siteUrlForClient.ts` | Pure identifier → `/sites/cli-<lower>` (M1) |
| `lib/m365/reconciliation.ts` | Pure M4 join: client groups + casing flags + unmatched plan MBAs (TI-1 §3a) |
| `lib/m365/reconciliation.server.ts` | Postgres loader for reconciliation report (read-only) |
| `app/admin/m365-reconciliation` | Admin UI skeleton; `GET /api/admin/m365-reconciliation` (`requireAdmin`) |
| `lib/m365/featureFlag.ts` | `isM365ProvisioningEnabled()` — on/1/true only |
| `lib/m365/graphTransport.ts` | Injectable HTTP transport; default `fetch` transport unused in tests |
| `lib/m365/graphClient.ts` | Token cache + skew, 429/`Retry-After` + exponential backoff+jitter, typed wrappers, `ensureSite` / `ensureFolderPath` |
| `lib/m365/provisioningLog.ts` | `m365_provisioning_log` writer (injectable; DB writer is server-only) |
| `db/migrations/0021_m365_provisioning_log.sql` | Audit table — one row per attempt |

## Reconciliation (M4 skeleton)

- Every `clients` row, grouped by `lower(trim(mbaidentifier))` (same model as `resolveClientGroup`).
- Columns: derived `siteUrlForClient`, stored `sharepoint_site_url` / `teams_group_id`, dashboard `slug`, casing anomaly (mixed casings in group or non-lower stored id), Graph check = `pending — provisioning flag off` until credentials + live check.
- Unmatched plans: `media_plan_masters.mba_number` with no client identifier prefix match (live set is the 12 junk MBAs from TI-1 §3a). Never provisionable via identifier join — per-row decision required.
- Read-only: no Graph calls, no DB writes from this surface.

## Behaviour laws

- **All network behind one transport** — unit suites inject a mock; no real Graph/login calls in `npm run test:*`.
- **Flag off → throw** before token or Graph (`M365ProvisioningDisabledError`); still records a failure log row when a log writer is wired.
- **Token:** client-credentials against `login.microsoftonline.com/{tenant}/oauth2/v2.0/token`, in-memory cache, refresh with expiry skew (default 60s).
- **Retries:** 429 honours `Retry-After` (seconds or HTTP-date); otherwise exponential backoff + jitter. Each attempt writes a provisioning log row (success or failure).
- **Idempotency:** `ensureSite` / `ensureFolderPath` are check-then-create; a second run is a no-op (returns existing).

## Related

- Client group model: `lib/clients/clientGroup.ts` + M1 invariants in `INVARIANTS.md`
- DI-15: rename vs persisted dashboard slug
