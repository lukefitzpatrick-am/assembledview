import { pageMetadata } from "@/lib/nav/routeManifest"

export const metadata = pageMetadata("/admin/users/new")

export default function AdminUsersNewLayout({ children }: { children: React.ReactNode }) {
  return children
}
