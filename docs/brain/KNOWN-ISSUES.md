# Known Issues & Tech Debt Register

One register, stable IDs. **Check here before "discovering" a bug** — it may be known. When you fix one, mark it `FIXED (commit)` — don't delete the row. Add new items with the next free ID in the right section.

C-numbers are allocated on `localhost` only, and are never reassigned once written. A hotfix that goes straight to `main` does NOT get a C-number on `main` — it is logged here after the main→localhost merge-back that BRANCHING.md rule 5 now requires. If you find yourself renumbering an existing row to resolve a conflict, the allocation rule has been broken somewhere upstream — say so rather than renumbering.

## Security (SEC-*) — treat as highest priority

| ID | Issue | Status |
|---|---|---|
| SEC-1 | Two catch-all Xano proxies (`/api/media_plans/[...path]`, `/api/media-details/[...path]`) accept any authenticated session and forward with server `XANO_API_KEY`. Allowlist (`proxyAllowlist.ts`) constrains paths/methods but not tenant | Constrained (SEC-D) live-verified (`7f652e42`) — staff-only `requireRole(["admin"])` (`manager` removed); client catch-all → 403 + `[proxy-403]` soak log; dies with Xano reads at T6 |
| SEC-2 | `GET /api/mediaplans` returns ALL clients' plans to any admin/manager — no per-caller filter (role-gated only) | FIXED (SEC-B) live-verified (`7f652e42`) — clients MBA-scoped (`resolveClientMbaScope`); admin full list (probe: client 1 vs admin 189) |
| SEC-3 | `PUT`/`PATCH` on `/api/mediaplans/mba/[mba_number]` skip the `checkClientMbaAccess` that GET enforces | Verified FIXED (`ff648830`) — both PUT and PATCH call `checkClientMbaAccess`; foreign PUT client → 403 live-verified (`7f652e42`) |
| SEC-4 | `GET /api/dashboard/[slug]` has no tenant match for client users | FIXED (SEC-A) live-verified (`7f652e42`) — own slug 200 / foreign slug 403; admin unscoped |
| SEC-5 | Finance + client admin APIs are session-only in places; two routes carry literal `// allow access for development` comments (`app/api/clients/route.ts`, `app/api/mba/generate/route.ts`) | FIXED (SEC-C) live-verified (`7f652e42`) — `/api/clients` + accrual + forecast: unauth 401 / client 403 / admin auth-path; staff gate is `requireRole(["admin"])` after manager removal |
| SEC-6 | Most `/api/kpis/*` write methods have no authz | FIXED (SEC-A) live-verified (`7f652e42`) — writes + client/publisher GETs `requireRole(["admin"])` (same staff gate; create/edit consume GETs via `lib/api/kpi.ts`; `manager` removed from UserRole) |
| SEC-7 | `/api/pacing/programmatic/{display,video}` and `/api/pacing/social/{meta,tiktok}` have **no authorization gate** — any authenticated client user can read another tenant's delivery data (compare `/api/pacing/bulk` which checks) | FIXED (SEC-A) live-verified (`7f652e42`) — client own MBA non-403 / foreign 403 / admin auth-path on all four POSTs |
| SEC-8 | Middleware enforces authentication only on `/api/*`; tenant isolation is per-handler and only ~13 routes check. `/api/cron/*` bypasses middleware entirely (protected only by `assertCronSecret`) | By design — but every new API route must add its own tenant check |
| SEC-9 | `checkClientMbaAccess` fallback uses exact equality `mbaNumber === mbaidentifier` (e.g. `PENFOLD001` vs `PENFOLD`) so it effectively always denies — client users MUST have `app_metadata.mba_numbers`. The working prefix-matcher `mbaNumberMatchesClientIdentifier.ts` exists but is imported only by its own test | FIXED (SEC-B) live-verified (`7f652e42`) — fallback uses `mbaNumberMatchesClientIdentifier`; `mba_numbers` remains primary |
| SEC-10 | Dynamic `[param]` API routes missing the role/tenant gate of their collection sibling (~19 suspected from UX handover; code inventory found **5 clear GAP methods**) | **static complete — live probe pending** (O6 + SEC-G): O6 gates + SEC-G morning answers — publishers/best-practice writes `requireRole(admin)`, television `[id]` PUT/DELETE `requireRole(admin)`, dedicated channel collection POSTs `requireRole(admin)` (SEC-G residual), `clients/[id]` GET intentional split, creative soft-spot via `checkClientMbaAccess` (admin-only unscoped); inventory `docs/brain/API-DYNAMIC-ROUTE-GATES.md` |
| SEC-11 | `GET /api/media_plans` (dashboard list), dedicated channel GETs via `createChannelLineItemsGetHandler` (~12 routes), and `POST /api/pacing/search` were session-only — any authenticated client could read other tenants' plan lists, channel line items by `mba_number`, and search pacing by foreign `lineItemIds` | FIXED (`d2fed8e6`) — list uses `resolveClientMbaScope` (twin of SEC-2 `/api/mediaplans`); channel handler calls `checkClientMbaAccess` before read; search derives MBA from each line item id via `parseMbaNumberFromLineItemId` then `checkClientMbaAccess` |
| SEC-13 | Five `/api/billing-overrides*` routes + `GET`/`PATCH` `/api/mba-line-approvals` were session-only — any authenticated client could read/write commercial override and approval rows for other tenants | FIXED — each route calls `checkClientMbaAccess` before read/write; GET overrides derives MBA from `media_plan_version_id` via `resolveMbaNumberForVersionId` (never trusts a client-supplied MBA for that lookup) |
| SEC-14 | `GET /api/mediaplans/mbanumber` is session-only — any authenticated session can mint/allocate MBA numbers for any `mbaidentifier` | FIXED — `requireRole(["admin"])`; probe: create + edit same-origin `fetch` only (session cookie); no client-role callers |
| SEC-15 | Living API AuthZ recount + static guard harness — unguarded new routes fail CI; remaining tenant-scoped `none` + admin consolidation candidates are report-only | Inventory landed — regenerate via `scripts/generate-api-tenant-classification.ts`; harness `lib/api/__tests__/tenantRouteGuard.harness.test.ts` |
| SEC-16 | `POST /api/pacing/search` returned opaque 403 on unparseable `lineItemId` before role resolution — admins hit by junk/legacy MBA ids | FIXED — resolve admin first; admin skips junk ids (logged); client 403 names `failedLineItemId` |
| SEC-17 | `GET …/expected-spend-to-date` had no local MBA gate (cookie-forward only) | FIXED — `checkClientMbaAccess` on mba param before upstream fetch |
| SEC-18 | `GET /api/dashboard/spend-parity` classified tenant-scoped+none (404 in prod is not a guard) | FIXED — `requireRole(["admin"])` then existing prod 404 |

## Session / save UI (SE-*)

| ID | Issue | Status |
|---|---|---|
| SE-1 | `POST /api/plans/save` 401 from edge middleware left SavingModal pending-spinners (looked hung) and create treated postgres `!ok` as success (`clearDirtyOnSaveSuccess` + `router.push`) while reads still looked logged-in | FIXED — session-expired copy + skip remaining steps + app banner until the next successful write; create throws `WriteSessionExpiredError` so `handleSaveAll` stays on the page |

## Campaign status / phase (CS-*)

| ID | Issue | Status |
|---|---|---|
| CS-1 | `lib/finance/computeAccrual.ts` `RECEIVABLE_STATUSES` mixes campaign commercial status (`booked`, `approved`) with billing lifecycle (`invoiced`, `paid`). Those last two are not `campaign_status` values. Different axis from `resolveCampaignPhase` — do not fold into phase. Accrual amounts stay on this mixed set. `lib/finance/financeTabFilterScope.ts` `RECEIVABLE_STATUSES` is a separate Overview KPI filter list. | Open — leave accrual membership unchanged |
| CS-B2 | CS-B `CampaignStatusControl.persist()` gated PATCH on a truthy MBA number. Create mints `mba_number` before any `media_plan_masters` row exists, so the Combobox PATCHed a missing master, `onStatusCommitted` never ran, and status could not be set on a new campaign. Guard is now `persisted` (master row exists). Derived phase must not disable the selector. | FIXED — `persisted={mediaPlanId != null}` on create; `persisted={true}` on edit |
| CS-E | Finance still filtered on version `campaign_status` after CS-B stopped writing it. First status PATCH (e.g. Planned → Booked) would freeze that campaign on its old finance treatment. | FIXED — `FINANCE_STATUS_*_SQL` and hub/forecast/client-pays/cutArQuery read `media_plan_masters.campaign_status` (alias `m`); version column is historical and untouched |

## Data integrity (DI-*)

| ID | Issue | Status |
|---|---|---|
| DI-1 | `media_plan_production` has no version FK → production lines render across all versions; MBA-only fallback must be retained (was D4-K1) | Open — needs Xano schema change + backfill |
| DI-2 | Container line items observed missing from saved `billingSchedule` (glenda007 v9: PD2 in Xano, absent from persisted JSON). Save serializer audit + "re-sync containers to billing" action proposed | Open |
| DI-3 | Legacy duplicate billing line-item ID producers can create double-count readers | Open |
| DI-4 | Orphan fix UPDATEs `MART.SEARCH_PACING_FACT` directly (no mapping table) — reverted by any warehouse full refresh; updates all dates for the tuple | Open |
| DI-14 | **MART identifier case** — fact `LINE_ITEM_NAME` mixed-case from platforms; snapshot carried uppercase ids (`BOSS001ML1`) beside lower fact ids; `XANO_LINE_ITEMS_SNAPSHOT` also had **324 case-alias duplicate PKs** (same id differing only by case) which **double-counted** spend joins for `boss001` (−$27,970.32 once collapsed). Views/tasks/sync now LOWER; snapshot folded to 2364 rows / 0 uppercase. **Blocked:** `AV_APP_WRITE_ROLE` cannot `CREATE OR REPLACE` MART views (OWNERSHIP=`ACCOUNTADMIN`) nor UPDATE `PACING_FACT`/`SOCIAL_PACING_FACT` — full historical fact backfill needs ACCOUNTADMIN via `scripts/snowflake/deploy-mba-case-norm.mjs` | Partial — snapshot+sync+SQL ready; fact view deploy + full backfill pending ACCOUNTADMIN |
| DI-15 | **Client rename vs persisted dashboard slug** — M1 adds unique `clients.slug` + create-time stamp; `updateClientPostgresFirst` preserves slug unless body sends `slug`. Auth0 `app_metadata.client_slug` + any UI that still assumes name→slug identity are not yet transactionally refreshed on rename. Rename write path: `PUT`/`PATCH` `app/api/clients/[id]/route.ts` → `lib/data/writeClients.ts` `updateClientPostgresFirst` (`mp_client_name` in `WRITABLE_SNAKE_TO_CAMEL`). | Open — constraint landed; full rename/Auth0 cutover pending |
| DI-5 | `fetchAllXanoPagesWithCompleteness` treats wrapped `{items:[…]}` envelopes as `[]` (silent truncation) and ignores `nextPage`. Plain `fetchAllXanoPages` silently caps at 10,000 rows (50 pages × 200) | Open |
| DI-6 | Xano tables in docs but absent from `xano-tables-schema.json` export (`pacing_mappings`, `pacing_thresholds`, `finance_forecast_snapshots*`, …) — export stale or tables never created; reconcile | Open |
| DI-7 | Publisher-ID parity between `XANO_PUBLISHERS_BASE_URL` and `XANO_CLIENTS_BASE_URL/get_publishers` unverified | Open |
| DI-8 | Xano filtering not applied on all line-item tables — raw counts stay MBA-wide on several channels (newspaper 174 raw / 3 kept) with client-side filtering doing the work (was D4-K5) | Open |
| DI-9 | Postgres `GET /api/mediaplans` list dropped master-owned `mp_client_name` (Xano `_latest` had it inline; `mergeLatestVersionsWithMasters` overlaid `version_number` only) → list search crashed on `.toLowerCase()` | FIXED (`58ed536d`) — `overlayMasterOwnedListFields` in `mediaPlansListCache` + fail-closed `matchesMediaPlanSearch` (Campaigns list path only; dashboard twin is DI-9b) |
| DI-9b | Postgres `GET /api/media_plans` (dashboard via `mediaPlanVersionsCache`) same miss as DI-9 — version rows served with no master overlay → empty `mp_client_name` → blank Client Name column, View disabled, client filter/search empty, Total Live Clients counted scopes only | FIXED (`206321cb`) — shared `lib/api/overlayMasterOwnedListFields.ts` + `applyMasterOwnedOverlayByMba` on versions-cache postgres path |
| DI-10 | Postgres read shaping via `coerceNumericStringsToNumbers` turned text identifiers into numbers (`mba_number` `"001001"` → `1001`) → list search threw, routes/scope/mirror keyed on wrong identity | FIXED (`3fc3517e`) — `IDENTIFIER_TEXT_FIELDS` default keep-as-text in `lib/data/toApiRow.ts` + `String(x ?? "")` in `matchesMediaPlanSearch` |
| DI-11 | Residual from 58ed536d (~75%): do list API consumers need Xano-only scalars `inputs_hash` / `rebill_needed` / `latest_version_id` / `temp_version_number`? | VERIFIED unused — sweep of `app/mediaplans`, list cache, and UI consumers finds no reads; safe to omit on postgres list path (finance write path still owns `inputs_hash`/`rebill_needed` on version PATCH) |
| DI-12 | Codex `tasks.client_id` (and `client_notes` / `client_domains` / `assignment_rules` / `ava_task_proposals`) has **no FK to `clients`** — deliberate in 0013 until T6 ETL reload ends. **App-level exists check** on task create + PATCH `client_id` via `codexClientExists` (`lib/codex/clientExists.ts`) → 400 if missing. RESTRICT FK still waits for T6 after orphan cleanup. | Open — FK at T6; app check landed Stage 0 hardening |
| DI-13 | **Codex `codex_activity` has no FK to `tasks`** — activity rows can outlive their subject. Observed: activity row id 1 references task id 2, which no longer exists; tasks 1 and 2 were hard-deleted outside the application on or before 4 Aug (cause unknown). App soft-delete path only (`deleted_at`); no hard-delete API. | Open observation — not a diagnosed defect; record only (`docs/brain/modules/codex.md`) |

## Correctness (C-*)

| ID | Issue | Status |
|---|---|---|
| C-1 | Excel media plan Column N labelled "Gross Media" but contains **net** `mediaAmount`; no per-line Fee column in Excel | Open — decide + fix labels or values |
| C-2 | 14 of 20 channels share the `buyAmountStr` bonus-only bug: manual-qty buy types other than `bonus` lose quantity on expert→standard mapping (`docs/expert-grid-bug-sweep.md` has the per-channel matrix) | Open |
| C-3 | `OohExpertGrid` CPM function body (`q * r`) contradicts its own tooltip (`/1000`); canonical inverse CPM is `(deliverables/1000) × unitRate` | Open |
| C-4 | `SocialMediaContainer` computes deliverables inline in 2 places, bypassing the shared primitive; also `lib/billing/computeSchedule.ts:206-220` | Open |
| C-5 | Rounding split across 9 local `computeLoadedDeliverables` copies (rounded on 5 channels, raw on 4) — unifying changes displayed numbers; explicit Luke decision required | Open — decision pending |
| C-6 | Pacing Overview claims "all clients across all channels" but several summaries load search-scope only; not fully rewired after new tabs | Verify current state |
| C-7 | `generateBillingLineItems.ts:89` re-implements only 1 of 4 fee branches (no gross-up, no bonus zeroing) | FIXED (PC2) |
| C-8 | `computeDerivedCampaignFeeAmount` is a second campaign-fee total reconciled only within $10 (vs $0.01 everywhere else) | FIXED (PC2) |
| C-9 | `normaliseScheduleMediaType` defaults unknown media types to `"search"` — misspelled channel silently inherits search fee % | FIXED (PC2) |
| C-10 | Search (and other non-empty channels) could stall at hydration: loader effect discarded 200s on `cancelled` while `loadKey` blocked re-run, and `LazyMountWhenVisible` only force-mounted after global `loadPhase === "ready"` so off-screen Search never published settle → watchdog "did not finish loading" | FIXED (edit loader generation + clear loadKey on cleanup; forceMount once past section loader) |
| C-16 | `deriveReceivableRecords` falls back to a djb2 hash of client name as synthetic client ID (100000–999999) flowing into grouping/filters/exports | Open |
| C-17 | Knowledge calculators: four percent formulas missing `* 100` (100× low), CPV/LTV variable mismatch made evaluator fail silently, Calculate button was a no-op beside auto-calc | FIXED (evaluator harden + terms/KNOWN_FORMULAS; copyable auto-calc result) |
| C-11 | `checkPublishLineItemIntegrity` fails open on Xano errors (client-side `shouldBlockEmptyPublish` is the primary gate) | By design — know it |
| C-12 | `filterByMbaAndVersion` treats `media_plan_version` as both FK id and version number depending on row shape; channel GETs try up to 5 param shapes and keep the "best" result heuristically | By design (legacy rows) — fragile |
| C-13 | Payment terms hardcoded "Net 30 days" in 4 places; ad-serving `BASELINE_CTR=0.001` / `BASELINE_VTR=0.25` hardcoded; RAG bands hardcoded; forecast labels say "20%/40%" but rates come from data | Open |
| C-14 | `monthYear` vs `YYYY-MM` normalisation before `isBillingMonthLocked`; UTC vs AU-local lock cutoff; super-admin override contract — all unresolved | FIXED (PC5): keys → `YYYY-MM` via `lib/finance/periods/monthKey.ts`; lock = `finance_periods.status` / Sydney last-day 23:59; admin amend = warning → mandatory reason → before/after audit → `amended_after_lock` + immutable v2 sheet |
| C-15 | Plan-C S1-P1b: `computeCampaignFinancials` / `recomputeBillingScheduleOnSave` emitted month-header schedules with no `lineItems` (fee scalar on `perLine` only) — blocked Postgres `schedule_months` explode + enforce | FIXED (`attachScheduleLineDetail`; `PLANC_SERVER_AUTHORITY=enforce` still OFF) |
| C-18 | Admin KPI write paths can return **500** after authz clears (PS-1 live matrix: admin KPI writes ×10 expected auth-path, observed `400/500/200` — gate cleared, body/handler still throws on some methods) | FIXED (`0e34fc13` / S7 handlers; X5 PG writes) — named 400/502; campaign/client writes are PG-first + Xano mirror via `lib/data/writeKpi.ts` (percentUnits decimal ≤1 enforced; no magnitude heuristic). Publisher KPI writes still Xano until a follow-up. Matrix: `npm run test:kpi-writes` + `npm run test:write-kpi` |
| C-19 | Spurious **auto-drift billing gate** — auto/`undefined` billingMode lines flag divergent (or block save) when amount noise / rematerialisation is expected, not a true manual override | FIXED (O4) — root cause: hydrate set `isManualBilling` from ANY `compareBillingDivergence` (incl. fee/header drift vs burst `autoReference`), so `shouldResyncBillingLineFromAuto` froze legacy-auto rows; postgres pre-save no longer blocks AUTO-only drift (server recomputes + correction toast); Xano `recomputeAndValidate` 409 path unchanged; explicit `billingMode=manual` + C2 sum still block |
| C-20 | **KPI percent-unit contract** — code assumes decimal storage (AV-25 v2 / O5); leftover percentage-point cells + ambiguous exact-`1.0` rows in Xano and Postgres still need per-row migration; `db:etl` reload reintroduces Xano ambiguity until Xano is fixed | Open — code landed; migration pending Luke (`npm run scan:kpi-percent-units`) |
| C-21 | **Publish/status-change fee wipe** — postgres save could persist media-only `schedule_months` when stamped snapshot `feePct` was dropped at `buildSavePlanLineItemsFromSnapshots` (`meta?.feePct` only) and/or feeLoading omitted from a non-shared body path (krusty015 v3: $40k media, $0 fee; v2 had +$10k fee) | FIXED (`53c4029f` / O4.5) — `assemblePlansSaveRequestBody` + stamped `feePct` prefer; `[savePlan-fee-zero]` tripwire; evidence: Luke re-publish krusty015 → **v4** must carry media+fee (v3 wipe) |
| C-22 | **MBA GET dual read path** (`/api/mediaplans/mba/[mba_number]`) — X2 closeout: default `DATA_BACKEND_PLAN_DETAIL=postgres` serves `readMbaPlanDetail` only; `xano` → 410 `PLAN_DETAIL_XANO_GONE` (fan-out deleted). `nextVersionNumber` = tip+1 (O4.6). Postgres errors return 500 `PLAN_DETAIL_POSTGRES_FAILED` with no silent Xano fallback | FIXED (X2) — postgres-only GET; flag kept for explicit 410 |
| C-23 | **Xano mirror dropped schedule blobs** — `mirrorInputFromSave` omitted `legacySchedules` so mirrored versions were line-items-only (krusty015 recon); failures lived only in the in-memory shadow-diff ring buffer and vanished on restart | FIXED (`9eccdd51` / S2) — return blobs from `savePlanVersion`, pass through `mirrorInputFromSave`; durable `app_notifications` (`xano_mirror_failed`) + resolve on retry |
| C-24 | **Draft doc-step skip** — SavingModal treated below-approved MBA PDF / Media Plan file skips as errors (“Saving with Errors”) | FIXED (`231fc295` / S3) — `lib/docs/saveDocSteps.ts` marks skips; modal does not enter error state for skips alone |
| C-25 | **Dashboard monthly treemap** still hit Xano custom endpoints under `DATA_BACKEND=postgres` | FIXED (`73fd1bbe` / S1) — `getGlobalMonthly{Publisher,Client}Spend` postgres path via `schedule_months` aggregates |
| C-26 | **`schedule_months.line_item_id` vs `line_items.line_item_id` join key mismatch** — schedule cells often store `billing-{mediaType}::{lineId}` while `line_items` stores bare `{lineId}`; exact-equality joins miss all rows → Unspecified publishers + client-pays filter never applies | FN-FIX-1: dual-shape join + fixture-proven client-pays exclusion in sections (`scheduleLineJoinSql` + `scheduleLineJoinClientPays.test.ts`); `__service__*` → campaign-level bucket + `coverage.lineDetailPct`. Residual: `lib/data/dashboardMonthlySpend.ts` still exact-equality |
| C-27 | **Investment cut fee line-month coverage near zero** — published-tip `schedule_months` often media-only for legacy/ETL tips (O4.5/C-21 class); FY2025 probe: billing 2/1686 media months have fee rows (~0.1%), delivery 0/1773 | Open — cut surfaces `coverage.fee` + caveat; UI shows human `FEE_COVERAGE_USER_NOTICE` (FIN-8) with technical caveat in tooltip; margin view must join `mba_fee_snapshots` / recompute feePct×media; do not treat `fee_cents` as complete; **agency-economics presets are current-FY-only** until this is fixed (422 `AGENCY_ECONOMICS_HISTORIC_FY_BLOCKED`) |
| C-28 | **Agency economics Luke opens** — (1) whether cut `adserving_cents` counts as agency revenue (forecast maps `adservingTechFees` into `service_fee_digital`, not 1:1 with schedule adserving); (2) margin_pct RAG thresholds; (3) billingAgency grain for revenue (retainer is client-level — currently refused) | Open — defaults: adserving excluded, margin neutral, billingAgency blocked for revenue measures |
| C-29 | **Costs payables under reconciliation** — after FN7, Costs is the only payables surface; legacy hub composition (version pool / production / month-header fee+adserving / status filters) diverged from sections published tip | Amended (CP-3) — status scope signed: `approved`+`booked`+`completed` included, `draft`/`planned`/`cancelled` excluded with `coverage.excludedByStatusCents`; payables headline = media only (fee/adserving labelled separately); D1 published tip + `schedule_months` is version-pool authority; orphan non-service media in `coverage.orphanLineCents`. Banner removed when FY2026 Jul–Aug verify targets hit. Residual: orphan join cleanup + FN7 UX register still pending live smoke. **CP-8:** client-pays detail page discloses the same line-detail limit (`__service__*` cannot carry `client_pays_for_media`) so the two views stay honest complements |
| C-30 | `getHighestBookedApprovedCompletedVersionPerMba` (DashboardOverview) reaches past newer non-live statuses to find booked/approved/completed — correct for Draft, wrong for **Cancelled**. A cancelled newest version should hide older live rows (`candel002`, `cuheal001`). Harmless today only because `/api/media_plans` pre-collapses to latest-per-MBA so older live rows never arrive | Open — exclude when a newer terminal status (cancelled) exists; do not treat all non-live as ignorable |
| C-31 | **MBA edit "Maximum update depth exceeded"** — `@radix-ui/react-switch` Root composes node-into-`useState` (`setButton` / 1.3.7 `setControl`) via `useComposedRefs`; React 19 ref detach/attach during channel-enable Switch remount churn calls setState in commitMutationEffects (HAR: 0 failed requests, edit document fetched 3×). Latent upstream (radix-ui/primitives#3963); #3967 stabilized other packages but **not** Switch — 1.2.6→1.3.7 still `useComposedRefs(forwardedRef, setControl)` + BubbleInput. Reproduced path-independent (`DATA_BACKEND_PLAN_DETAIL` unset and postgres) | FIXED — native `components/ui/switch.tsx` (no Radix Root); stable ref callback, no node-in-useState; consumers all use `onCheckedChange` / RHF (no BubbleInput `name`/`form`); commit `fix(ui): stop Switch remount loop… (C-31)` |
| C-32 | **BUG-2 — create/edit max update depth via `usePlanDraftSession`** — pill effect depended on `modeResolved` (new object every render from `resolvePostgresSaveMode`) and called `setPill(null)` on the `!enabled` path (flag default off → runs for everyone). Driver = unstable `modeResolved` identity, not non-null pill init (`useState(null)`). | FIXED — `useMemo` on primitive mode inputs; disabled path `setPill(prev => prev == null ? prev : null)`; hook test in `hooks/__tests__/usePlanDraftSession.test.tsx` |
| C-33 | **PC6 class-(c) completed MBAs with line items but empty `schedule_months`** — named non-zero cohort: `golf021` (candidate INV-1135), `golf020` ($90,010), plus remaining `PGAAUS005`, `PENFOLD008` / `PENFOLD010` / `PENFOLD011` (~$100k combined). Probe script: `scripts/pc6-class-c-verdict.ts` | **Standing manual invoice-matching** (Luke 2026-08-02) — remain in the human match process; **not a launch gate**. Do not block cutover/flip on auto-match for this class |
| C-35 | **MBA edit ad-serving ignored line `no_adserving`** — `generateBillingLineItems` preview read `burst.noAdserving` (null on every persisted burst; BOSS011 all 10 null) while server `computeCampaignFinancials` stamps from the LINE. Editor charged ad serving when the line excluded it | FIXED (AS-2) — `resolveLineNoAdserving` + `computeMediaLineAdServingMonthlyAmounts`; create has no twin (shared media-only `generateBillingLineItems`); `computeSchedule` / `attachScheduleLineDetail` keep reading the LINE stamp only |
| C-36 | **OOH + Integration form camel is `noAdserving`** (everyone else: `noadserving`) — `containerChannelConfig` fieldMap, Zod schemas, `OOHContainer`/`IntegrationContainer` (`item.noAdserving` / `li.noAdserving`), `expertChannelMappings` + `expertModeSwitch` all agree on camelCase A. Neither channel is ad-serving-eligible (hygiene only). Aligning fieldMap alone breaks checkbox/hydration | Open — leave intentional; full align is a multi-surface rename, not a one-liner. Postgres save already accepts both via `resolveLineNoAdserving` / `raw.noAdserving` |
| C-37 | **`rebucketRowsForWeekStartsOn` destroys merges** — materialises span totals into week cells then sets `mergedWeekSpans: []` (`lib/mediaplan/expertDayModel.ts`). Changing week-start silently clears merges. Luke (4 Aug): merges must become day-aware and survive week-start change when daily detail lands. Do not conflate with Apply-column parity (fixed separately; `weekStartsOn` owned by containers) | Open — day-aware merge survival; touches `expertDayModel`, ExpertGrid merge UI/drag, channel mappers that emit/consume `mergedWeekSpans` |
| C-38 | **OOH Expert Apply never lights `expertApplyPendingPageSave`** — `OOHContainer` subscribes to `subscribeMediaPlanPageSaved` and renders the badge UI, but `handleExpertApply` omits `setExpertApplyPendingPageSave(true)` (13 peers + `useMediaChannelContainer` set it). Soft "Applied earlier — awaiting page Save" never appears for OOH. Pinned by `hasUnsavedChanges.characterisation.test.ts` | FIXED (P2-2 — `setExpertApplyPendingPageSave(true)` on Apply) |
| C-39 | **Codex `codex_activity.actor_kind` is hardcoded `"user"`** (`lib/codex/repo.ts` `appendActivity`) — `CodexActorKind` declares `user` \| `ava` \| `system` (`db/schema/codex.ts`) but writes never use `ava`/`system`. Matters at Stage 3+ when AVA proposals must be attributable to AVA, not a person | FIXED (`2fe0ac69`) — `appendActivity` takes `actorKind?: CodexActorKind` (default `user`); `createComment` with `authorKind: "ava"` stamps activity `ava` |
| C-40 | **Production double-counted in MBA scope / Media Plan Excel** — `computeCampaignFinancials` summed production line `.media` into `mbaScopeTotals.grossMedia` and again via `sumScheduleField(..., "production")`, so `nettExGst` was inflated by the production total on every affected campaign (Excel N56 “Total Gross Media” ≠ sum of media-type rows above; N61 “Total Ex GST” overstated). Schedule headers (`computeSchedule` → `productionTotal` not `totalMedia`), persisted `schedule_months`, `approvedSlice` / `buildMbaFromPersisted` PDFs, and `computeCampaignFinancialsFromVersion` (mediaTotal + production fields) were already correct — issued MBA PDFs and persisted schedule rows were NOT affected. Fixed on `hotfix/production-not-gross-media` (`grossMedia` excludes production; UI marks production non-additive). | FIXED (`b8288268` / `5d6649ce`) |
| C-41 | **Delivery container deliverable roll-up mixes units** — `programmaticAdapterShared` (and social) sum `m.booked.deliverables` / actuals across lines whose deliverable keys differ (e.g. BOSS005 Prog Display: Taboola **clicks** + DV360 **impressions** into one card). Title fix (buy-type aggregate → "Deliverable delivery" when mixed) does not make the number commensurate | Open for programmatic + social (one mixed deliverable card). Direct Booked Digital roll-up exclusion lifted — two progress cards (impressions / clicks); `bookedDeliverables` routes planned totals by buy type so denominators never mix; delivered impressions are a physical count. Programmatic still needs split cards or refuse mixed roll-up; do not “fix” by retitling alone |
| C-42 | **Media plan PNG export threw / hung** — `backgroundColor: "hsl(var(--card))"` → html2canvas `Unsupported angle type`; no catch so silent fail; data-URL `<a download>` often blocked; double `requestAnimationFrame` never fires in a hidden tab → button stuck on "Exporting…" | FIXED (`3eddb669`) — computed CSS colour, timeouts + toast, blob `triggerDownload`, scroll-extent size |
| C-43 | **Delivery line labels fell through to raw ids** — `attrs.line_item_name` is populated on **0 of 12,143** line items platform-wide, so every adapter `line_item_name` fallback was dead and always rendered `line_item_id` | FIXED (`797370fc`) — `deliveryLineItemDisplayName` from platform/publisher + creative_targeting |
| C-44 | **Planned-media donut counted agency fees (and production) as media** — card captioned "planned media" while ~16.5% of BOSS005 was Fees; every media share diluted. Types-shape could also emit a `Production` slice (glenda008 $12.0K) while costs-shape already folds production into Fees | FIXED (`0b9fbacb` fees; CD6 production on `hotfix/client-dashboard-clarity`) — display-only `isNonMediaMixSlice` excludes Fees + Production; `channelTotalsFromDeliverySchedule` / Planned-to-date unchanged |
| C-45 | **Google Ads ad-group names show plan-code suffixes on client UI** — `LINE_ITEM_NAME` (= AD_GROUP_NAME) carries `-BOSS00xSEy` (BOSS005 all `-BOSS002SE1`; BOSS006 mix). Attribution is **correct** (Jul-2026 rollovers of Sep-25→Jun-26 campaigns; see `av-review/boss006se1-gads-snowflake-variance-2026-08-04.md`). Only **display** strips the suffix via `stripPlanCodeSuffix` — **no Snowflake data changed** | FIXED (`52712e44`) — ad-group table inside plan-line block; totals-only additive `adGroups` query |
| C-47 | **Xano stage-then-publish: plain Save advanced tip** — create/edit hard-coded phase-two `PATCH version_number` whenever `deferredPublish` (glenda006/008: draft tip cut + stamped). `saveIntent` existed but was unused after mode resolve | FIXED — `shouldRunDeferredMasterPublish` gates PATCH + empty-publish / `pendingPublishRetry` on `saveIntent === "publish"`; stage-without-publish stays for `intent: "save"` |
| C-46 | **`captureNodePng` SVG fast path drops CSS var fills** — `chart-shell.tsx` serialises chart SVGs standalone, so `var(--av-*)` fills cannot resolve in the exported PNG; axis/label text on exported charts is affected | Open — mechanism only; Luke to decide (rasterise / inline computed colours / skip SVG fast path) |
| C-48 | **Confirm MyHours write then Postgres update is non-atomic** — `Create Time Log` can succeed before the proposal row records `confirmed` / `myhours_log_id`; until the MyHours mirror catches up, a retry can create a duplicate time log | Open |
| C-50 | **Publisher Hub spend used `header1 ?? publisher`** — empty-string `header1` is not nullish, so ingest-created QMS / JCDecaux / SCA / SEN lines never attributed. Matcher now treats empty/whitespace `header1` as absent (`header1 \|\| publisher \|\| publisherid`), prefers numeric `publisher_id` / `publisher_profiles.publisher_id` (0036), and joins short stamps via `resolveCatalogueIdForProfileName` | FIXED — `lineItemMatchesPublisher`; regression: `header1=""`, `publisher="QMS"` → QMS |
| C-53 | **Structured deadline columns exist on `publisher_specs` but are still unpopulated** — Cover / SPECS / Codex / campaign strip now read `publisher_specs` min/max/business first and fall back to the vendored `supply_deadline_rule` parse only when the row is missing or columns are NULL. `0046_publisher_specs_deadline_seed.sql` is AUTHOR ONLY (marker `0046_deadline_seed`); do not drizzle-kit. Runtime never writes `mi-library/`. | FIXED (pending Claude's 0046 apply) |
| C-54 | **`publishers.id` 61 is a trailing-space duplicate of Nine (id 11)** — catalogue row must not be a join/seed/assign target. Fireflies Assign already excludes it (`EXCLUDED_PUBLISHER_IDS`); `0041_publisher_specs` seed comment says do not touch. Do not merge or delete the row without Luke. | Open — report-only |
| C-55 | **`publishers.id` 3 `publishertype` is `'internal biddable'` (space) vs app enum `'internal_biddable'`** — Zod / publisher forms / `PUBLISHER_TYPE_INTERNAL_BIDDABLE` expect the underscore. Finance `norm()` is trim+lower only, so the space form will not match. Do not rewrite the cell without Luke. | Open — report-only |
| C-56 | **CM360 delivery does not attribute** — `VW_PACING_CM360` derives its plan key with `regexp_substr(PLACEMENT,'[A-Z]{3}AU[0-9]{3,}[A-Z0-9]+')`, a pattern no Assembled plan code matches, so 100% of CM360 delivery is orphaned and all four Direct Booked Digital containers render plan-only until the mart view is fixed | Open — warehouse-side, tracked separately |
| C-57 | **MBA PDF refused 163/180 versions with no frozen `approved_slice`** — slice is written only in the Postgres publish txn, so booked/completed plans with `schedule_months` still hit `MISSING_SLICE`. Synthetic `__service__*` rows must stay out of a derived slice (`rowInApprovedSlice` admits them unconditionally). | FIXED (HF6) — `deriveApprovedSliceFromScheduleRows` at render time only; never persist; 15 versions with no schedule rows skip via `isExpectedDocGateSkipError` |
| C-58 | **Roster alias collision** — `samantha.keah@` and `samantha.murphy@` both list `samantha@assembledmedia.com.au` in `email_aliases`. Meeting `**Name**` blocks and time drafts that resolve that alias had two candidates and no tie-breaker | Open — detect + guard landed (explicit decline, refuse new colliding aliases, Team-tab report). Do **not** auto-resolve or deactivate either row; Luke picks who keeps `samantha@`. Allocated C-58 because C-40 was already the production double-count row. |
| C-59 | **Post-cutover MBAs 404 on the Xano master PATCH** — MBAs created after the 2 Aug cutover have no Xano `media_plan_master` row, so T4b `mirrorPlanToXano` always fails the publish watermark PATCH (`ERROR_CODE_NOT_FOUND`). Live `app_notifications` `kind=xano_mirror_failed` 20 times 7–27 Aug (most recently 2026-08-27T23:37Z). Mirror cannot serve as a rollback target. | Gated off — `XANO_MIRROR_ENABLED` default false (`isXanoMirrorEnabled`); save returns `{ mirror: "disabled" }`; admin retry 409 `XANO_MIRROR_DISABLED`. Internals of `mirrorToXano.ts` unchanged. |
| C-60 | **Drizzle mirrors omit two live columns** — `clients.retainer_end_month` and `tasks.estimated_minutes` exist in Postgres (`information_schema` via `drizzle-kit pull`) and are absent from `db/schema/*.ts`. Do not add them without Luke; SQL remains source of truth. | FIXED — both columns mirrored (`ported.ts` / `codex.ts`); live types date NULL / integer NULL, no default. SQL remains source of truth; `db:drift` is the control |
| C-61 | **Drizzle mirrors dropped index direction and partial predicates** — seven migrations authored correct SQL (0010, 0018, 0018a, 0028, 0030, 0040, 0042) and the TypeScript mirrors omitted DESC / WHERE. Column order, ASC/DESC, and partial predicates are semantics, never exclusions. | FIXED — mirrors match live `pg_indexes`; `db:drift` compares canonical identity (table + ordered cols + direction + uniqueness + canonicalised predicate) and is the control. Exclusions cover writing only (naming, unique-index vs unique-constraint, opclass, parens, casts, RLS, CHECKs) |
| C-62 | **Save from an older version rewrote the newest** — `resolvePostgresSaveMode` had no loaded-version input; edit-page label and submit both filled `publishedVersionNumber` from `latestVersionNumber`. Live 31 Aug 09:15:57 UTC: opening glenda006 v3 (id 719), changing a rate and hitting Save deleted v5 (id 4347) line items + all 8 `schedule_months` and recreated them with v3 content. No new version row. `snapshot_checksum` did not move because it covers schedule money, not rate-only edits — `line_items.created_at` is the evidence a save landed. | FIXED — `editingVersionNumber` + `buildSaveModeInput` shared by label and submit; older+save cuts `new_version` at newest+1. Create omits the field. |
| C-63 | **Xero sync wrote invoices but never `xero_sync_log`** — live 1 Sep 2026: `last_synced_at` only 2026-07-10 (backfill) and 2026-09-01 (144 rows); newest log row is 2026-07-11 Xano `failed`. Resume reads `ORDER BY id DESC` so a failed log write leaves the next run on a **stale If-Modified-Since** (re-ingest, not no-op). Log INSERT is now fail-open. 51-day ingest gap cause is C-70 (ETL truncate-reload), not a silent INSERT. | Open — health check + fail-open log write landed; gap root cause is C-70 |
| C-64 | **`finance_billing_records` split-brain** — reads were Postgres (`DATA_BACKEND_FINANCE`) while mark-billed / notes / PATCH / line-items wrote Xano. Live 1 Sep 2026: Xano GET 444 rows (1 `media:` + 443 `xero:`); Postgres 480 (1 `media:` `media:BICAU003:2026-06` + 479 `xero:`). The one app-written Xano row was already in PG; `finance_edits` had a single `billed:media:BICAU003:2026-06` event. Identity is `invoice_key`, not Xano id. | FIXED — writes via `lib/data/writeFinance.ts`; `xero:` refused; echo reads Postgres; `0051` applied (0 inserted; 1 media: already present) |
| C-65 | **Xero GST-inc stored as internal money + invoiced double-count** — `importBillingRecords` copied Xero Total (inc-GST) into `finance_billing_records.total` / `billed_amount_cents`; Costs AP used `xero_ap_bills.total` against ex-GST booked delivery. Overview Invoiced to date summed every `billed IS TRUE` row, so after T0-1 a `media:` mark-billed plus the matching `xero:` import could count twice. Live 1 Sep 2026: 479 matched xero: rows, stored billed $9,753,196.79 vs sub_total $8,899,778.54 (movement -$853,418.25). | FIXED — store/compare `sub_total`; invoicedToDate `LIKE 'xero:%'`; `0052` applied (479 rows, movement -$853,418.25) |
| C-66 | **Xero matcher ignored bracketed MBA tokens** — `TOKEN_RE` treated `(PENFOLD018)` as one token, so FY26 AR rows with the finance convention never set `mba_number`. Scope ids (`*_sow*`) were reported as "No MBA found". Measured: 332 FY26 unmatched; 44 bracketed tokens; 41 unambiguous MBA. | Matcher updated (`()[]{}` separators + SOW class). Historical `mba_number` and `xero_sync_exceptions` not backfilled — report-only `scripts/verify/t03-mba-rematch.ts` |
| C-67 | **Xero PDF sync skipped every migrated invoice** — `sync_pdfs` selected `pdf_file IS NULL` only, so 1,207 FY26 Xano empty stubs (non-null, no `url` key) were never fetched; genuinely-null rows then 429'd with no backoff, so `/finance/costs/invoices` PDF links resolved to nothing | FIXED — pending is `IS NULL OR NOT (pdf_file ? 'url')`; 429 Retry-After / exponential jitter (cap 5/row); per-row isolation; batch 50 / `XERO_PDF_BATCH_SIZE` |
| C-68 | **PC5 collectors query columns that do not exist** — `collectMediaMonthAggs` joins `media_plan_masters.version_number` (column does not exist; published tip is `published_version_id`). `collectSowMonthAggs` selects `clients_id` and `months` from `scope_of_work`; neither exists (`client_name` / `billing_schedule`; the SOW path catches and returns `[]`). Do not invent a join here — the PC5 repair owns both collectors. Live: `FINANCE_PERIODS` off so crons skip; admin run/lock no longer pass `force` while the flag is off. | Open — record only; fix in the PC5 repair |
| C-69 | **Invoicing inline amount silently discarded** — `MoneyInput` calls `onChange` only on its own blur, then the cell's `onBlur`/`Enter` read stale `draft` and hit the 0.005 no-op guard. Typed Tab/Enter saved nothing and toasted nothing. Already-billed invoices were editable with no warning. | FIXED — cell commits the parsed `onChange` argument; Enter blurs into that path; billed marker + toast + confirm (`billed_drift`). Tests: `test:inline-schedule-amount` |
| C-70 | **db:etl truncate-reloaded the Xero tables from the 10 Jul Xano export**, destroying every Postgres ingest, every runXeroSync log row, and the resume watermark. Cause of the 11 Jul – 31 Aug ingest gap. | FIXED (`dd1ccbf2`) |
| C-71 | **CB-5 `notes::jsonb` 22P02 on every cron/pull** — `xero_sync_log.notes` is text; all 12 live rows are prose. `WHERE notes::jsonb->>'source'` (ingest resume + draft-match last-pulled) evaluated the cast across the table before LIMIT 1 | FIXED — CASE-guarded extract in `lib/xero/syncLogNotes.ts`; prose stays cron-eligible. `0056` source column authored, not applied |
| C-72 | Approve snapshot trusted the browser grain (`total` / `line_items`); media/sow `composeInvoiceKey` still omits `clients_id` (separate migration if that belongs in the key) | FIXED — POST `/api/finance/billing/approve` is keys + month; server composes and snapshots. Key shape unchanged. |
| C-73 | **Excel download stamped `exported_at`**, so one exploratory workbook lock made unapprove 409 forever; `reapprove` existed on the API with no UI, and nothing cleared the stamp | FIXED — download does not stamp; "Mark as sent to finance" is the writer (AlertDialog); `POST /api/finance/billing/unmark-exported` clears `exported_at` only; drifted approved rows expose "Re-approve at the current amount"; sent-to-finance must un-mark first (`ALREADY_EXPORTED`) |
| C-74 | **GET `/api/finance/sections/draft-match` mutated** (`persistAutoStamps` rewrote `matched_at` on every page load); pull-xero 1/min lived in a process-local Map (void on Vercel isolates); CB-5 wired untracked ingest tests into `package.json` | FIXED — GET is a pure read; auto-stamps run on `POST /api/finance/sections/pull-xero` (stamps transactional; ingest/import are not in that txn); unchanged pairs skip `matched_at`; rate limit is best-effort last completed pull-xero log row per `notes.pulled_by` (does not reserve); `0057` unique index authored not applied; untracked ingest files unwired |
| C-75 | **CB-2 swapped invoicing KPI math to `hasBillingEvidence` but left tiles labelled Billed / Outstanding**, so after 0053 (every row `ready`) Billed showed `$0.00`. Same commit gated the inline-amount confirm on evidence only, so the one legacy `billed=true` / `state=ready` row no longer prompted. | FIXED — tiles relabelled Approved & beyond / Not yet approved (calculation unchanged); write gate is evidence **or** `billed === true`; display chip stays on state; strip uses StatTile four-state (no `$0.00` while loading/error) |
| C-76 | **Editing the Drizzle mirror is a deploy even when the migration is only authored.** CB-0 added eight columns to `db/schema/ported.ts` before Postgres had them; Drizzle's explicit column list made every finance `db.select()` 500. `db:drift` already compared columns but was not loud, not fixture-tested, and not in CI. | FIXED — mirror-ahead columns print a FATAL banner; `npm run test:db-drift` fixtures run in CI; live `npm run db:drift` remains the pre-merge gate against the applied database |
| C-77 | **Home Media Spend to Date joined MBA keys case-sensitively.** `/api/dashboard/planned-to-date` keyed `byMba` on `mba_number` as stored; `DashboardOverview` looked up `mp_mba_number` as stored. Production: 70/194 masters mixed-case; live-today split 22 lowercase / 11 mixed; tile showed the lowercase half only. Same silent miss on master↔version joins in overlay, list caches, finance relevance, and client-hub published watermark. | FIXED — `mbaJoinKey` (trim + lowercase) on both sides of the tile join and the sibling in-memory MBA maps; stored/displayed `mba_number` unchanged |
| C-78 | **Save-equals-publish volume.** `SAVE_PUBLISHES_IMMEDIATELY` (default on) makes version count = save count. Today ~30 versions/week (~2.2 per master touched). A planner iterating a 100-line OOH plan saves many times per version cut; 3×–10× more version rows is the realistic range (1,124 versions now → five figures in a year). The parked decision — renaming or pruning old versions on promotion — is now an operational requirement, not a later nice-to-have. Working-draft path is suspended, not deleted (flip the constant). | Open — decide prune/rename this week |
| C-79 | **Ingest has no publisher value-level synonym store.** `publisher_profiles` holds `column_map` (header→field), `legend_map`, `detect_signature`, `sheet_rules`, `grouping_keys` — not publisher-string→AV-canonical. JCDecaux "DIGITAL LARGE FORMAT" therefore cannot be learned onto the profile the way `remapIngestColumn` persists column mappings. A human `ingest:value:*` answer rewrites the staged proposal only; the next upload asks again. | FIXED (this commit) — `publisher_value_synonyms` (`0060` AUTHOR ONLY); `learnSynonym` on value-card confirm; publisher-scoped auto-applies, global is a suggestion only |
| C-80 | **Chat remap destroyed and invented `column_map` keys.** `isSkipAnswer("Leave unmapped")` is false, so four JCDecaux money cards treated that answer as `mappedTo: null` and `applyColumnRemap` deleted the keys. The Format required card then wrote `column_map["Large Format"] = "format"` with no check that the header exists on the sheet. | FIXED — `validateRemapHeader` against review headers; Leave unmapped / Not in this file decline (record, write nothing); Remove this mapping is the only delete; invented header does not record so the card re-opens; `0059` audit AUTHOR ONLY |
| C-81 | **`serializeBurstsJson` writes `budget` verbatim** while zeroing `mediaAmount` / `feeAmount` for bonus / package_inclusions. A stale non-zero `bursts[].budget` can persist after the user clears cost. Named billing/report consumers use `mediaAmount` or `computeBurstAmounts` (buyType-aware). Residual readers of raw `burst.budget`: `computeCampaignFinancials.resolveBurstBudgets`, `lib/finance/utils.ts` month overlap. Editor `enteredAmount` no longer refills a stated `"0"` from the line total. | Open — do not zero `budget` on persist without a consumer sweep |
| C-82 | **`buildPostgresSavePayload` still back-fills `enteredAmount` from `totalMedia` when burst budgets sum `<= 0`.** Same defect as the editor mapper, on persist. After `mapBurst`, blank and `"0"` are both number `0`, so `resolveBurstBudgets` cannot tell them apart — do not gate the even-spread there or the legacy all-blank + line-total path regresses. | FIXED (this commit) — stated-zero test on the save snapshot: back-fill only when no raw burst states a budget; production keeps `enteredAmount <= 0` |
| C-83 | **CB-FIX-8:** approved freeze did not cover line-item DELETE (bare `DELETE … WHERE id =`); unapprove / mark-exported returned `ok: true` even when every key failed (typed client declared `ok: true`) | FIXED — DELETE joins parent and requires `r.approved_at IS NULL` (PATCH sibling); `ok: billingBatchOk(errors)` / `ok: boolean` |
| C-84 | **Invoicing `po_required` blocker can never fire.** `invoicingClientBlockerMeta.ts` read `row.po_required` off `GET /api/clients`; no table has the column (`clients`, `finance_billing_records`, Drizzle mirror). The `missing_po` gate was removed because nothing backed it and enforcement was not wanted. Do not add `clients.po_required`. | FIXED (this commit) — gate removed; ABN / legal name / sow $0-or-missing-month blockers unchanged |
| C-85 | **`db:etl` still truncate-reloaded `finance_billing_records` / `finance_billing_line_items`** after T0-9 protected the nine Xero tables. Lifecycle stamps (`approved_*` / `exported_*` / `matched_*`) have no Xano snapshot twin — a reload would wipe every approval and every Xero match and reset the identity sequence. Same class as C-70. | FIXED — both names in `POSTGRES_AUTHORITATIVE_TABLES`; ETL skip path `continue`s past truncate/insert |
| C-86 | **`db:recon` still hard-failed billing counts** after C-85 / `c775cd3a` put `finance_billing_records` / `finance_billing_line_items` on the ETL skip list only. `POSTGRES_AUTHORITATIVE_RECON_TABLES` and `ONE_TO_ONE` were a second set; billing stayed on the 1:1 gate (C-64 480 PG vs 444 Xano). | FIXED — both names in `POSTGRES_AUTHORITATIVE_RECON_TABLES`; commented out of `ONE_TO_ONE` (T0-9 shape) |
| C-87 | **Receivable path reads persisted billing, it does not recompute.** `getBillingSchedule` is the version blob; `computeCampaignFinancialsFromVersion` parses it (or PC1 `schedule_months` rebuild). Flipping `client_pays_for_media` without a save leaves stale non-zero months on that line. SF-8 drops flagged lines on read; SF-11 also refuses when the joined plan line is client-pays (`(version_id, line_item_id)`, dual-shape) and stamps `clientPaysForMedia` on the `DATA_BACKEND_FINANCE_SCHEDULE=rows` rebuild. It does **not** recompute-on-read (finance hub is not channel-hydrated) and does **not** backfill headers — blob is clean today (probe: 15,586 entries / 138 client-pays / $0 leaked). When the month header still includes leaked media, `record.total` disagrees with remaining `line_items` — that is the persisted-schedule bug, not a derive-total bug. Probe + Luke pick: (i) recompute on read, (ii) re-zero persisted client-pays months, or (iii) both. | Open — SF-8 + SF-11 refusal landed; wait for Luke before (i)/(ii)/(iii) |
| C-88 | **Expert grid multi-cell Delete / Cut / cell-range drag.** Selection model exists (`weekRectSelection` + derived `multiCellSelection`, `weekMultiSelect`, `weekStripSelection`, `focusedCell`). Delete+Cut: `resolveWeeklyExportSelection` takes `weekMultiSelect` (order: rect → multiSelect → strip → mergeTarget → focused); week-cell Delete/Backspace range-clears via `applyWeeklyCutToRows` on logical rows when the selection is more than one cell (single caret stays native — `handleExpertGridInputKeyDown` still ignores Delete); clipboard write failure still clears (toast). Cut also drops covered `dailyValues`. Cell-range *move* was never built — HTML5 drag is `single` \| `merged` only; fill-handle is fill-down; `useExpertRowReorder` is rows. Tests: `expertGridSelectionOps.sf9.test.ts` (range-move RED backlog). | Delete+Cut FIXED (this commit); range-move still Open |
| C-89 | **Prog BVOD editor GET still uses catch-all `/api/media_plans/media_plan_prog_bvod`.** Catch-all `GET` is staff-only (`requireRole(["admin"])`) before the postgres channel handler. Sibling programmatic channels hydrate via dedicated `prog-display` / `prog-video` / `prog-ooh` routes with `checkClientMbaAccess`. Non-admin (and a 403) paints the section error card "Failed to load Programmatic BVOD" and never mounts the container; a hang holds Save (`Waiting for Prog BVOD`) until the 150s watchdog. Shared `containerChannelConfig` flags already match progVideo — `site` / `targetingAttribute` are video-only, not a hydration blank. Tests: `progBvodLoad.sf10.test.ts` (RED on dedicated route). Same catch-all fetch remains on `progAudio` and several legacy `media_plan_*` keys. | FIXED (this commit) — dedicated GET for all eight catch-all channels (`prog-bvod`, `prog-audio`, `radio`, `magazines`, `ooh`, `digi-display`, `digi-audio`, `digi-video`); `LINE_ITEM_BROWSER_API_PATH` retargeted; `resolveChannelLineItemEndpoint` maps kebab → `media_plan_*`; catch-all admin gate unchanged. Tests: `test:channel-line-item-routes` |
| C-90 | **`delivery_source_map.derive_spend_from_plan`.** Quantcast programmatic rows are `true`; DSP rows are `false`. The flag is OFF for all Direct Booked Digital (not in the seed), so `lib/pacing/ad-serving/*` and `lib/pacing/overview/mapOverviewItems.ts` keep no-spend row shapes. Spend derivation is live in `deriveSpendFromPlanRate` / programmatic metrics (`actualsDaily.spend` modelled, capped at planned total, labelled modelled; deliverables uncapped). Runtime lookup is still the TypeScript seed in `lib/delivery/deliverySourceMap.ts` until 0063 is applied — do not `db.select()` the table (C-76). `partner_file` is in the CHECK and has no seed rows and no Snowflake channel. | Spend derivation FIXED (this commit); live SELECT still waits for 0063 apply |
| BUX-1 | **False "manual billing differences" banner (C-34 echo)** — `compareBillingDivergence` matched saved vs auto rows by strict `line.id`, so bare `supabase001PB1` vs `billing-…::supabase001PB1` reported as two `missing_in_*` lines on first load of an untouched draft; banner could also linger / reappear inconsistently vs working drift | FIXED (`2ec7538a`) — canonical id collect + `billingOverrideLineIdsMatch` in comparator; debounce syncs single MbaBillingModal banner with ack/reset |
| BUX-2 | **Save billing changes appears to do nothing** — `handleManualBillingSave` treated expected `preservedManualOverrides` (manual≠burst after a real month edit) as a hard gate, set `billingError`, and returned with **no toast**; the error panel only renders inside Advanced editor, so MbaBillingModal footer Save looked dead | FIXED — only `blockingErrors` gate the override PATCH (preserved = notices, campaign-save parity); every exit path toasts; blocking opens Advanced editor |
| BUX-3 | **MBA & billing modal jargon overload** — overlapping chips (`N manual`, `billing≠delivery:N`, Prepaid/Prepayment/Manual, glyph `≠` pills) + unexplained amber month dots | FIXED — one status row (version · approval · Auto/Manual billing); concept chips title-case + invoicing tooltips; month dots hover "This month differs from auto billing"; divergence stays in BUX-1 banner words |
| BUX-5 | **False divergence banner during channel hydrate** — `compareBillingDivergence` ran when `loadPhase === ready` but channels still loading; auto-reference from empty line arrays flashed "N months differ" then self-corrected | FIXED — gate compare + banner + ack on `allChannelsHydrated` (same as "Loading channels…" / `reconciliationReady`) |
| BUX-6 | **Save billing changes never persists Prebill** — `applyBillingLineMode` used strict `line.id ===` so bare schedule ids vs `billing-…::` scope ids skipped the `billingMode=manual` stamp; `persistManualBillingOverrides` wrote 0 `billing_overrides` rows but still toasted success; reload looked like auto | FIXED — canonical id match in `applyBillingLineMode`; persist unions Prebill meta; refuse empty success toast; `replace_line` mirrors billing-basis `schedule_months` as `source='override'` (delivery untouched) |
| MB-1 | **`savePlanVersion` computed financials without `billing_overrides`** — overrides attached only for C2 sum validation after explode; `legacy_schedules` blob stayed auto while `schedule_months` got `source='override'` via reconcile. Default `DATA_BACKEND_FINANCE_SCHEDULE=blob` (and `shadow`) served the auto blob (supabase001 v1162: Aug/Sep override vs auto split) | FIXED — txn order: resolve versionId → load overrides → attach → `computeCampaignFinancials` → explode → reconcile; blob built from override-attached billing; delivery override-free; fixture in `savePlan.test.ts` |
| MB-2 | **Publish orphaned `billing_overrides`** — overrides key by `version_id`; publish inserts a new version row then MB-1 reads the new id (empty) so every `schedule_months` row became `source='computed'` and manual billing was lost with no warning/audit | FIXED — publish/`new_version` copies tip−1 overrides onto the new versionId before the financials override read; deleted-line overrides dropped + named in `droppedBillingOverrides`; C2 re-gated; `app_notifications` kind `billing_overrides_publish_carry` |
| MB-3 | **`reset_line` left orphan `schedule_months` source=override** — `resetBillingOverrideLine` deleted `billing_overrides` only; no inverse of `mirrorOverrideIntoBillingScheduleMonths`. Live: empty overrides table + v1162 still had Aug $20k / Sep $0 billing override rows while delivery/blob were auto | FIXED — reset restores billing-basis months via override-free `computeCampaignFinancials` (not delivery copy); replace_line mirrors full month span + server C2 sum gate; edit reset blocks when version id unresolved. AUTHOR cleanup SQL for v1162 in commit notes |
| MB-4 | **Strict `line.id === lineItemId` still live (C-34 class)** — Prebill uncheck, schedule cell prev-amount, `rebalanceLineOnSchedule`, and `findMediaKeyForScheduleLine` used `===` so bare scope ids missed `billing-{media}::` schedule rows (silent no-op on Prebill off). Create twin had the same uncheck/cell paths | FIXED — `billingOverrideLineIdsMatch` at those sites + `restoreLinePrebillSnapshot`; CI `npm run check:billing-line-id-equality`; regression `test:billing-line-id-match` |
| MB-11 | **Map/Set membership on billing line ids not canonical** — MB-4 guard caught `===` but missed `.has(lineItemId)` / `.get(line.lineItemId)`; MB-2 `livingLineItemIds.has` could drop a live override on publish when one side was decorated | FIXED — `buildCanonicalBillingLineIdSet` / `canonicalBillingLineIdSetHas`; both sides of living-set + `collectScheduleLinesById` + attach/preserve/gates Maps; guard widened to membership + `lib/data` |
| MB-12 | **Delivery blob stamped billingMode/preBill from overrides** — `attachScheduleLineDetail` gated fee amounts on `basis==="billing"` but stamped `billingMode` / `feeBillingMode` / `preBill` from overrides on both bases; since MB-1, delivery carried manual/prepaid metadata over auto amounts → false positives in `computeCampaignFinancialsFromVersion` perLine flags | FIXED — gate those three fields on billing basis; delivery reports auto / no preBill; billing unchanged |
| MB-13 | **Fee override vs approved_slice / mba_fee_snapshots unpinned** — open decision whether fee override could change the amount vs timing-only; slice/snapshot/post-publish/media+fee reset unpinned | FIXED (decision + pin) — **6 Aug 2026: fee override is timing-only; months must sum to AUTO fee (±$0.01) on savePlan (`validateManualOverrideSumRules`) and `replaceBillingOverrideLine` (SUM_VIOLATION).** Pack: `feeOverridePublish.mb13.test.ts` retarget; write-path fee sum gate in `writeBillingOverrides.ts`; INVARIANTS timing-only law; `mbaFeeAdjusted` retained as defect detector only. First publish freezes auto `feeCents` into `approved_slice`, retimes billing `schedule_months` with `source=override`, `mba_fee_snapshots.fees` stays rate JSON; post-publish fee override refused by MB-15c; media+fee Prebill + `reset_line` clears both |
| MB-15c | **Published version billing mutated after freeze** — `replace_line` / `reset_line` gated only on `assertVersionOwnedByMba`; timing edit on approved/booked rewrote `billing_overrides` + billing-basis `schedule_months` after `snapshot_checksum` froze → PC3 / regenerated MBA PDF mismatch | FIXED — `isApprovedOrBeyond` gate → `VERSION_PUBLISHED_IMMUTABLE` (409); MbaBillingModal timing read-only; `publishedBillingImmutable.mb15c.test.ts`. Stage 1 keeps mutability on commercial status (not `published_at`) — planned/draft stay mutable while downloadable |
| MB-16 | **Published-plan billing retiming unavailable until VC-3** — after MB-15c, approved/booked/completed cannot retarget billing in place; only path is publish a new version. Revisions (VC-3) will hold overrides on the revision and promote on Publish; MB-2 tip−1 carry is transitional until then | Open |
| MB-17 | **`applyBillingOverrideRowsToMonths` broke months[0] read contract** — overlay wrote each month amount onto only that month row's line instance; grid reads `resolveManualBillingLineItemAmount` → `months[0].monthlyAmounts[monthYear]`, so month 2+ showed stale AUTO after Save (DB correct; right schedule panel correct). Live: supabase001PB1 Prebill Aug $20k / Sep $0 → Save snapped Sep to $9,836.07 Off-by chip | FIXED — overlay routes through `syncLineItemMonthlyAmountAcrossAllMonthRows` / fee twin (mediaKey via `collectScheduleLinesById`); regression asserts via months[0] read path for 2-mo + 6-mo media and fee |
| MB-18 | **`billingOverridesLoadNotice` latched on unresolved version id** — `handleManualBillingOpen` else branch set "Manual billing overrides could not be loaded…" when `isUsableBillingVersionId` was false (load race, not failure); `refreshBillingOverrideRowsForPanels` early-return did not clear → permanent amber banner on a clean page. Also: null mediaKey after meta stamp left mode=manual + AUTO amounts silent | FIXED — `nextBillingOverridesLoadNotice`: unresolved/ok clear, fetch_failed only sets; refresh early-return clears; mediaKey miss `console.warn` once per line id |
| MB-19 | **Orphan `billing_overrides` row blocks every Save billing changes** — deleted line leaves override at draft tip (cleanup only on publish); `applyBillingOverrideRowsToMonths` stamps meta before mediaKey miss skip; BUX-6 meta fallback pulls orphan into `mediaWriteIds`; write loop `return {ok:false}` on empty `monthsIso` aborted sibling lines; reset_line skipped because canon stayed in `currentMedia` | FIXED — choice (b): empty monthsIso → per-line skip (leave DB row; cannot distinguish deleted vs hydrate lag); result reports `skippedEmptyMonths`; toast names count + "plan line(s) no longer present"; orphan-only still hits BUX-6 Nothing to save |
| MB-5 | **Dead Xano `fetchBillingOverridesForVersion` soft-failed to `[]`** — axios GET in `billingOverrides.ts` treated missing endpoint/auth/errors as no overrides; zero callers but rewiring would erase manual billing silently | FIXED — function + Xano imports deleted; attach helpers kept; reads stay on `readBillingOverridesForVersion` |
| MB-6 | **Save billing changes stranded empty timing draft** — success path closed Advanced and `setManualBillingMonths([])` but left `manualBillingDraftReady` true while MBA modal stayed open → zero month inputs, false "Off by $lineTotal", second Save fee-drift vs $0; stale `manualBillingOverrideMetaRef` could also yield "No month amounts found to persist" | FIXED — option (a): rebuild draft from just-persisted overrides via `rebuildTimingDraftAfterBillingSave`; empty-draft validators skip; teardown clears months+ready+meta together |
| MB-7 | **MBA & billing modal dual money sources** — left Adjust timing read `manualBillingMonths`; right schedule / pills / scope read `campaignFinancialsForPanels` (overrides only). Nothing synced them → live "$0 Off by $20k" next to "$20k Matches MBA" (C-34/BUX-1/5/6 shape) | FIXED — `resolveMbaBillingModalState`: one view = core + override rows + draft layer; healthy draft session shares `billingSchedule`/`resolvedMonths` by reference; stranded draftReady+empty → both halves empty; C2/fee-drift gates read resolved months |
| MB-8 | **Prebill badge lied + fee-drift on media-only** — `applyLinePrebillToMonths` moved media only (fees stayed on delivery) but UI said "Prepaid" and fee-drift fired on legitimate media-only timing | FIXED — compact `PrebillScopeDialog`: Media only (default) vs Media + fee; session memory per draft; fee lane via `applyLineFeePrebillToMonths` + `component=fee` replace_line; badges: Prepaid = media+fee, Media prepaid = media-only (same word on container/line/timing); fee-drift skips when month fees still sum to derived |
| MB-9 | **Manual billing status over-alarmed** — one deliberate Prebill stacked header "Manual billing —1line", container Manual, Prepaid badges, amber month dots, and Acknowledge banner for expected timing | FIXED — header spacing via `manualBillingHeaderLabel`; one vocab map (`manualBillingVocabulary.ts`); container shares Prepaid/Media prepaid/Manual; banner gated on `isUnintendedBillingDivergence` (line_total / adserving_total / missing_* only); amber month dots suppressed |
| MB-10 | **Fee dialog DISPLAY still summed raw draft** — MB-7 routed the gate through resolved months but Advanced "Current sum" / fee row Total still reduced `manualBillingMonths` alone (Aug 2: "$0.00" vs $5k schedule) | FIXED — `monthsForMbaBillingGates` shared by gate + display; fee sum/drift via `validateAgencyFeeMonthTotalDrift` on those months; create twin fee Total converted |
| MB-14 | **Post-persist override refetch empty recovered silently** — after successful persist, `metaByLine.size === 0` kept prior meta with no warn; catch only `console.warn`'d the message | FIXED — retain prior meta still, but `billing_overrides_refetch_anomaly` app_notifications + loud warn (versionId/mba/persist counts); empty + throw paths |
| MB-24 | **False "· saved" before Apply + Cancel commits Prebill** — `billingOverrideRowsForPanels` mixed fetch with optimistic Prebill/uncheck/reset/date-basis writes so provenance read `tableRows.length > 0` as saved; Cancel cleared pending but left that cache so campaign Save REPLACE-SET wrote cancelled overrides | FIXED — rename to `savedBillingOverrideRows` (fetch-only); three-state provenance draft/`not applied` > pending/`unsaved` > saved; save merges pending over saved only; `manualBillingVocabulary.mb24.test.ts` |
| MB-25 | **Save wiped overrides on failed/unknown GET (GR-A) + Reset re-inserted after refetch (GR-B)** — `savedBillingOverrideRows=[]` on fetch_failed/version_unresolved asserted zero overrides → REPLACE-SET deleted all; Reset stripped pending but reopen refetch restored saved → merge re-inserted | FIXED — three-valued load state; envelope `{authoritative,clearedLineIds}`; server skips REPLACE-SET when not authoritative; Reset tombstone survives refetch; Cancel undoes tombstone; `billingOverridesSaveIntent.mb25.test.ts` |
| MB-26 | **Apply left MBA modal open; five overlapping exits; Done discarded un-Applied work; amber "unsaved" vs header "saved"** — Apply only closed Advanced; Done cleared draft while reading like confirm; toolbar+footer duplicate Cancel; ChatWidget could cover Apply | FIXED — Apply terminal (close modal + tear draft, keep pending); Done removed; one footer Cancel (left) + Apply (right); tools grouped left; dialog above Ask Ava via stacking scale (MB-29) + footer pr-28; shared provenance; `manualBillingVocabulary.mb26.test.ts` |
| MB-29 | **Nested confirms under MbaBillingModal froze UI** — AlertDialog stayed at z-50 after MB-26 raised the MBA modal; invisible overlay ate clicks/focus. Same class: Select-in-dialog would fail if Dialog rose without raising popovers | FIXED — named overlay scale (`lib/ui/stackingLayers.ts` / theme zIndex); AlertDialog=`z-nested`; Select/Dropdown/Popover=`z-popover`; Dialog `layer="nested"` for same-tier rule; `check:stacking-layers` |
| MB-30 | **Add channel duplicates overridden line in workingBillingMonths** — append merge can leave two progBvod rows (prepaid + AUTO / `::new-N`); banner false-alarms; validateBillingBeforeSave may false-block. DB + container save payload stay clean | Open — loud guard only tonight: canonical-id dedupe at end of merge + `working_billing_canonical_dedupe` app_notifications + template/container id logs. Does **not** fix distinct-canon `::new-N` ghost; ticket stays open |

## Finance hub UX (UX-*) — FN7 sections cutover (pending live verification)

| ID | Issue | Status |
|---|---|---|
| UX-1 | **Load-gate** — classic hub toolbar required explicit Load after filter Apply; easy to stare at stale/empty data | fixed pending live verification — classic hub deleted; sections use Apply → auto-load (`useFinanceScope` / section data hooks); not FIXED until live smoke passes |
| UX-2 | **$0 / empty landing** — hub Overview KPI hero + empty month range looked “all zeros” before Load | FIXED (FIN-1) — Overview retired; `/finance` → `/finance/invoicing` (Clients billing) |
| UX-3 | **Dead treemaps on hub Overview** — `global-monthly-*` / exact schedule join → black empty charts (related C-26 on dashboard path) | fixed pending live verification for hub surface — Overview treemaps deleted with hub; sections costs/investment use `scheduleLineJoinSql` (C-26 residual remains on `dashboardMonthlySpend.ts` only) |
| UX-4 | **Periods and Xero have no scope bar** — they still cannot change month range or clients on those pages. Periods hydrates `applied` from the URL on mount via `hydrateFromUrl` (same path as `SectionScopeBar`) so the FY notice and board match `?fy=`. Xero does not hydrate from the URL and does not show a FY notice (it ignores finance scope until a bar lands). Do not infer FY from `campaign_status`. | Open — add a scope bar (or FY-only bar) on Periods and Xero; not this pack |

## Build / tooling (B-*)

| ID | Issue | Status |
|---|---|---|
| B-1 | `npm run build` failed: Edge instrumentation traced `instrumentation.ts` → `clientsCache` → `readClients` → `db/index.ts` → `postgres` (Module not found: crypto/stream/tls/net). Latent follow-ons once that cleared: client `edit/page.tsx` imported `publishVersionIntegrity` → `readMediaPlans` (`server-only`); `lib/finance/c1FullScopeGate.ts` UTF-16 LE → ESLint "File appears to be binary"; WIP `scripts/live-*` / `_s1p1b-*` pulled into `tsc` via `**/*.ts` | FIXED — Node warmers in `instrumentation.node.ts` via `webpackIgnore`; `serverExternalPackages: ["postgres"]`; `db/index.ts` is `server-only`; client helpers in `publishVersionIntegrityClient.ts`; `c1FullScopeGate.ts` re-encoded UTF-8; WIP probes excluded from `tsconfig.json` |
| B-2 | **Repo-wide test gate never existed** — no aggregate `test` / `test:all`; “tests green” on commits meant nothing. First real baseline via `npm run test:all` on **2026-08-04**: **41/44** named suites PASS (then RED: finance-schedule, mb13, mb15c). After MB-13 timing-only retarget: `npm run test:all` reports **46/46** named suites PASS (including previously RED mb13 / finance-schedule / mb15c). Gate: do not claim typecheck/tests green without `typecheck` + `test:all`. | FIXED (gate) — `test:all` green at 46/46; keep running the gate before claiming green |

## Performance / caching (P-*)

| ID | Issue | Status |
|---|---|---|
| P-1 | Pacing rows cached 4h (`unstable_cache`); ONLY invalidation trigger is orphan-assign. Plan edits/Fivetran refreshes don't bust it. Cache key includes `asOfDate` → guaranteed cold full-crawl every Melbourne midnight | Known trade-off |
| P-2 | `fetchAllMasters` + `fetchCurrentVersionRowsForMasters` = full unpaginated crawls of master + versions tables, invoked ~6× on a cold Overview. **S8 soak:** finance hub `relevantPlanVersions` still Xano-crawls masters+all versions (~4.5s for 2026-08) even when `DATA_BACKEND=postgres` — dominates hub load vs shadow | Open |
| P-3 | N+1 to Xano per MBA with up to 5 param-shape attempts per channel; cold MBA GET = 20 channels × up to 5 attempts (`maxDuration=60` is load-bearing). Mitigated when `DATA_BACKEND_PLAN_DETAIL=postgres` (one `line_items` query set; C-22 / `6f1f0323`) | FIXED (X2) — PLAN_DETAIL default postgres; Xano fan-out removed from MBA GET |
| P-4 | `getSearchCampaignsPacingData` has no date clamp / LIMIT — one long line item widens the scan for the whole portfolio | Open |
| P-5 | 4 of 7 coalesced caches have no invalidation path (mediaPlanVersions, mediaPlansList, publishers, publisherKpi); writes invisible up to TTL; all per-lambda | Known trade-off |
| P-6 | Two independent clients caches (10min vs 30s) with separate invalidation — client PUT/PATCH invalidates only one | FIXED (X1) — `invalidateAllClientsCaches()` on create/update/patch via `writeClients` |
| C-32 | **Clients split-brain** — `POST /api/clients` wrote Xano only while `DATA_BACKEND_CLIENTS=postgres` GET served PG; new client invisible to campaign create; `ensureMaster` omitted `client_id` (NULL on new masters); `clients_id_seq` lagged after ETL (`last_value` &lt; `max(id)`) | FIXED (X1) — PG-first writes + Xano mirror + seq sync + `ensureMaster.clientId` + `resolveClientIdForMaster` |
| X9 | **`media_plan_master` identity still Xano** — `POST /api/mediaplans` axios-POSTed Xano unconditionally; that id became `masterId` for `/api/plans/save` `ensureMaster` dual-write into PG — cancelling the Xano workspace broke campaign create | FIXED — PG-first `writeMediaPlanMasters` + seq sync + Xano mirror; MBA uniqueness on PG; `ensureMaster` logged safety net only |
| X9.1 | **`setval(seq, MAX(id))` rewound identity** — per-insert / ETL sync walked `media_plan_masters_id_seq` backwards (live: 10000262 → 10000129) so PG could reissue ids still present in Xano | FIXED — `GREATEST(MAX(id), last_value)` in one SQL statement; hot-path create no longer calls sync; ETL uses the same no-rewind form |
| P-7 | `getDeliveredTotalsFor{Campaign,Client}` bypass the pacing cache → client dashboard pays full Snowflake cost and can disagree with /pacing/direct mid-TTL | Open |
| P-8 | Every pacing tab refetches on mount (no SWR/react-query); stale-silently caches send `x-warning` headers most consumers ignore. **S8:** campaigns list server TTL warm ≈0ms; client remount still re-fetches `/api/mediaplans` (not measured in soak script) | Open |
| P-9 | Four in-memory rate limiters (ava autopopulate, ad copy, search copy, live mockup) are per-instance Maps — ineffective on serverless | Known |
| P-10 | exceljs imported client-side in mega-pages and finance dialogs — large bundle cost | Open |
| P-11 | **`ingestStageStore` is an in-memory Map with no TTL** — staged AVA/Hub packages vanish on cold start or another instance. AVA-UX-1 reports this honestly (`ingestStageMissing`, never "expired"); AVA-UX-2 owns persistence. Do not add a TTL as a side-effect of the honest-failure copy. | FIXED — `ingest_stages` (0050 AUTHOR ONLY) + overlay until applied. 24h TTL. Missing ≠ expired; `expires_at` past is the only expired case. Accept retains (`expires_at` NULL, `master_id` + `accepted_version_id`). Cancelled/blocked keep TTL and sweep via ops-health. |

## Dead / misleading code (D-*)

| ID | Issue |
|---|---|
| D-1 | `lib/avaSnowflake.ts` (219 lines) — zero importers; "repair or retire" |
| D-2 | `lib/cache/ttlCache.ts` — zero importers | FIXED (S8) — deleted |
| D-3 | `lib/billing.ts` (legacy) — no importers | FIXED (S8) — already absent from tree; register closed |
| D-4 | `lib/pacing/calcExpected.ts` — types only, no importers; `lib/pacing/mockMetaPacing.ts` is NOT mock — canonical home of production types |
| D-5 | `lib/delivery/programmatic/_prog_extract.txt` — 780 lines of live-looking TS checked in as .txt | FIXED (S8) — deleted |
| D-6 | `lib/auth/mbaNumberMatchesClientIdentifier.ts` — FIXED (SEC-B): wired into `checkClientMbaAccess` / `resolveClientMbaScope` (was test-only; see SEC-9) | FIXED (SEC-B) — wired into `checkClientMbaAccess` / `resolveClientMbaScope` |
| D-7 | Several channel routes import axios unused; cinema route POSTs to `cinema_line_items` while GET reads `media_plan_cinema` | PARTIAL (S8) — unused axios stripped from television/social/newspaper/influencers/integration/prog-display/prog-ooh; **cinema table mismatch left open** (verify separately) |
| D-8 | `app/management/page.tsx` is a stub (inert date-range picker removed — read-only placeholder only); `docs/archive/design-refresh/BASELINE.md` is an unfilled template | FIXED (management route + stub removed; User Management stays at `/admin/users/new`) — BASELINE.md template still unfilled |
| D-9 | `lib/codex/**` is the Tasks domain, not AVA — naming is misleading |
| D-10 | Stale docs that contradict code: `docs/archive/PERF-DISCOVERY-2-CACHE-DESIGN.md` ("zero unstable_cache usages" — now false), `docs/archive/FINANCE-HUB-STAGES-DISCOVERY.md` (references deleted `components/finance/tabs/`), `README.md` Xano config section (references nonexistent `lib/xano/config.ts`) |

## UI chrome (UI-*)

| ID | Issue | Status |
|---|---|---|
| UI-1 | Create/edit floating save bar stacked the unsaved-draft banner, save-mode line, and validation alerts under the action buttons, overlapping Step 02 | FIXED — same `PlanWizardSaveMessages` card below Draft Summary on both mega-pages (banner, then save-mode, then alerts; empty = not rendered). Floating bar is action buttons only. SavingModal + toasts unchanged |
| UI-2 | Create/edit still passed `saveMode`/`alerts` into `PlanWizardSaveMessages` after that component dropped those props, so the Save-status panel silently rendered nothing | FIXED — both mega-pages pass `issues`, `extraProblemTexts`, `draftBanner`, `savePrimary` / `saveSecondary`, `saveTip` |

## Features / integrations (F-*)

| ID | Issue | Status |
|---|---|---|
| F-27 | Xano `codex` API group (`XANO_CODEX_BASE_URL` + `lib/api/codex.ts` proxy) backs `/api/codex/*` | FIXED (`6772069b` / C0-2) — Postgres-native Codex (`lib/codex/repo.ts`); flag `CODEX_V2=on`; Xano path deleted |
| F-30 | **Create MBA number race / unscoped prefix** — `generateMBANumber` was unsequenced (last response won vs `mp_client_name`); route used `startsWith` + `slice(-3)` so a prefix sibling or suffix &gt;999 could collide; mixed-case identifiers emitted mixed-case numbers | FIXED — `allocateNextMbaNumber` uses `mbaNumberMatchesClientIdentifier` + full trailing digits + lowercase; create page request-token discard + same-client skip. Dead leftover `generateMBANumber` on edit (never called — MBA is the route param) deleted; create still owns the live fetch. |
| F-28 | **FIN-6 legal name / ABN blanks** — Simmone Logue + Olive Grove `legalbusinessname` / ABN intentionally blank; bookkeeper Excel falls back to display name + toast for missing set (`62fefc03`). Internal billing buckets for those clients also stay blank | By decision (Luke) — human updates via Clients admin in due course; not an export defect |

## Open product decisions (needs Luke)

- Rounding unification across the 9 `computeLoadedDeliverables` copies (C-5)
- Excel Column N label vs value (C-1)
- The 3 genuine per-channel deliverable variants for package/package_inclusions/bonus (OOH/newspaper/BVOD return 1 or bonusDeliverables; canonical returns NaN). **Package OOH/Cinema container hotfixes RESOLVED (unified on canonical derived, 2026-08-04, Luke)** — removed OOH `handleValueChange` HOTFIX early-return and Cinema `cinemaBurstDeliverables` preserve branch for `package`; bonus/package_inclusions stay manual via `computeDeliverableFromMedia` → NaN. Residual: newspaper/BVOD/`computeLoadedDeliverables` still special-case package on load.
- `docs/archive/AUDIT.md` §7's ~30 unanswered Stage 1–6 questions (fold the still-relevant ones here over time)
- **Percent-unit contract (AV-25 v2 / C-20):** code landed (`lib/kpi/percentUnits.ts`); dual-store data migration pending — classify via scan CSV; never auto-decide ambiguous `1.0` rows; fix Xano before next `db:etl` truncate-reload
